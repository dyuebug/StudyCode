// ============================================
// 生命周期入门 - 超详细注释版
// 日期：2026-05-31（第4天）
// 难度：⭐⭐⭐ 重要
// ============================================

/*
【核心概念】生命周期（Lifetime）

是什么？
- 生命周期是引用有效的作用域
- 生命周期注解描述引用之间的关系
- 生命周期是Rust类型系统的一部分

为什么需要？
- 防止悬垂引用（dangling reference）
- 确保引用始终有效
- 在编译时保证内存安全

核心特点：
- 生命周期注解不改变生命周期本身
- 只是描述引用之间的关系
- 编译器通过生命周期检查防止内存错误

Rust特色：
- 大多数情况下生命周期是隐式的
- 编译器可以自动推断生命周期
- 只在编译器无法推断时需要显式注解
*/

// ============================================
// 第一部分：核心概念详解 ⭐⭐⭐⭐⭐
// ============================================

fn main() {
    println!("============================================================");
    println!("第一部分：核心概念详解");
    println!("============================================================");

    // --------------------------------------------
    // 1.1 概念1：什么是生命周期
    // --------------------------------------------

    println!("\n1.1 什么是生命周期");
    println!("------------------------------------------------------------");

    /*
    【生命周期】

    是什么？
    - 生命周期是引用有效的作用域范围
    - 从引用创建开始，到引用最后一次使用结束
    - 每个引用都有生命周期

    为什么需要？
    - 防止悬垂引用（引用指向已释放的内存）
    - 确保引用的数据在使用期间始终有效
    - 在编译时检查，零运行时开销

    核心特点：
    - 生命周期是编译时概念，运行时不存在
    - 大多数情况下是隐式的（编译器自动推断）
    - 只在编译器无法推断时需要显式注解

    Rust中的实现：
    - 生命周期注解：'a, 'b, 'static
    - 生命周期参数：<'a>
    - 生命周期约束：T: 'a

    示例：
    ```rust
    {
        let r;                // -------+-- 'a
                              //        |
        {                     //        |
            let x = 5;        // -+-- 'b|
            r = &x;           //  |     |
        }                     // -+     |
                              //        |
        println!("{}", r);    // -------+
    }
    // 错误！r 引用的 x 已经被释放
    ```
    */


    // --------------------------------------------
    // 1.2 概念2：生命周期注解语法
    // --------------------------------------------

    println!("\n1.2 生命周期注解语法");
    println!("------------------------------------------------------------");

    /*
    【生命周期注解】

    是什么？
    - 生命周期注解以单引号开头：'a, 'b, 'static
    - 描述多个引用之间的生命周期关系
    - 不改变生命周期本身，只是描述关系

    为什么需要？
    - 当函数返回引用时，编译器需要知道返回值与哪个参数相关
    - 当结构体包含引用时，需要说明引用的生命周期
    - 帮助编译器验证引用的有效性

    语法：
    - 函数：fn foo<'a>(x: &'a str) -> &'a str
    - 结构体：struct Foo<'a> { x: &'a str }
    - 多个生命周期：fn foo<'a, 'b>(x: &'a str, y: &'b str)

    命名规则：
    - 通常使用 'a, 'b, 'c 等
    - 可以使用有意义的名字：'input, 'output
    - 'static 是特殊的生命周期（整个程序运行期间）

    Rust特色：
    - 生命周期注解是类型的一部分
    - &'a str 和 &'b str 是不同的类型
    - 编译器会检查生命周期约束
    */

    // --------------------------------------------
    // 1.3 概念3：为什么需要生命周期注解
    // --------------------------------------------

    println!("\n1.3 为什么需要生命周期注解");
    println!("------------------------------------------------------------");

    /*
    【为什么需要生命周期注解】

    问题场景：
    当函数返回引用时，编译器需要知道：
    - 返回的引用来自哪个参数？
    - 返回的引用有效期有多长？

    示例：
    ```rust
    fn longest(x: &str, y: &str) -> &str {
        if x.len() > y.len() {
            x
        } else {
            y
        }
    }
    ```

    编译器的困惑：
    - 返回值可能是 x，也可能是 y
    - 编译器无法确定返回值的生命周期
    - 需要程序员明确说明

    解决方案：
    ```rust
    fn longest<'a>(x: &'a str, y: &'a str) -> &'a str {
        if x.len() > y.len() {
            x
        } else {
            y
        }
    }
    ```

    含义：
    - x 和 y 的生命周期至少和 'a 一样长
    - 返回值的生命周期也是 'a
    - 返回值的生命周期不会超过 x 和 y 中较短的那个

    记忆技巧：
    - 生命周期注解 = 给编译器的提示
    - 描述"关系"，不是"长度"
    - 帮助编译器验证引用的安全性
    */
}

/*
==============================================
🔍 深入理解：生命周期的本质（选学）
==============================================

如果你想知道"为什么"，可以阅读这部分。
如果觉得难，可以跳过，不影响后续学习。

【生命周期不是什么】

常见误解：
- ❌ 生命周期不是变量存活的时间
- ❌ 生命周期不是内存分配的时间
- ❌ 生命周期注解不会改变实际的生命周期

实际情况：
- ✅ 生命周期是引用有效的作用域
- ✅ 生命周期注解只是描述关系
- ✅ 编译器用生命周期验证引用的安全性

【生命周期检查器的工作原理】

编译器做什么：
1. 分析每个引用的作用域
2. 检查引用是否在有效范围内使用
3. 验证生命周期注解的约束是否满足

示例：
```rust
fn longest<'a>(x: &'a str, y: &'a str) -> &'a str {
    if x.len() > y.len() { x } else { y }
}

let s1 = String::from("long string");
let result;
{
    let s2 = String::from("short");
    result = longest(&s1, &s2);  // 错误！
}
println!("{}", result);  // s2 已经被释放
```

编译器的推理：
- result 的生命周期是 'a
- 'a 必须至少和 s1 和 s2 中较短的一样长
- s2 在内部作用域结束时被释放
- result 在外部作用域使用，但引用的数据已释放
- 编译错误！

==============================================
*/

// ============================================
// 第二部分：基础操作实践
// ============================================

fn part2_basic_operations() {
    println!("\n============================================================");
    println!("第二部分：基础操作实践");
    println!("============================================================");

    // --------------------------------------------
    // 2.1 示例1：函数中的生命周期
    // --------------------------------------------

    println!("\n2.1 示例1：函数中的生命周期");
    println!("------------------------------------------------------------");

    // 调用 longest 函数
    let string1 = String::from("long string is long");
    let string2 = String::from("xyz");

    let result = longest(&string1, &string2);
    println!("最长的字符串是: {}", result);

    // --------------------------------------------
    // 2.2 示例2：生命周期约束
    // --------------------------------------------

    println!("\n2.2 示例2：生命周期约束");
    println!("------------------------------------------------------------");

    let string1 = String::from("long string is long");

    {
        let string2 = String::from("xyz");
        let result = longest(&string1, &string2);
        println!("最长的字符串是: {}", result);
        // result 在这里使用，没问题
    }
    // string2 被释放，但 result 也不再使用

    // --------------------------------------------
    // 2.3 示例3：结构体中的生命周期
    // --------------------------------------------

    println!("\n2.3 示例3：结构体中的生命周期");
    println!("------------------------------------------------------------");

    let novel = String::from("Call me Ishmael. Some years ago...");
    let first_sentence = novel.split('.').next().expect("Could not find a '.'");

    let excerpt = ImportantExcerpt {
        part: first_sentence,
    };

    println!("摘录: {}", excerpt.part);
}

// 辅助函数：返回两个字符串切片中较长的一个
// 生命周期注解 'a 表示：
// - x 和 y 的生命周期至少和 'a 一样长
// - 返回值的生命周期也是 'a
fn longest<'a>(x: &'a str, y: &'a str) -> &'a str {
    if x.len() > y.len() {
        x
    } else {
        y
    }
}

// 结构体中的生命周期
// 'a 表示：part 引用的数据必须比 ImportantExcerpt 实例活得更久
struct ImportantExcerpt<'a> {
    part: &'a str,
}

// ============================================
// 第四部分：常见错误和陷阱 ⭐⭐⭐⭐⭐
// ============================================

fn part4_common_mistakes() {
    println!("\n============================================================");
    println!("常见错误和陷阱 ⭐⭐⭐⭐⭐");
    println!("============================================================");

    // --------------------------------------------
    // 4.1 错误1：返回局部变量的引用
    // --------------------------------------------

    println!("\n4.1 错误1：返回局部变量的引用");
    println!("------------------------------------------------------------");

    /*
    ❌ 错误代码：
    ```rust
    fn dangle<'a>() -> &'a str {
        let s = String::from("hello");
        &s  // 返回局部变量的引用
    }
    ```

    编译器错误信息：
    error[E0515]: cannot return reference to local variable `s`
      --> src/main.rs:X:Y
       |
    X |     &s
       |     ^^ returns a reference to data owned by the current function

    原因：
    - s 在函数结束时被释放
    - 返回的引用指向已释放的内存
    - 这是悬垂引用

    ✅ 正确代码：
    ```rust
    fn no_dangle() -> String {
        let s = String::from("hello");
        s  // 转移所有权，不是返回引用
    }
    ```

    记忆技巧：
    - 不要返回局部变量的引用
    - 返回 String 而不是 &str
    - 或者接受参数并返回其引用

    Rust特别提示：
    - 这是生命周期检查器的核心功能
    - 防止悬垂引用，保证内存安全
    */

    // --------------------------------------------
    // 4.2 错误2：生命周期不匹配
    // --------------------------------------------

    println!("\n4.2 错误2：生命周期不匹配");
    println!("------------------------------------------------------------");

    /*
    ❌ 错误代码：
    ```rust
    let string1 = String::from("long string");
    let result;
    {
        let string2 = String::from("short");
        result = longest(&string1, &string2);
    }
    println!("{}", result);  // string2 已被释放
    ```

    编译器错误信息：
    error[E0597]: `string2` does not live long enough
      --> src/main.rs:X:Y
       |
    X |         result = longest(&string1, &string2);
       |                                    ^^^^^^^^ borrowed value does not live long enough
    X |     }
       |     - `string2` dropped here while still borrowed
    X |     println!("{}", result);
       |                    ------ borrow later used here

    原因：
    - result 的生命周期是 'a
    - 'a 必须至少和 string1 和 string2 中较短的一样长
    - string2 在内部作用域结束时被释放
    - result 在外部作用域使用，违反生命周期约束

    ✅ 正确代码：
    ```rust
    let string1 = String::from("long string");
    let string2 = String::from("short");
    let result = longest(&string1, &string2);
    println!("{}", result);
    // string1 和 string2 在 result 使用后才被释放
    ```

    记忆技巧：
    - 引用的数据必须比引用本身活得更久
    - 在使用引用之前，确保数据还存在

    Rust特别提示：
    - 这是生命周期检查的典型场景
    - 编译器会准确指出问题所在
    */

    // --------------------------------------------
    // 4.3 错误3：忘记生命周期注解
    // --------------------------------------------

    println!("\n4.3 错误3：忘记生命周期注解");
    println!("------------------------------------------------------------");

    /*
    ❌ 错误代码：
    ```rust
    fn longest(x: &str, y: &str) -> &str {
        if x.len() > y.len() {
            x
        } else {
            y
        }
    }
    ```

    编译器错误信息：
    error[E0106]: missing lifetime specifier
      --> src/main.rs:X:Y
       |
    X | fn longest(x: &str, y: &str) -> &str {
       |               ----     ----     ^ expected named lifetime parameter
       |
       = help: this function's return type contains a borrowed value, but the signature does not say whether it is borrowed from `x` or `y`

    原因：
    - 编译器无法确定返回值来自 x 还是 y
    - 需要显式说明生命周期关系

    ✅ 正确代码：
    ```rust
    fn longest<'a>(x: &'a str, y: &'a str) -> &'a str {
        if x.len() > y.len() {
            x
        } else {
            y
        }
    }
    ```

    记忆技巧：
    - 返回引用时，通常需要生命周期注解
    - 编译器会提示需要注解的地方

    Rust特别提示：
    - 编译器的错误信息非常有帮助
    - 按照提示添加生命周期注解即可
    */

    // --------------------------------------------
    // 4.4 错误4：结构体忘记生命周期注解
    // --------------------------------------------

    println!("\n4.4 错误4：结构体忘记生命周期注解");
    println!("------------------------------------------------------------");

    /*
    ❌ 错误代码：
    ```rust
    struct ImportantExcerpt {
        part: &str,  // 引用，但没有生命周期注解
    }
    ```

    编译器错误信息：
    error[E0106]: missing lifetime specifier
      --> src/main.rs:X:Y
       |
    X |     part: &str,
       |           ^ expected named lifetime parameter

    原因：
    - 结构体包含引用，必须指定生命周期
    - 编译器需要知道引用的有效期

    ✅ 正确代码：
    ```rust
    struct ImportantExcerpt<'a> {
        part: &'a str,
    }
    ```

    记忆技巧：
    - 结构体包含引用 = 需要生命周期注解
    - 生命周期参数在结构体名后声明

    Rust特别提示：
    - 这确保结构体实例不会比引用的数据活得更久
    - 防止悬垂引用
    */

    // --------------------------------------------
    // 4.5 错误5：混淆生命周期和作用域
    // --------------------------------------------

    println!("\n4.5 错误5：混淆生命周期和作用域");
    println!("------------------------------------------------------------");

    /*
    常见误解：
    "生命周期注解会改变变量的生命周期"

    实际情况：
    - 生命周期注解不改变实际的生命周期
    - 只是描述引用之间的关系
    - 编译器用它来验证代码的安全性

    示例：
    ```rust
    fn longest<'a>(x: &'a str, y: &'a str) -> &'a str {
        // 'a 不会让 x 或 y 活得更久
        // 只是说明返回值的生命周期与 x 和 y 相关
        if x.len() > y.len() { x } else { y }
    }
    ```

    记忆技巧：
    - 生命周期注解 = 约束，不是命令
    - 描述"是什么"，不是"做什么"

    Rust特别提示：
    - 生命周期是编译时概念
    - 运行时不存在生命周期检查
    */
}

// ============================================
// 第五部分：生命周期卡片速查
// ============================================

/*
【生命周期注解语法】

函数中的生命周期：
```rust
fn foo<'a>(x: &'a str) -> &'a str {
    x
}
```

多个生命周期参数：
```rust
fn foo<'a, 'b>(x: &'a str, y: &'b str) -> &'a str {
    x
}
```

结构体中的生命周期：
```rust
struct Foo<'a> {
    x: &'a str,
}
```

────────────────────────────────────

【生命周期省略规则】

编译器可以自动推断生命周期的情况：

规则1：每个引用参数都有自己的生命周期
```rust
fn foo(x: &str) -> &str  // 等同于
fn foo<'a>(x: &'a str) -> &'a str
```

规则2：如果只有一个输入生命周期，赋给所有输出
```rust
fn foo(x: &str) -> &str  // 自动推断
```

规则3：如果有多个输入生命周期，其中一个是 &self，
        则 self 的生命周期赋给所有输出

────────────────────────────────────

【'static 生命周期】

'static 表示整个程序运行期间：
```rust
let s: &'static str = "hello";  // 字符串字面量
```

特点：
- 存储在程序的二进制文件中
- 整个程序运行期间都有效
- 不会被释放

────────────────────────────────────

【生命周期最佳实践】

1. 优先让编译器推断
   - 大多数情况不需要显式注解
   - 只在编译器要求时添加

2. 函数参数使用 &str 而不是 String
   - 更灵活
   - 避免不必要的所有权转移

3. 结构体包含引用时考虑使用 String
   - 避免生命周期复杂性
   - 除非性能关键

4. 理解编译器错误信息
   - 错误信息会指出问题所在
   - 按照提示修复
*/

// ============================================
// 第六部分：练习题
// ============================================

/*
练习题 ⭐⭐⭐

1. 基础生命周期函数
   要求：
   - 编写函数 first_or_second<'a>(x: &'a str, y: &'a str, first: bool) -> &'a str
   - 如果 first 为 true，返回 x，否则返回 y
   - 测试函数
   提示：参考 longest 函数

2. 结构体生命周期
   要求：
   - 定义结构体 Book<'a>，包含字段 title: &'a str
   - 创建 Book 实例
   - 打印书名
   提示：结构体名后添加 <'a>

3. 理解生命周期约束
   要求：
   - 创建两个 String，一个在外部作用域，一个在内部作用域
   - 调用 longest 函数
   - 观察哪些情况编译成功，哪些失败
   提示：引用的数据必须比引用本身活得更久

4. 修复生命周期错误
   要求：
   - 尝试编写返回局部变量引用的函数
   - 观察编译错误
   - 修复错误（返回 String 而不是 &str）
   提示：不要返回局部变量的引用

5. 多个生命周期参数
   要求：
   - 编写函数 announce<'a, 'b>(x: &'a str, y: &'b str) -> &'a str
   - 函数打印 y，返回 x
   - 测试函数
   提示：返回值只与 x 相关，所以只需要 'a

6. 挑战：实现带生命周期的方法
   要求：
   - 为 ImportantExcerpt 实现方法 level(&self) -> i32
   - 为 ImportantExcerpt 实现方法 announce_and_return_part(&self, announcement: &str) -> &str
   - 测试方法
   提示：方法的生命周期省略规则
*/

// ============================================
// 主函数 - 运行所有示例
// ============================================

// 取消下面的注释来运行各部分示例
// fn main() {
//     part2_basic_operations();
//     part4_common_mistakes();
// }

