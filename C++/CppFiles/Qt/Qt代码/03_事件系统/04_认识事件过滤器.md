## myfilter.h
```C++
#ifndef MYFILTER_H
#define MYFILTER_H

#include <QObject>

class MyFilter : public QObject
{
    Q_OBJECT
public:
    explicit MyFilter(QObject *parent = nullptr);
protected:
    bool eventFilter(QObject *watched, QEvent *event) override;
signals:

};

#endif // MYFILTER_H
```

## myfilter.cpp
```C++
#include "myfilter.h"
#include <QDebug>
#include <QEvent>
MyFilter::MyFilter(QObject *parent) : QObject(parent)
{

}
bool MyFilter::eventFilter(QObject *watched, QEvent *ev){
    if(ev->type() == QEvent::MouseButtonPress){
        qDebug() << "watched object is " << watched->objectName() << " filter mouse press";
    }
    //return true;
    return false;
}
```
## mainwindow.h
```C++
#ifndef MAINWINDOW_H
#define MAINWINDOW_H

#include <QMainWindow>
#include <QDebug>
#include <QEvent>
#include <QMouseEvent>
QT_BEGIN_NAMESPACE
namespace Ui { class MainWindow; }
QT_END_NAMESPACE

class MainWindow : public QMainWindow
{
    Q_OBJECT

public:
    MainWindow(QWidget *parent = nullptr);
    ~MainWindow();
    bool event(QEvent *ev) override{
        if(ev->type() == QEvent::MouseButtonPress){
            qDebug() << "MainWindow event press";
        }
        return QMainWindow::event(ev);
    }
    void mousePressEvent(QMouseEvent *ev) override{
        qDebug() << "MainWindow event_handler press";
        ev->ignore();
    }
private:
    Ui::MainWindow *ui;
};
#endif // MAINWINDOW_H

```
## mainwindow.cpp
```C++
#include "mainwindow.h"
#include "ui_mainwindow.h"

MainWindow::MainWindow(QWidget *parent)
    : QMainWindow(parent)
    , ui(new Ui::MainWindow)
{
    ui->setupUi(this);
}

MainWindow::~MainWindow()
{
    delete ui;
}
```

## mian.cpp
```C++
#include "mainwindow.h"
#include "myfilter.h"
#include <QApplication>
#include <QLabel>
int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    MainWindow w;
    w.resize(800,600);
    QLabel *pframe = new QLabel(&w);
    pframe->setObjectName("Out");
    pframe->resize(700,500);
    pframe->move(50,50);
    pframe->setFrameStyle(QFrame::Panel | QFrame::Plain);
    pframe->setLineWidth(2);

    MyFilter filter;
    pframe->installEventFilter(&filter);
    w.installEventFilter(&filter);
    w.show();
    return a.exec();
}

```
## 演示结果
![[Pasted image 20240719220238.png]]
