// ============================================
// 协程超时控制 - 超详细注释版
// 日期：2026-06-17（第84天）
// 难度：⭐⭐⭐⭐ 高级
// ============================================

/*
【核心概念】简要说明

是什么？
- 超时控制：限制任务的最大执行时间
- 超时检测：检查任务是否超时
- 超时处理：超时后的清理和通知

为什么需要？
- 防止任务无限期等待
- 及时释放资源
- 提高系统响应性

核心特点？
- 时间限制：每个任务有最大执行时间
- 自动检测：系统自动检测超时
- 优雅处理：超时后正确清理资源
*/

#include <iostream>
#include <coroutine>
#include <chrono>
#include <string>

using namespace std;
using namespace chrono;

// ============================================
// 第一部分：超时控制核心概念
// ============================================

void part1_timeout_concepts() {
    cout << string(60, '=') << endl;
    cout << "第一部分：超时控制核心概念" << endl;
    cout << string(60, '=') << endl;

    cout << R"(
【什么是超时控制？】

定义：
- 为任务设置最大执行时间
- 超过时间后自动取消任务
- 防止资源被长时间占用

超时场景：
1. 网络请求超时（如3秒）
2. 数据库查询超时（如10秒）
3. 文件操作超时（如5秒）
4. 用户操作超时（如30秒）

【为什么需要超时控制？】

问题1：资源泄漏
- 任务一直等待，占用资源
- 协程无法复用
- 内存持续增长

问题2：响应缓慢
- 用户长时间等待
- 系统响应变慢
- 用户体验差

问题3：雪崩效应
- 一个慢任务导致队列堆积
- 后续任务都被阻塞
- 整个系统崩溃

超时控制的价值：
✅ 及时释放资源
✅ 保证响应时间
✅ 防止雪崩效应
✅ 提高系统稳定性

【超时检测方法】

方法1：轮询检测
- 定期检查任务执行时间
- 简单但开销大
- 精度取决于检查间隔

方法2：定时器触发
- 为每个任务设置定时器
- 超时时自动触发
- 精确但复杂

方法3：时间轮（Time Wheel）
- 使用环形结构
- O(1)时间复杂度
- 适合大量定时任务

【超时后的处理策略】

策略1：立即取消
- 设置取消标志
- 协程检查标志后退出
- 清理已分配的资源

策略2：优雅降级
- 返回部分结果
- 或返回默认值
- 记录超时日志

策略3：重试机制
- 自动重试任务
- 指数退避策略
- 最大重试次数限制

【超时时间的设置】

考虑因素：
1. 任务类型
   - 快速任务：100ms-1s
   - 中等任务：1s-10s
   - 慢任务：10s-60s

2. 网络环境
   - 内网：短超时
   - 外网：长超时
   - 不稳定网络：更长超时

3. 用户体验
   - 交互式：短超时（用户不愿等）
   - 后台任务：长超时（可以等）

4. 业务需求
   - 实时性要求高：短超时
   - 准确性要求高：长超时

推荐值：
- HTTP请求：3-10秒
- 数据库查询：5-30秒
- 文件上传：60-300秒
- 批处理任务：5-60分钟

【超时控制的实现要点】

1. 记录开始时间
   - 任务开始时记录时间戳
   - 使用chrono::steady_clock

2. 定期检查
   - 在任务执行过程中检查
   - 计算已用时间

3. 超时标志
   - 设置超时标志
   - 协程检查标志

4. 资源清理
   - 关闭文件句柄
   - 释放内存
   - 取消网络请求
)" << endl;
}

// ============================================
// 第二部分：超时控制实现
// ============================================

void part2_timeout_implementation() {
    cout << "\n" << string(60, '=') << endl;
    cout << "第二部分：超时控制实现" << endl;
    cout << string(60, '=') << endl;

    cout << R"(
【简单超时检测示例】

核心思想：
1. 记录任务开始时间
2. 定期检查是否超时
3. 超时后取消任务

实现步骤：
1. 使用 chrono 记录时间
2. 计算已用时间
3. 与超时阈值比较
4. 超时则设置标志

代码示例：

auto start = steady_clock::now();
const int timeout_ms = 1000;  // 1秒超时

while (!task.done()) {
    auto now = steady_clock::now();
    auto elapsed = duration_cast<milliseconds>(now - start).count();

    if (elapsed > timeout_ms) {
        cout << "任务超时！" << endl;
        break;
    }

    // 执行任务
    task.resume();
}

【带超时的协程池】

增强功能：
1. 为每个任务设置超时时间
2. 执行过程中检查超时
3. 超时后取消并通知

关键数据结构：
struct TimedTask {
    function<void()> task;
    int timeout_ms;
    steady_clock::time_point start_time;
    bool is_timeout;
};

【超时控制的最佳实践】

1. 合理设置超时时间
   - 不要太短（频繁超时）
   - 不要太长（失去意义）
   - 根据实际测试调整

2. 提供超时配置
   - 不同任务不同超时
   - 允许用户配置
   - 提供默认值

3. 记录超时日志
   - 记录超时任务
   - 统计超时率
   - 用于性能分析

4. 优雅处理超时
   - 清理资源
   - 通知调用者
   - 记录状态

5. 考虑重试
   - 某些任务可以重试
   - 指数退避策略
   - 最大重试次数
)" << endl;
}

// ============================================
// 主函数
// ============================================

int main() {
    cout << "============================================" << endl;
    cout << "协程超时控制" << endl;
    cout << "============================================\n" << endl;

    part1_timeout_concepts();
    part2_timeout_implementation();

    cout << "\n" << string(60, '=') << endl;
    cout << "学习完成！" << endl;
    cout << string(60, '=') << endl;

    cout << "\n【温馨提示】" << endl;
    cout << "现在是凌晨时间，学习材料已创建完成。" << endl;
    cout << "请休息好，明天再继续学习！💤" << endl;

    return 0;
}
