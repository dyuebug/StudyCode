// ============================================
// 协程调度策略：负载均衡与高级策略 - 超详细注释版
// 日期：2026-06-16（第83天）
// 难度：⭐⭐⭐⭐ 高级
// ============================================

#include <iostream>
#include <string>

using namespace std;

void part1_concepts() {
    cout << string(60, '=') << endl;
    cout << "负载均衡与高级调度策略" << endl;
    cout << string(60, '=') << endl;

    cout << R"(
【工作窃取（Work Stealing）】

是什么？
- 每个线程维护自己的任务队列
- 线程优先执行自己队列的任务
- 队列为空时，从其他线程"偷"任务

优点：
✅ 减少锁竞争（大部分时间无锁）
✅ 自动负载均衡
✅ 缓存友好

工业界应用：
- Go的goroutine调度器
- Java的ForkJoinPool
- Tokio异步运行时

【多队列调度】

是什么？
- 每个线程一个队列
- 新任务按策略分配到队列
- 线程只处理自己队列的任务

分配策略：
1. 轮询分配（Round-Robin）
2. 最少任务分配（Least Load）
3. 哈希分配（Hash）
4. 随机分配（Random）

【亲和性调度】

是什么？
- 任务绑定到特定线程/CPU
- 相关任务在同一线程执行
- 利用CPU缓存提高性能

类型：
1. 线程亲和性
2. CPU亲和性
3. NUMA亲和性

【调度策略选择指南】

┌──────────┬────────┬────────┬────────┬──────────┐
│  策略    │ 公平性 │ 效率   │ 复杂度 │ 适用场景 │
├──────────┼────────┼────────┼────────┼──────────┤
│ FIFO     │ 好     │ 中等   │ 简单   │ 简单任务 │
│ 优先级   │ 差     │ 好     │ 中等   │ 有优先级 │
│ 轮询     │ 最好   │ 中等   │ 简单   │ 多用户   │
│ 时间片   │ 好     │ 好     │ 中等   │ 交互式   │
│ 工作窃取 │ 好     │ 最好   │ 复杂   │ 多线程   │
│ 亲和性   │ 中等   │ 最好   │ 复杂   │ 缓存敏感 │
└──────────┴────────┴────────┴────────┴──────────┘

决策树：
1. 多线程？NO → 单线程调度，YES → 继续
2. 有优先级？YES → 多队列+优先级，NO → 继续
3. 需负载均衡？YES → 工作窃取，NO → 多队列
4. 缓存敏感？YES → 工作窃取+亲和性，NO → 工作窃取

实际场景：
- Web服务器：工作窃取 + 亲和性
- 游戏引擎：优先级 + 时间片
- 数据处理：工作窃取
- 实时系统：优先级 + 亲和性
- 批处理：FIFO 或 工作窃取

【性能对比】

吞吐量（相对值）：
- 单队列：100
- 多队列：150
- 工作窃取：180

复杂度（1-5星）：
- FIFO：⭐
- 优先级：⭐⭐
- 轮询：⭐⭐
- 时间片：⭐⭐⭐
- 多队列：⭐⭐⭐
- 工作窃取：⭐⭐⭐⭐
- 亲和性：⭐⭐⭐⭐⭐

【工业界实现】

Go的goroutine调度器：
- 策略：工作窃取 + 抢占式
- M-P-G模型
- 动态负载均衡

Tokio异步运行时：
- 策略：工作窃取 + 多队列
- 本地队列 + 全局队列
- 支持单线程/多线程

Nginx事件循环：
- 策略：事件驱动 + 亲和性
- 多进程模型
- CPU亲和性绑定

【六大调度策略总结】

1. FIFO - 简单公平
2. 优先级 - 重要优先
3. 轮询 - 完全公平
4. 时间片 - 平衡响应
5. 工作窃取 - 负载均衡
6. 亲和性 - 缓存优化

选择原则：
- 根据业务需求
- 可以组合使用
- 没有最好，只有最合适

实现建议：
1. 先实现FIFO（验证功能）
2. 添加优先级（支持重要任务）
3. 添加时间片（改善响应）
4. 多线程化（提高吞吐）
5. 添加工作窃取（负载均衡）
6. 优化亲和性（缓存优化）

不要过早优化！
)" << endl;
}

int main() {
    cout << "============================================" << endl;
    cout << "协程调度策略：负载均衡与高级策略" << endl;
    cout << "============================================\n" << endl;

    part1_concepts();

    cout << "\n" << string(60, '=') << endl;
    cout << "学习完成！" << endl;
    cout << string(60, '=') << endl;

    cout << "\n【第83天学习总结】" << endl;
    cout << R"(
✅ 今天完成了协程调度策略的全部学习！

任务1：FIFO vs 优先级调度
- 掌握了 priority_queue
- 实现了优先级调度器
- 理解了5个常见错误

任务2：公平调度策略
- 实现了轮询调度器
- 实现了时间片调度器
- 掌握了公平性指标

任务3：负载均衡与高级策略
- 理解了工作窃取原理
- 掌握了多队列调度
- 学习了亲和性调度
- 了解了工业界实现

【核心收获】

六大调度策略全部掌握：
1. FIFO - 简单公平
2. 优先级 - 重要优先  
3. 轮询 - 完全公平
4. 时间片 - 平衡响应
5. 工作窃取 - 负载均衡
6. 亲和性 - 缓存优化

选择原则：
✅ 根据业务需求选择
✅ 可以组合使用
✅ 没有最好，只有最合适

【明天预告】

第84天：协程池与超时控制
- 协程池设计
- 超时机制
- 取消机制
- 资源管理

继续加油！🚀
)" << endl;

    return 0;
}
