// ============================================
// 06_switch_case_exercise.cpp - switch-case练习题
// 日期：2026-02-18（第2天）
// 难度：⭐⭐ 中级
// ============================================

/*
【练习说明】

本文件包含8道switch-case练习题，涵盖：
- 基本switch-case使用
- case穿透技巧
- 字符类型判断
- 菜单系统设计

学习目标：
- 熟练使用switch-case结构
- 掌握case穿透技巧
- 理解switch与if-else的选择
- 培养代码组织能力

⚠️ 重要提示：
- 每道题都有详细的注释和提示
- 先自己思考，再看提示
- 完成后编译运行，验证结果
- 不提供参考答案，鼓励独立思考
*/

#include <iostream>
#include <string>
#include <cstdlib>
using namespace std;

int main() {
    system("chcp 65001");  // 设置UTF-8编码，支持中文

    cout << string(60, '=') << endl;
    cout << "        switch-case 练习题" << endl;
    cout << string(60, '=') << endl;
    cout << endl;

    // ============================================
    // 练习1：星期查询
    // ============================================

    cout << "练习1：星期查询" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【题目】
输入数字1-7，输出对应的星期名称。

【要求】
- 使用 cin 输入数字
- 使用 switch-case 判断
- 输出格式："星期X（English Name）"
- 处理无效输入（不是1-7）

【提示】
- case 1: 星期一（Monday）
- case 2: 星期二（Tuesday）
- ...
- default: 无效输入

【思考】
- 如果用if-else实现，代码会怎样？
- switch和if-else哪个更清晰？
)" << endl;

    // 在这里编写代码
    int day;
    cout << "请输入数字（1-7）：";
    cin >> day;

    // TODO: 使用switch-case输出星期名称


    cout << endl;

    // ============================================
    // 练习2：月份天数查询
    // ============================================

    cout << "练习2：月份天数查询" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【题目】
输入月份（1-12），输出该月的天数。

【天数规则】
- 31天：1、3、5、7、8、10、12月
- 30天：4、6、9、11月
- 28天：2月（暂不考虑闰年）

【要求】
- 使用 switch-case 判断
- 使用 case穿透技巧（多个case共享代码）
- 输出格式："X月有X天"
- 处理无效输入

【提示】
switch (month) {
    case 1:
    case 3:
    case 5:
    case 7:
    case 8:
    case 10:
    case 12:
        cout << "31天" << endl;
        break;
    case 4:
    case 6:
    ...
}

【思考】
- case穿透有什么好处？
- 如何扩展支持闰年判断？
)" << endl;

    // 在这里编写代码
    int month;
    cout << "请输入月份（1-12）：";
    cin >> month;

    // TODO: 使用switch-case输出天数


    cout << endl;

    // ============================================
    // 练习3：简易计算器（完整版）
    // ============================================

    cout << "练习3：简易计算器（完整版）" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【题目】
实现一个简易计算器，支持加减乘除和取模。

【要求】
- 输入两个数字和一个运算符（+、-、*、/、%）
- 使用 switch-case 判断运算符
- 输出计算结果
- 处理除零错误（除法和取模）
- 处理无效运算符

【提示】
- 除法前检查：if (num2 == 0) { ... }
- 取模只能用于整数，需要类型转换
- 输出格式："num1 op num2 = result"

【思考】
- 如何支持更多运算（如幂运算、开方）？
- 如何实现连续计算？
)" << endl;

    // 在这里编写代码
    double num1, num2;
    char op;
    cout << "请输入第一个数字：";
    cin >> num1;
    cout << "请输入运算符（+、-、*、/、%）：";
    cin >> op;
    cout << "请输入第二个数字：";
    cin >> num2;

    // TODO: 使用switch-case计算结果


    cout << endl;

    // ============================================
    // 练习4：成绩等级转换
    // ============================================

    cout << "练习4：成绩等级转换" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【题目】
输入等级字母（A/B/C/D/F），输出对应的分数范围和评语。

【等级对应】
- A: 90-100分（优秀）
- B: 80-89分（良好）
- C: 70-79分（中等）
- D: 60-69分（及格）
- F: 0-59分（不及格）

【要求】
- 使用 switch-case 判断
- 支持大小写（A和a都可以）
- 输出格式："等级X：分数范围，评语"
- 处理无效输入

【提示】
- 使用case穿透支持大小写：
  case 'A':
  case 'a':
      // 处理A等级
      break;

【思考】
- 如何实现反向转换（分数→等级）？
- 为什么这里适合用switch而不是if-else？
)" << endl;

    // 在这里编写代码
    char grade;
    cout << "请输入成绩等级（A/B/C/D/F）：";
    cin >> grade;

    // TODO: 使用switch-case输出分数范围和评语


    cout << endl;

    // ============================================
    // 练习5：季节查询
    // ============================================

    cout << "练习5：季节查询" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【题目】
输入月份（1-12），输出对应的季节。

【季节划分】
- 春季（Spring）：3、4、5月
- 夏季（Summer）：6、7、8月
- 秋季（Autumn）：9、10、11月
- 冬季（Winter）：12、1、2月

【要求】
- 使用 switch-case 判断
- 使用 case穿透技巧
- 输出格式："X月属于X季"
- 处理无效输入

【提示】
switch (month) {
    case 3:
    case 4:
    case 5:
        cout << "春季" << endl;
        break;
    ...
}

【思考】
- 不同地区的季节划分可能不同，如何设计？
- 如何添加节气信息？
)" << endl;

    // 在这里编写代码
    int seasonMonth;
    cout << "请输入月份（1-12）：";
    cin >> seasonMonth;

    // TODO: 使用switch-case输出季节


    cout << endl;

    // ============================================
    // 练习6：菜单系统（完整版）
    // ============================================

    cout << "练习6：菜单系统（完整版）" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【题目】
实现一个简单的文件管理菜单系统。

【菜单选项】
1. 新建文件
2. 打开文件
3. 保存文件
4. 另存为
5. 关闭文件
6. 退出程序

【要求】
- 显示菜单
- 输入选择（1-6）
- 使用 switch-case 执行对应操作
- 每个操作输出提示信息
- 处理无效输入

【提示】
- 先用cout显示菜单
- 再用cin输入选择
- switch判断并执行
- 可以添加更多细节（如文件名输入）

【思考】
- 如何实现循环菜单（不退出一直显示）？
- 如何添加子菜单？
)" << endl;

    // 在这里编写代码
    cout << "========== 文件管理系统 ==========" << endl;
    cout << "1. 新建文件" << endl;
    cout << "2. 打开文件" << endl;
    cout << "3. 保存文件" << endl;
    cout << "4. 另存为" << endl;
    cout << "5. 关闭文件" << endl;
    cout << "6. 退出程序" << endl;
    cout << "===================================" << endl;

    int choice;
    cout << "请输入选择（1-6）：";
    cin >> choice;

    // TODO: 使用switch-case执行对应操作


    cout << endl;

    // ============================================
    // 练习7：字符类型判断
    // ============================================

    cout << "练习7：字符类型判断" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【题目】
输入一个字符，判断是数字、大写字母、小写字母还是其他。

【字符范围】
- 数字：'0'-'9'（ASCII 48-57）
- 大写字母：'A'-'Z'（ASCII 65-90）
- 小写字母：'a'-'z'（ASCII 97-122）
- 其他：default

【要求】
- 使用 switch-case 判断
- 使用 case穿透技巧（多个字符共享代码）
- 输出格式："'X'是X类型"

【提示】
switch (ch) {
    case '0':
    case '1':
    case '2':
    ...
    case '9':
        cout << "数字" << endl;
        break;
    case 'A':
    case 'B':
    ...
}

⚠️ 注意：
- 这种方法需要写很多case，不推荐
- 实际应该用if-else判断范围：
  if (ch >= '0' && ch <= '9') { ... }
- 但为了练习switch，这里用case穿透

【思考】
- 为什么这里用if-else更好？
- switch适合什么场景？
)" << endl;

    // 在这里编写代码
    char ch;
    cout << "请输入一个字符：";
    cin >> ch;

    // TODO: 使用switch-case判断字符类型
    // 提示：可以只判断部分字符，其他用default


    cout << endl;

    // ============================================
    // 练习8：运算符优先级查询
    // ============================================

    cout << "练习8：运算符优先级查询" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【题目】
输入运算符（+、-、*、/、%），输出其优先级。

【优先级规则】
- 优先级1（最高）：*、/、%
- 优先级2：+、-

【要求】
- 使用 switch-case 判断
- 使用 case穿透技巧
- 输出格式："运算符'X'的优先级是X"
- 处理无效运算符

【提示】
switch (op) {
    case '*':
    case '/':
    case '%':
        cout << "优先级1（最高）" << endl;
        break;
    case '+':
    case '-':
        cout << "优先级2" << endl;
        break;
    default:
        cout << "无效运算符" << endl;
        break;
}

【思考】
- 如何扩展支持更多运算符（如括号、幂运算）？
- 如何实现表达式求值（考虑优先级）？
)" << endl;

    // 在这里编写代码
    char operator_char;
    cout << "请输入运算符（+、-、*、/、%）：";
    cin >> operator_char;

    // TODO: 使用switch-case输出优先级


    cout << endl;

    // ============================================
    // 练习完成
    // ============================================

    cout << string(60, '=') << endl;
    cout << "所有练习题已完成！" << endl;
    cout << "编译命令：g++ -o 06_switch_case_exercise 06_switch_case_exercise.cpp" << endl;
    cout << "运行命令：./06_switch_case_exercise 或 06_switch_case_exercise.exe" << endl;
    cout << string(60, '=') << endl;

    return 0;
}
