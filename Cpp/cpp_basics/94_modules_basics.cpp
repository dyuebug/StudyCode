// ============================================
// 94_modules_basics.cpp - C++20 Modules 基础概念
// 功能：理解模块化编程的核心概念和工作原理
// 注意：本文件以概念讲解为主，包含伪代码示例
// 编译：需要 C++20 支持，但本文件主要用于学习概念
// ============================================

#include <iostream>
#include <string>
using namespace std;

// ============================================
// 第一部分：核心概念详解 ⭐⭐⭐⭐⭐
// ============================================

/*
【C++20 Modules 核心概念】

是什么？
- Modules（模块）是 C++20 引入的新特性，用于替代传统的头文件系统
- 模块是一种更现代、更高效的代码组织方式
- 模块提供了更好的封装性和编译性能

为什么需要？
- 传统头文件存在诸多问题：重复包含、编译慢、符号污染、宏污染
- 模块解决了这些问题，提供了更好的代码组织方式
- 模块是 C++ 现代化的重要一步

核心特点：
1. 编译一次：模块只编译一次，不像头文件每次都编译
2. 默认私有：模块内容默认私有，只有 export 的内容才对外可见
3. 无宏污染：模块不会导出宏定义
4. 清晰依赖：模块的依赖关系清晰，避免循环依赖

模块 vs 头文件：
┌─────────────────┬──────────────────┬──────────────────┐
│     特性        │      头文件      │      模块        │
├─────────────────┼──────────────────┼──────────────────┤
│ 编译次数        │ 每次包含都编译   │ 只编译一次       │
│ 默认可见性      │ 公开             │ 私有             │
│ 宏定义          │ 会污染           │ 不会污染         │
│ 编译速度        │ 慢               │ 快               │
│ 依赖管理        │ 复杂             │ 清晰             │
│ 符号冲突        │ 容易             │ 不容易           │
└─────────────────┴──────────────────┴──────────────────┘
*/

int main() {
    cout << string(60, '=') << endl;
    cout << "第一部分：核心概念详解" << endl;
    cout << string(60, '=') << endl;

    // --------------------------------------------
    // 1.1 概念1：什么是模块？
    // --------------------------------------------

    cout << "\n1.1 什么是模块？" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【模块的定义】

模块是一种代码组织单元，具有以下特点：
1. 独立编译：模块只编译一次，生成二进制接口文件（BMI）
2. 明确接口：使用 export 明确导出接口
3. 封装性强：默认私有，只有 export 的内容才对外可见
4. 无宏污染：模块不会导出宏定义

【形象比喻】

传统头文件就像复印文档：
- 每次包含都要复印一份（重复编译）
- 所有内容都公开（没有封装）
- 容易弄乱（符号冲突、宏污染）

模块就像图书馆的书：
- 书只印刷一次（只编译一次）
- 只有目录对外公开（export 接口）
- 内容有序管理（封装性强）

【实际例子】

想象你在开发一个大型项目：
- 传统头文件：每个 .cpp 文件都要重新编译所有包含的头文件
- 模块：模块只编译一次，其他文件直接使用编译结果

【代码对比】

传统头文件方式：
------------------------------------------------------------
// math.h
#ifndef MATH_H
#define MATH_H

int add(int a, int b);
int multiply(int a, int b);

#endif

// math.cpp
#include "math.h"

int add(int a, int b) {
    return a + b;
}

int multiply(int a, int b) {
    return a * b;
}

// main.cpp
#include "math.h"  // 每次包含都要重新编译 math.h

int main() {
    int result = add(1, 2);
    return 0;
}

问题：
- math.h 在每个包含它的 .cpp 文件中都要重新编译
- 如果 math.h 很大，编译时间会很长
- 容易出现重复定义、符号冲突

模块方式（伪代码）：
------------------------------------------------------------
// math.cppm（模块接口单元）
export module math;  // 声明模块

export int add(int a, int b) {
    return a + b;
}

export int multiply(int a, int b) {
    return a * b;
}

// main.cpp
import math;  // 导入模块

int main() {
    int result = add(1, 2);
    return 0;
}

优势：
- math 模块只编译一次，生成 BMI（二进制接口文件）
- main.cpp 直接使用 BMI，不需要重新编译模块
- 编译速度快，依赖关系清晰
)" << endl;

    // --------------------------------------------
    // 1.2 概念2：三个关键字 ⭐⭐⭐⭐⭐
    // --------------------------------------------

    cout << "\n1.2 三个关键字" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【module】- 声明模块

作用：
- 声明一个模块
- 定义模块的名称
- 标记模块接口单元或实现单元

使用场景：
- 模块接口单元（.cppm 或 .ixx）
- 模块实现单元（.cpp）

语法：
------------------------------------------------------------
// 模块接口单元
export module 模块名;

// 模块实现单元
module 模块名;

示例：
------------------------------------------------------------
// math.cppm（模块接口单元）
export module math;  // 声明并导出模块 math

export int add(int a, int b) {
    return a + b;
}

// math_impl.cpp（模块实现单元）
module math;  // 声明这是 math 模块的实现

// 实现细节（不导出）
int helper_function() {
    return 42;
}

────────────────────────────────────

【export】- 导出接口

作用：
- 导出模块的接口（函数、类、变量、常量）
- 只有 export 的内容才对外可见
- 未 export 的内容是模块私有的

使用场景：
- 导出函数
- 导出类
- 导出常量
- 导出类型别名

语法：
------------------------------------------------------------
export 声明;

示例：
------------------------------------------------------------
export module math;

// 导出函数
export int add(int a, int b) {
    return a + b;
}

// 导出类
export class Calculator {
public:
    int add(int a, int b);
    int subtract(int a, int b);
};

// 导出常量
export const double PI = 3.14159;

// 导出类型别名
export using Number = int;

// 私有函数（不导出）
int helper() {  // 没有 export，外部不可见
    return 42;
}

────────────────────────────────────

【import】- 导入模块

作用：
- 导入一个模块
- 使用模块导出的接口
- 建立模块之间的依赖关系

使用场景：
- 在其他模块中使用某个模块
- 在普通 .cpp 文件中使用模块

语法：
------------------------------------------------------------
import 模块名;

示例：
------------------------------------------------------------
// main.cpp
import math;  // 导入 math 模块

int main() {
    int result = add(1, 2);  // 使用 math 模块导出的 add 函数
    return 0;
}

// 导入多个模块
import math;
import string_utils;
import file_io;

────────────────────────────────────

【三个关键字对比】

┌─────────────┬──────────────┬──────────────┬──────────────┐
│   关键字    │    作用      │   位置       │   示例       │
├─────────────┼──────────────┼──────────────┼──────────────┤
│ module      │ 声明模块     │ 模块文件开头 │ export module math; │
│ export      │ 导出接口     │ 声明前       │ export int add(...); │
│ import      │ 导入模块     │ 使用模块处   │ import math; │
└─────────────┴──────────────┴──────────────┴──────────────┘

记忆技巧：
- module：模块的"身份证"，声明"我是谁"
- export：模块的"对外窗口"，声明"我提供什么"
- import：模块的"使用许可"，声明"我要用什么"
)" << endl;

    // --------------------------------------------
    // 1.3 概念3：模块的结构
    // --------------------------------------------

    cout << "\n1.3 模块的结构" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【模块的组成部分】

1. **模块接口单元（Module Interface Unit）**
   - 文件扩展名：.cppm（GCC）、.ixx（MSVC）、.mpp（其他）
   - 作用：定义模块的公开接口
   - 包含：export module 声明 + export 的接口

2. **模块实现单元（Module Implementation Unit）**
   - 文件扩展名：.cpp
   - 作用：实现模块的私有细节
   - 包含：module 声明 + 实现代码

3. **模块分区（Module Partition）**
   - 作用：将大模块拆分为多个部分
   - 语法：export module 模块名:分区名;

【模块文件结构】

完整的模块通常包含：
------------------------------------------------------------
math/
├── math.cppm          # 模块接口单元（公开接口）
├── math_impl.cpp      # 模块实现单元（私有实现）
└── math_advanced.cppm # 模块分区（可选）

【示例：完整的模块】

// math.cppm（模块接口单元）
------------------------------------------------------------
export module math;  // 声明并导出模块

// 导出函数声明
export int add(int a, int b);
export int multiply(int a, int b);

// 导出类
export class Calculator {
public:
    int add(int a, int b);
    int subtract(int a, int b);
private:
    int memory;  // 私有成员
};

// 私有辅助函数（不导出）
int validate(int x) {
    return x > 0 ? x : 0;
}

// math_impl.cpp（模块实现单元）
------------------------------------------------------------
module math;  // 声明这是 math 模块的实现

// 实现导出的函数
int add(int a, int b) {
    return a + b;
}

int multiply(int a, int b) {
    return a * b;
}

// 实现导出的类
int Calculator::add(int a, int b) {
    return a + b;
}

int Calculator::subtract(int a, int b) {
    return a - b;
}

// 私有辅助函数的实现
int helper_function() {
    return 42;
}

// main.cpp（使用模块）
------------------------------------------------------------
import math;  // 导入 math 模块

int main() {
    // 使用导出的函数
    int result1 = add(1, 2);
    int result2 = multiply(3, 4);

    // 使用导出的类
    Calculator calc;
    int result3 = calc.add(5, 6);

    // 错误：不能使用私有函数
    // int x = validate(10);  // 编译错误：validate 未导出
    // int y = helper_function();  // 编译错误：helper_function 未导出

    return 0;
}
)" << endl;

    // --------------------------------------------
    // 1.4 概念4：模块的编译流程
    // --------------------------------------------

    cout << "\n1.4 模块的编译流程" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【模块的编译流程】

传统头文件编译流程：
------------------------------------------------------------
1. 预处理：展开 #include，处理宏
2. 编译：每个 .cpp 文件独立编译
3. 链接：链接所有 .o 文件

问题：
- 每个 .cpp 文件都要重新编译包含的头文件
- 如果头文件很大，编译时间很长
- 修改头文件，所有包含它的文件都要重新编译

模块编译流程：
------------------------------------------------------------
1. 编译模块接口单元：生成 BMI（二进制接口文件）
2. 编译使用模块的文件：直接使用 BMI，不需要重新编译模块
3. 链接：链接所有 .o 文件

优势：
- 模块只编译一次，生成 BMI
- 使用模块的文件直接使用 BMI，编译速度快
- 修改模块实现，不影响使用模块的文件（只要接口不变）

【编译命令示例】

GCC 编译模块：
------------------------------------------------------------
# 1. 编译模块接口单元，生成 BMI
g++ -std=c++20 -fmodules-ts -c math.cppm -o math.o

# 2. 编译使用模块的文件
g++ -std=c++20 -fmodules-ts -c main.cpp -o main.o

# 3. 链接
g++ math.o main.o -o program

MSVC 编译模块：
------------------------------------------------------------
# 1. 编译模块接口单元
cl /std:c++20 /c /interface math.ixx

# 2. 编译使用模块的文件
cl /std:c++20 /c main.cpp

# 3. 链接
cl math.obj main.obj /Fe:program.exe

【BMI（二进制接口文件）】

什么是 BMI？
- Binary Module Interface（二进制模块接口）
- 模块编译后生成的中间文件
- 包含模块的接口信息（导出的函数、类、常量等）
- 类似于预编译头文件（PCH），但更高效

BMI 的优势：
- 编译速度快：不需要重新解析源代码
- 依赖清晰：BMI 包含完整的依赖信息
- 跨平台：BMI 格式标准化（理论上）

【编译流程图】

传统头文件：
------------------------------------------------------------
math.h ──┬──> main.cpp ──> 编译 ──> main.o ──┐
         │                                    ├──> 链接 ──> program
         └──> test.cpp ──> 编译 ──> test.o ──┘

每个 .cpp 都要重新编译 math.h

模块：
------------------------------------------------------------
math.cppm ──> 编译 ──> math.o + BMI
                           │
                           ├──> main.cpp ──> 编译 ──> main.o ──┐
                           │                                    ├──> 链接 ──> program
                           └──> test.cpp ──> 编译 ──> test.o ──┘

math 模块只编译一次，生成 BMI
main.cpp 和 test.cpp 直接使用 BMI
)" << endl;

    cout << "\n程序结束" << endl;
    return 0;
}
