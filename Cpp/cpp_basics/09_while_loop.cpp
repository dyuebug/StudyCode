// ============================================
// while 循环 - 超详细注释版
// 日期：2026-02-26（第2天）
// 难度：⭐⭐ 中级
// 编译：g++ -o 09_while_loop 09_while_loop.cpp
// 运行：./09_while_loop 或 09_while_loop.exe
// C++版本：C++11 及以上
// ============================================

/*
【核心概念】while 循环

是什么？
- while 循环是一种"条件型"循环结构
- 只要条件为 true，就一直重复执行

为什么需要？
- 有些场景你不知道要循环多少次
- 比如：让用户输入密码，直到输入正确为止
- 比如：猜数字游戏，直到猜对为止
- 这种"不确定次数"的重复，用 while 最合适

核心特点：
- 先判断条件，再执行循环体
- 如果条件一开始就是 false，循环体一次都不执行
- 适合"不知道循环多少次"的场景
*/

#include <iostream>
using namespace std;

int main() {

    cout << "============================================" << endl;
    cout << "  第一部分：核心概念详解 ⭐⭐⭐⭐⭐" << endl;
    cout << "============================================" << endl;

    // --------------------------------------------
    // 1.1 while 循环是什么？
    // --------------------------------------------

    cout << endl;
    cout << "1.1 while 循环是什么？" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【while 循环】

想象一个场景：你在排队买奶茶

你的行为模式：
  1. 看一眼：到我了吗？（条件判断）
  2. 没到 → 继续等（执行循环体）
  3. 回到第 1 步
  4. 到了 → 离开队伍（循环结束）

关键点：你不知道要等多久（循环次数未知）
        但你知道什么时候该停（条件明确）

这就是 while 循环的思维模型！

)" << endl;

    // --------------------------------------------
    // 1.2 while 循环语法详解 ⭐⭐⭐⭐⭐
    // --------------------------------------------

    cout << "1.2 while 循环语法详解" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【语法卡片】while 循环

完整语法：
┌─────────────────────────────────────────────┐
│  while (条件) {                              │
│      // 循环体：要重复执行的代码             │
│      // 必须包含改变条件的语句！             │
│  }                                           │
└─────────────────────────────────────────────┘

执行流程：
  1. 检查条件 → true？继续  false？结束
  2. 执行循环体
  3. 回到第 1 步

与 for 循环的对比：

  for 循环：三要素写在一行
  ┌──────────────────────────────────┐
  │ for (int i = 0; i < 5; i++) {   │
  │     cout << i;                   │
  │ }                                │
  └──────────────────────────────────┘

  while 循环：三要素分散在三处
  ┌──────────────────────────────────┐
  │ int i = 0;        // 初始化     │
  │ while (i < 5) {   // 条件       │
  │     cout << i;                   │
  │     i++;           // 更新       │
  │ }                                │
  └──────────────────────────────────┘

  两者完全等价！只是写法不同。

)" << endl;

    // --------------------------------------------
    // 1.3 while vs for：如何选择？ ⭐⭐⭐⭐
    // --------------------------------------------

    cout << "1.3 while vs for：如何选择？" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【选择指南】

用 for 循环的场景：
  ✅ 知道循环次数：打印 1 到 100
  ✅ 遍历数组：逐个处理每个元素
  ✅ 固定步长：每次加 1、加 2

用 while 循环的场景：
  ✅ 不知道循环次数：等待用户输入正确密码
  ✅ 条件驱动：读取文件直到末尾
  ✅ 事件驱动：游戏循环直到玩家退出

简单记忆：
  "数得清" → 用 for
  "数不清" → 用 while

)" << endl;

    cout << endl;
    cout << "============================================" << endl;
    cout << "  第二部分：基础操作实践" << endl;
    cout << "============================================" << endl;

    // --------------------------------------------
    // 2.1 最简单的 while 循环
    // --------------------------------------------

    cout << endl;
    cout << "2.1 最简单的 while 循环" << endl;
    cout << string(60, '-') << endl;

    // 初始化：在 while 之前
    int i = 1;

    // 条件：i <= 5 时继续循环
    while (i <= 5) {
        cout << "  第 " << i << " 次循环" << endl;
        // 更新：在循环体内部，必须有！
        // 如果忘记 i++，i 永远是 1，条件永远为 true → 死循环
        i++;
    }
    // 循环结束后，i = 6（因为 i = 6 时条件 6 <= 5 为 false）

    // --------------------------------------------
    // 2.2 用 while 实现累加
    // --------------------------------------------

    cout << endl;
    cout << "2.2 用 while 实现累加（1+2+...+100）" << endl;
    cout << string(60, '-') << endl;

    int sum = 0;     // 存储累加结果
    int num = 1;     // 从 1 开始

    while (num <= 100) {
        sum += num;  // 把当前数加到 sum 上
        num++;       // 数字加 1
    }

    cout << "  1 + 2 + ... + 100 = " << sum << endl;

    // --------------------------------------------
    // 2.3 数字拆分（while 的经典应用）
    // --------------------------------------------

    cout << endl;
    cout << "2.3 数字拆分（提取每一位数字）" << endl;
    cout << string(60, '-') << endl;

    // 这是 while 循环的经典应用场景
    // 你不知道数字有几位，所以不知道循环几次 → 用 while
    int number = 12345;
    int original = number;  // 保存原始值，因为 number 会被修改

    cout << "  数字 " << original << " 的每一位：";

    while (number > 0) {
        // number % 10 取出最后一位
        // 12345 % 10 = 5
        // 1234 % 10 = 4
        // 123 % 10 = 3
        // ...
        int digit = number % 10;
        cout << digit << " ";

        // number / 10 去掉最后一位
        // 12345 / 10 = 1234
        // 1234 / 10 = 123
        // ...
        number /= 10;
    }
    cout << "(从低位到高位)" << endl;

    // --------------------------------------------
    // 2.4 猜数字游戏（模拟版）
    // --------------------------------------------

    cout << endl;
    cout << "2.4 猜数字游戏（模拟版）" << endl;
    cout << string(60, '-') << endl;

    // 模拟猜数字的过程（不需要真正输入）
    int secret = 42;       // 秘密数字
    int guess = 10;        // 初始猜测
    int attempts = 0;      // 猜测次数

    cout << "  秘密数字是 " << secret << "，开始猜测..." << endl;

    // 条件：猜测值不等于秘密数字时继续
    while (guess != secret) {
        attempts++;

        if (guess < secret) {
            cout << "  第 " << attempts << " 次猜 " << guess << " → 太小了！" << endl;
            guess += 10;  // 模拟：每次加 10
        } else {
            cout << "  第 " << attempts << " 次猜 " << guess << " → 太大了！" << endl;
            guess -= 3;   // 模拟：每次减 3
        }
    }

    attempts++;
    cout << "  第 " << attempts << " 次猜 " << guess << " → 猜对了！🎉" << endl;
    cout << "  共猜了 " << attempts << " 次" << endl;

    // --------------------------------------------
    // 2.5 求一个数的位数
    // --------------------------------------------

    cout << endl;
    cout << "2.5 求一个数有几位" << endl;
    cout << string(60, '-') << endl;

    int value = 987654;
    int temp = value;   // 用临时变量，不破坏原始值
    int digits = 0;     // 位数计数器

    // 特殊情况：0 也算 1 位
    if (temp == 0) {
        digits = 1;
    } else {
        while (temp > 0) {
            digits++;       // 每去掉一位，计数器加 1
            temp /= 10;     // 去掉最后一位
        }
    }

    cout << "  " << value << " 有 " << digits << " 位" << endl;

    cout << endl;
    cout << "============================================" << endl;
    cout << "  第三部分：深入理解（选学）" << endl;
    cout << "============================================" << endl;

    cout << R"(
🔍 深入理解：while(true) 无限循环

有时候我们会故意写一个"永远为 true"的循环：

while (true) {
    // 做一些事情
    if (某个条件) {
        break;  // 用 break 跳出循环
    }
}

这种写法在以下场景很常见：
- 游戏主循环（直到玩家退出）
- 服务器监听（一直等待请求）
- 菜单系统（直到用户选择退出）

它和 do-while 有点像，都是"先做再判断"。
具体区别我们在下一节 do-while 中会讲。

如果现在不理解，可以跳过，不影响后续学习。
)" << endl;

    cout << endl;
    cout << "============================================" << endl;
    cout << "  第四部分：常见错误和陷阱 ⭐⭐⭐⭐⭐" << endl;
    cout << "============================================" << endl;

    // --------------------------------------------
    // 4.1 错误1：忘记更新条件变量
    // --------------------------------------------

    cout << endl;
    cout << "4.1 错误1：忘记更新条件变量（最常见！）" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
❌ 错误代码：
int i = 0;
while (i < 5) {
    cout << i << " ";
    // 忘记写 i++ ！
}
// 结果：无限打印 0，程序卡死

原因：i 永远是 0，0 < 5 永远为 true

✅ 正确代码：
int i = 0;
while (i < 5) {
    cout << i << " ";
    i++;  // 别忘了更新！
}

预防措施：
- 写 while 循环时，先写更新语句，再写其他逻辑
- 养成习惯：写完 while 后立刻检查"条件会变吗？"
)" << endl;

    // --------------------------------------------
    // 4.2 错误2：条件永远为 true
    // --------------------------------------------

    cout << "4.2 错误2：条件写反导致死循环" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
❌ 错误代码：
int count = 10;
while (count > 0) {
    cout << count << " ";
    count++;  // 应该是 count--，写成了 count++
}
// 结果：count 越来越大（10, 11, 12, ...），永远 > 0

✅ 正确代码：
int count = 10;
while (count > 0) {
    cout << count << " ";
    count--;  // 递减，让 count 趋向 0
}

预防措施：
- 更新方向必须让条件趋向 false
- 条件是 > 0 → 更新用 --（让值变小）
- 条件是 < 10 → 更新用 ++（让值变大）
)" << endl;

    // --------------------------------------------
    // 4.3 错误3：while 后面加分号
    // --------------------------------------------

    cout << "4.3 错误3：while 后面加分号" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
❌ 错误代码：
int i = 0;
while (i < 5);  // 多了一个分号！
{
    cout << i << " ";
    i++;
}
// 结果：死循环（分号是空循环体，i 永远不变）

原因：分号被当作空的循环体
      while 一直检查 i < 5（永远 true），什么都不做

✅ 正确代码：
int i = 0;
while (i < 5) {  // 没有分号
    cout << i << " ";
    i++;
}

记忆技巧：
- while (...) 后面直接跟 {
- 和 for 循环一样，不要在 () 后加分号
)" << endl;

    // --------------------------------------------
    // 4.4 错误4：初始条件就是 false
    // --------------------------------------------

    cout << "4.4 错误4：初始条件就是 false（不是错误，但要理解）" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
代码：
int i = 10;
while (i < 5) {  // 10 < 5 → false
    cout << "这行永远不会执行" << endl;
    i++;
}
// 结果：什么都不打印

这不是错误，但你要理解：
- while 是"先判断，再执行"
- 如果条件一开始就是 false，循环体一次都不执行
- 这和 do-while 的区别（do-while 至少执行一次）
)" << endl;

    // --------------------------------------------
    // 4.5 错误5：= 和 == 混淆
    // --------------------------------------------

    cout << "4.5 错误5：= 和 == 混淆" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
❌ 错误代码：
int password = 0;
while (password = 1234) {  // 用了 = 而不是 ==
    cout << "请输入密码：";
    // ...
}
// 结果：死循环！

原因：
  = 是赋值：password = 1234 把 1234 赋给 password
  赋值表达式的值是 1234，非零即 true → 永远循环

✅ 正确代码：
int password = 0;
while (password == 1234) {  // 用 == 比较
    // ...
}

预防措施：
- 条件判断永远用 ==（双等号）
- 赋值用 =（单等号）
- 有些程序员会写 if (1234 == password) 来避免这个错误
  因为 1234 = password 会编译报错
)" << endl;

    cout << endl;
    cout << "============================================" << endl;
    cout << "  第五部分：函数卡片速查" << endl;
    cout << "============================================" << endl;

    cout << R"(

【结构卡片：while 循环】

语法：
  初始化;
  while (条件) {
      循环体;
      更新;
  }

执行流程：判断条件 → true 执行循环体 → 回到判断
特点：先判断，再执行（可能一次都不执行）

────────────────────────────────────

【对比卡片：for vs while】

for 循环：
  for (int i = 0; i < n; i++) { ... }
  适合：已知循环次数

while 循环：
  while (条件) { ... }
  适合：未知循环次数，条件驱动

等价转换：
  for (A; B; C) { D; }
  等价于
  A; while (B) { D; C; }

────────────────────────────────────

【运算符卡片：% 取余运算】

语法：a % b
作用：返回 a 除以 b 的余数

示例：
  12345 % 10 = 5    （取最后一位）
  12345 / 10 = 1234  （去掉最后一位）
  7 % 2 = 1          （判断奇偶：余1是奇数）
  10 % 3 = 1         （判断整除：余0能整除）

)" << endl;

    cout << endl;
    cout << "============================================" << endl;
    cout << "  学习完成！" << endl;
    cout << "============================================" << endl;

    cout << R"(
本节你学到了：
✅ while 循环的基本语法
✅ while 循环的执行流程（先判断，再执行）
✅ while vs for 的选择指南
✅ 数字拆分、位数计算等经典应用
✅ 5 个常见错误和预防措施

下一步：学习 10_do_while_loop.cpp（do-while 循环）！
)" << endl;

    return 0;
}
