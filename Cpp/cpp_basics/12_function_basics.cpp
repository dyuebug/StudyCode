// ============================================
// 函数基础 - 超详细注释版
// 日期：2026-03-09（第3天）
// 难度：⭐⭐ 中级
// ============================================

/*
【核心概念】函数是什么？

是什么？
- 函数是一段可以重复使用的代码块，有自己的名字

为什么需要？
- 避免代码重复（写一次，用多次）
- 让程序结构更清晰（每个函数做一件事）
- 方便维护和修改（改一处，全部生效）

核心特点？
- 有名字（函数名）
- 可以接收数据（参数）
- 可以返回结果（返回值）
*/

// ============================================
// 第一部分：核心概念详解 ⭐⭐⭐⭐⭐
// ============================================

#include <iostream>
#include <string>
using namespace std;

void conceptExplanation() {
    cout << string(60, '=') << endl;
    cout << "第一部分：核心概念详解 ⭐⭐⭐⭐⭐" << endl;
    cout << string(60, '=') << endl;

    // --------------------------------------------
    // 1.1 什么是函数？
    // --------------------------------------------

    cout << "\n1.1 什么是函数？" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【函数的本质】

想象你在做饭：
- 每次做饭都要：洗菜 → 切菜 → 炒菜 → 装盘
- 如果每次都写一遍这些步骤，太麻烦了
- 更好的方式：把这些步骤封装成"做饭"这个动作

函数就是这样：
- 把一系列操作封装成一个名字
- 需要时直接调用这个名字
- 不用每次都重复写相同的代码

【函数的三要素】

1. 函数名：给这段代码起个名字（如：做饭、计算、打印）
2. 参数：函数需要的输入数据（如：食材、数字）
3. 返回值：函数执行后的结果（如：菜品、计算结果）
)" << endl;

    // --------------------------------------------
    // 1.2 为什么要用函数？
    // --------------------------------------------

    cout << "\n1.2 为什么要用函数？" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【没有函数的代码】

// 计算1到10的和
int sum1 = 0;
for (int i = 1; i <= 10; i++) {
    sum1 += i;
}
cout << "1到10的和：" << sum1 << endl;

// 计算1到20的和
int sum2 = 0;
for (int i = 1; i <= 20; i++) {
    sum2 += i;
}
cout << "1到20的和：" << sum2 << endl;

// 计算1到30的和
int sum3 = 0;
for (int i = 1; i <= 30; i++) {
    sum3 += i;
}
cout << "1到30的和：" << sum3 << endl;

问题：
- 代码重复了3次
- 如果逻辑有错，要改3个地方
- 代码又长又乱

【有函数的代码】

// 定义一个计算和的函数
int calculateSum(int n) {
    int sum = 0;
    for (int i = 1; i <= n; i++) {
        sum += i;
    }
    return sum;
}

// 使用函数
cout << "1到10的和：" << calculateSum(10) << endl;
cout << "1到20的和：" << calculateSum(20) << endl;
cout << "1到30的和：" << calculateSum(30) << endl;

优点：
- 代码只写一次
- 逻辑清晰，易于理解
- 修改方便，改一处全部生效
)" << endl;

    // --------------------------------------------
    // 1.3 函数的基本语法
    // --------------------------------------------

    cout << "\n1.3 函数的基本语法" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【函数定义的完整格式】

返回类型 函数名(参数类型1 参数名1, 参数类型2 参数名2, ...) {
    // 函数体：要执行的代码
    return 返回值;  // 如果返回类型不是void
}

【各部分说明】

1. 返回类型：函数执行后返回什么类型的数据
   - int：返回整数
   - double：返回小数
   - string：返回字符串
   - void：不返回任何值

2. 函数名：给函数起的名字
   - 命名规则：字母、数字、下划线，不能以数字开头
   - 建议：使用有意义的名字（如：calculateSum、printInfo）

3. 参数列表：函数需要的输入数据
   - 可以有0个、1个或多个参数
   - 每个参数都要指定类型和名字
   - 多个参数用逗号分隔

4. 函数体：函数要执行的代码
   - 用大括号 {} 包围
   - 包含具体的操作步骤

5. return 语句：返回结果
   - 如果返回类型不是void，必须有return
   - return后面的值类型要和返回类型匹配
   - return会立即结束函数执行

【示例】

// 示例1：无参数，无返回值
void sayHello() {
    cout << "Hello!" << endl;
}

// 示例2：有参数，无返回值
void printNumber(int num) {
    cout << "数字是：" << num << endl;
}

// 示例3：有参数，有返回值
int add(int a, int b) {
    return a + b;
}

// 示例4：无参数，有返回值
int getRandomNumber() {
    return 42;
}
)" << endl;

    return 0;
}

// ============================================
// 第二部分：基础操作实践
// ============================================

// --------------------------------------------
// 2.1 定义一个简单函数
// --------------------------------------------

// 函数定义：打印问候语
// 返回类型：void（不返回任何值）
// 函数名：greet
// 参数：无
void greet() {
    // 函数体：要执行的代码
    cout << "欢迎学习C++函数！" << endl;
}

// --------------------------------------------
// 2.2 定义带参数的函数
// --------------------------------------------

// 函数定义：打印指定次数的问候语
// 返回类型：void（不返回任何值）
// 函数名：greetMultipleTimes
// 参数：int times - 要打印的次数
void greetMultipleTimes(int times) {
    // 使用循环打印多次
    for (int i = 0; i < times; i++) {
        cout << "你好！" << endl;
    }
}

// --------------------------------------------
// 2.3 定义有返回值的函数
// --------------------------------------------

// 函数定义：计算两个数的和
// 返回类型：int（返回整数）
// 函数名：add
// 参数：int a - 第一个加数
//       int b - 第二个加数
// 返回值：a + b 的结果
int add(int a, int b) {
    // 计算和
    int sum = a + b;

    // 返回结果
    return sum;

    // 注意：return后面的代码不会执行
    // cout << "这行不会被执行" << endl;
}

// --------------------------------------------
// 2.4 定义多个参数的函数
// --------------------------------------------

// 函数定义：计算矩形面积
// 返回类型：double（返回小数）
// 函数名：calculateRectangleArea
// 参数：double length - 长度
//       double width - 宽度
// 返回值：长 × 宽
double calculateRectangleArea(double length, double width) {
    // 直接返回计算结果
    return length * width;
}

// --------------------------------------------
// 2.5 主函数：测试所有函数
// --------------------------------------------

void testFunctions() {
    cout << string(60, '=') << endl;
    cout << "第二部分：基础操作实践" << endl;
    cout << string(60, '=') << endl;

    // --------------------------------------------
    // 测试1：调用无参数函数
    // --------------------------------------------

    cout << "\n2.1 调用无参数函数" << endl;
    cout << string(60, '-') << endl;

    // 调用函数：直接写函数名和括号
    greet();

    cout << "\n说明：" << endl;
    cout << "- 调用函数时，写函数名 + 括号" << endl;
    cout << "- 程序会跳转到函数定义处执行" << endl;
    cout << "- 执行完后返回继续执行后面的代码" << endl;

    // --------------------------------------------
    // 测试2：调用带参数的函数
    // --------------------------------------------

    cout << "\n2.2 调用带参数的函数" << endl;
    cout << string(60, '-') << endl;

    // 调用函数：传入参数3
    cout << "打印3次问候：" << endl;
    greetMultipleTimes(3);

    cout << "\n说明：" << endl;
    cout << "- 调用时在括号里传入参数值" << endl;
    cout << "- 参数值会传递给函数内部使用" << endl;
    cout << "- 这里传入3，函数就会循环3次" << endl;

    // --------------------------------------------
    // 测试3：调用有返回值的函数
    // --------------------------------------------

    cout << "\n2.3 调用有返回值的函数" << endl;
    cout << string(60, '-') << endl;

    // 方式1：用变量接收返回值
    int result = add(10, 20);
    cout << "10 + 20 = " << result << endl;

    // 方式2：直接在表达式中使用
    cout << "5 + 8 = " << add(5, 8) << endl;

    // 方式3：在条件判断中使用
    if (add(3, 4) > 5) {
        cout << "3 + 4 的结果大于5" << endl;
    }

    cout << "\n说明：" << endl;
    cout << "- 有返回值的函数可以当作一个值来使用" << endl;
    cout << "- 可以赋值给变量、用在表达式中、用在条件判断中" << endl;

    // --------------------------------------------
    // 测试4：调用多参数函数
    // --------------------------------------------

    cout << "\n2.4 调用多参数函数" << endl;
    cout << string(60, '-') << endl;

    // 计算矩形面积
    double area = calculateRectangleArea(5.5, 3.2);
    cout << "长5.5，宽3.2的矩形面积：" << area << endl;

    cout << "\n说明：" << endl;
    cout << "- 多个参数用逗号分隔" << endl;
    cout << "- 参数顺序要和函数定义时一致" << endl;
    cout << "- 第一个参数对应length，第二个对应width" << endl;
}

/*
==============================================
🔍 深入理解：函数的执行流程（选学）
==============================================

如果你想知道"函数调用时程序是如何执行的"，可以阅读这部分。
如果觉得难，可以跳过，不影响后续学习。

【函数调用的内存过程】

当你调用一个函数时，计算机做了这些事：

1. 保存当前位置
   - 记住调用函数的地方，以便执行完后能回来

2. 跳转到函数定义
   - 程序跳转到函数定义的位置开始执行

3. 创建局部变量
   - 为函数的参数和局部变量分配内存空间
   - 这些变量只在函数内部有效

4. 执行函数体
   - 按顺序执行函数内的代码

5. 返回结果
   - 如果有return，将返回值传回调用处
   - 如果是void函数，直接返回

6. 清理内存
   - 释放函数的局部变量占用的内存

7. 回到调用处
   - 程序回到调用函数的地方继续执行

【示例：追踪执行流程】

int add(int a, int b) {
    cout << "进入add函数" << endl;  // 第2步
    int sum = a + b;                 // 第3步
    cout << "计算完成" << endl;      // 第4步
    return sum;                      // 第5步
}

int main() {
    cout << "开始" << endl;          // 第1步
    int result = add(3, 5);          // 调用函数
    cout << "结果：" << result << endl;  // 第6步
    return 0;
}

执行顺序：
1. 打印"开始"
2. 调用add(3, 5)，跳转到add函数
3. 打印"进入add函数"
4. 计算sum = 3 + 5 = 8
5. 打印"计算完成"
6. 返回8，赋值给result
7. 打印"结果：8"

【函数调用栈】

计算机使用"栈"来管理函数调用：
- 每次调用函数，就在栈顶压入一个"栈帧"
- 栈帧包含：参数、局部变量、返回地址
- 函数执行完，就弹出栈帧
- 这就是为什么函数可以嵌套调用

示例：
main() 调用 funcA()
funcA() 调用 funcB()
funcB() 调用 funcC()

栈的变化：
[main]
[main][funcA]
[main][funcA][funcB]
[main][funcA][funcB][funcC]  ← 栈顶
[main][funcA][funcB]          ← funcC执行完，弹出
[main][funcA]                 ← funcB执行完，弹出
[main]                        ← funcA执行完，弹出

==============================================
*/

// ============================================
// 第四部分：常见错误和陷阱 ⭐⭐⭐⭐⭐
// ============================================

void commonMistakes() {
    cout << string(60, '=') << endl;
    cout << "常见错误和陷阱 ⭐⭐⭐⭐⭐" << endl;
    cout << string(60, '=') << endl;

    cout << R"(
// --------------------------------------------
// 4.1 错误1：函数未定义就使用
// --------------------------------------------

❌ 错误代码：

int main() {
    greet();  // 错误：greet函数还没定义
    return 0;
}

void greet() {
    cout << "Hello!" << endl;
}

结果：编译错误 - 'greet' was not declared in this scope

原因：C++从上到下编译，使用函数前必须先定义或声明

✅ 正确代码（方式1）：

// 先定义函数
void greet() {
    cout << "Hello!" << endl;
}

// 再使用函数
int main() {
    greet();  // 正确
    return 0;
}

✅ 正确代码（方式2）：

// 先声明函数（告诉编译器函数存在）
void greet();

// 使用函数
int main() {
    greet();  // 正确
    return 0;
}

// 后面再定义函数
void greet() {
    cout << "Hello!" << endl;
}

记忆技巧：
- 函数要"先声明，后使用"
- 或者把函数定义放在main函数前面

// --------------------------------------------
// 4.2 错误2：返回类型不匹配
// --------------------------------------------

❌ 错误代码：

int add(int a, int b) {
    return a + b;  // 正确
}

int divide(int a, int b) {
    return a / b;  // 可能丢失小数部分
}

int main() {
    cout << divide(5, 2) << endl;  // 输出2，不是2.5
    return 0;
}

结果：输出2（整数除法，丢失小数）

原因：返回类型是int，会截断小数部分

✅ 正确代码：

double divide(int a, int b) {
    // 将其中一个转换为double，确保小数除法
    return (double)a / b;
}

int main() {
    cout << divide(5, 2) << endl;  // 输出2.5
    return 0;
}

记忆技巧：
- 返回类型要和实际返回的数据类型匹配
- 需要小数结果时，用double而不是int

// --------------------------------------------
// 4.3 错误3：忘记return语句
// --------------------------------------------

❌ 错误代码：

int getNumber() {
    int num = 42;
    // 忘记return
}

int main() {
    int result = getNumber();
    cout << result << endl;  // 输出随机值
    return 0;
}

结果：编译警告，运行时输出随机值

原因：函数声明了返回int，但没有return语句

✅ 正确代码：

int getNumber() {
    int num = 42;
    return num;  // 必须有return
}

记忆技巧：
- 返回类型不是void的函数，必须有return
- 编译器会给警告，一定要注意

// --------------------------------------------
// 4.4 错误4：参数顺序错误
// --------------------------------------------

❌ 错误代码：

// 函数定义：第一个参数是被除数，第二个是除数
double divide(double dividend, double divisor) {
    return dividend / divisor;
}

int main() {
    // 错误：参数顺序反了
    double result = divide(2, 10);  // 想算10/2，结果算成2/10
    cout << result << endl;  // 输出0.2，而不是5
    return 0;
}

结果：计算结果错误

原因：参数顺序和预期不一致

✅ 正确代码：

int main() {
    // 正确：按照函数定义的顺序传参
    double result = divide(10, 2);  // 10/2 = 5
    cout << result << endl;  // 输出5
    return 0;
}

记忆技巧：
- 调用函数时，参数顺序要和定义时一致
- 给参数起有意义的名字，避免混淆

// --------------------------------------------
// 4.5 错误5：void函数尝试返回值
// --------------------------------------------

❌ 错误代码：

void printSum(int a, int b) {
    int sum = a + b;
    return sum;  // 错误：void函数不能返回值
}

结果：编译错误 - void function should not return a value

原因：void表示不返回任何值

✅ 正确代码（方式1）：

void printSum(int a, int b) {
    int sum = a + b;
    cout << "和是：" << sum << endl;
    // void函数可以不写return，或者写return;
}

✅ 正确代码（方式2）：

int calculateSum(int a, int b) {
    int sum = a + b;
    return sum;  // 改成int返回类型
}

记忆技巧：
- void函数用于"做事情"，不返回结果
- 需要返回结果时，改用对应的返回类型

// --------------------------------------------
// 4.6 错误6：参数类型不匹配
// --------------------------------------------

❌ 错误代码：

void printNumber(int num) {
    cout << num << endl;
}

int main() {
    printNumber("123");  // 错误：传入字符串，期望int
    return 0;
}

结果：编译错误 - invalid conversion from 'const char*' to 'int'

原因：参数类型不匹配

✅ 正确代码：

int main() {
    printNumber(123);  // 正确：传入整数
    return 0;
}

记忆技巧：
- 调用函数时，参数类型要和定义时一致
- 编译器会检查类型，注意看错误提示

// --------------------------------------------
// 4.7 错误7：函数内修改参数不影响外部
// --------------------------------------------

❌ 错误代码：

void changeValue(int num) {
    num = 100;  // 只改变了函数内部的副本
}

int main() {
    int x = 10;
    changeValue(x);
    cout << x << endl;  // 输出10，不是100
    return 0;
}

结果：x的值没有改变

原因：参数是按值传递的，函数内修改的是副本

✅ 正确理解：

这不是错误，而是C++的设计：
- 默认情况下，参数是按值传递的
- 函数内部得到的是参数的副本
- 修改副本不影响原始变量

如果想修改原始变量，需要使用引用（后续会学）

记忆技巧：
- 函数参数默认是"复制"过去的
- 想修改原始变量，需要用引用（下节课学习）

)" << endl;
}

// ============================================
// 第五部分：函数卡片速查
// ============================================

void functionCards() {
    cout << string(60, '=') << endl;
    cout << "函数卡片速查" << endl;
    cout << string(60, '=') << endl;

    cout << R"(
【函数定义语法】

返回类型 函数名(参数类型1 参数1, 参数类型2 参数2, ...) {
    // 函数体
    return 返回值;  // 如果返回类型不是void
}

────────────────────────────────────

【无参数无返回值函数】

void functionName() {
    // 执行操作
}

示例：
void sayHello() {
    cout << "Hello!" << endl;
}

调用：sayHello();

────────────────────────────────────

【有参数无返回值函数】

void functionName(类型 参数名) {
    // 使用参数执行操作
}

示例：
void printNumber(int num) {
    cout << num << endl;
}

调用：printNumber(42);

────────────────────────────────────

【有参数有返回值函数】

返回类型 functionName(类型 参数名) {
    // 计算结果
    return 结果;
}

示例：
int add(int a, int b) {
    return a + b;
}

调用：int result = add(3, 5);

────────────────────────────────────

【函数声明】

返回类型 函数名(参数类型1, 参数类型2, ...);

示例：
int add(int, int);  // 参数名可以省略
void greet();

作用：告诉编译器函数存在，具体定义可以放在后面

)" << endl;
}

// ============================================
// 第六部分：练习题
// ============================================

void exercises() {
    cout << string(60, '=') << endl;
    cout << "练习题" << endl;
    cout << string(60, '=') << endl;

    cout << R"(
1. 编写一个函数 printStars(int n)
   要求：打印n个星号
   提示：使用循环，每次打印一个*

2. 编写一个函数 isEven(int num)
   要求：判断一个数是否为偶数，返回bool类型
   提示：偶数的特点是能被2整除（num % 2 == 0）

3. 编写一个函数 getMax(int a, int b)
   要求：返回两个数中较大的那个
   提示：使用if-else或三元运算符

4. 编写一个函数 calculateAverage(int a, int b, int c)
   要求：计算三个数的平均值，返回double类型
   提示：先求和，再除以3，注意类型转换

5. 编写一个函数 printMultiplicationTable(int n)
   要求：打印n的乘法表（1*n到9*n）
   提示：使用循环，每次打印 i * n 的结果

)" << endl;
}

// ============================================
// 主程序：运行所有示例
// ============================================

// 注意：这个main函数会调用上面定义的所有测试函数
// 编译命令：g++ -o 12_function_basics 12_function_basics.cpp
// 运行命令：./12_function_basics 或 12_function_basics.exe

/*
int main() {
    // 运行所有测试
    testFunctions();
    cout << "\n";
    commonMistakes();
    cout << "\n";
    functionCards();
    cout << "\n";
    exercises();

    return 0;
}
*/
