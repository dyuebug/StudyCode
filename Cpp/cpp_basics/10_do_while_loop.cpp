// ============================================
// do-while 循环 - 超详细注释版
// 日期：2026-02-26（第2天）
// 难度：⭐⭐ 中级
// 编译：g++ -o 10_do_while_loop 10_do_while_loop.cpp
// 运行：./10_do_while_loop 或 10_do_while_loop.exe
// C++版本：C++11 及以上
// ============================================

/*
【核心概念】do-while 循环

是什么？
- do-while 是一种"先执行，后判断"的循环结构
- 循环体至少执行一次，然后再检查条件

为什么需要？
- 有些场景必须先做一次，再决定要不要继续
- 比如：菜单系统 → 先显示菜单，再问用户要不要继续
- 比如：输入验证 → 先让用户输入，再检查是否合法

核心特点：
- 先执行循环体，再判断条件
- 循环体至少执行一次（和 while 的关键区别）
- 末尾有分号：while (条件);  ← 别忘了这个分号！
*/

#include <iostream>
using namespace std;

int main() {

    cout << "============================================" << endl;
    cout << "  第一部分：核心概念详解 ⭐⭐⭐⭐⭐" << endl;
    cout << "============================================" << endl;

    // --------------------------------------------
    // 1.1 do-while 循环是什么？
    // --------------------------------------------

    cout << endl;
    cout << "1.1 do-while 循环是什么？" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【do-while 循环】

想象一个场景：你去餐厅吃自助餐

你的行为模式：
  1. 先去拿一盘菜（执行循环体）
  2. 吃完后问自己：还吃得下吗？（条件判断）
  3. 吃得下 → 再去拿一盘（继续循环）
  4. 吃不下 → 结束用餐（循环结束）

关键点：你至少会拿一盘菜（至少执行一次）
        不可能一盘都不拿就走（这就是和 while 的区别）

)" << endl;

    // --------------------------------------------
    // 1.2 do-while 语法详解 ⭐⭐⭐⭐⭐
    // --------------------------------------------

    cout << "1.2 do-while 语法详解" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【语法卡片】do-while 循环

完整语法：
┌─────────────────────────────────────────────┐
│  do {                                        │
│      // 循环体：要重复执行的代码             │
│  } while (条件);  ← 注意这里有分号！         │
└─────────────────────────────────────────────┘

执行流程：
  1. 执行循环体（第一次无条件执行）
  2. 检查条件 → true？回到第 1 步  false？结束

)" << endl;

    // --------------------------------------------
    // 1.3 三种循环的对比 ⭐⭐⭐⭐⭐
    // --------------------------------------------

    cout << "1.3 三种循环的对比" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【三种循环对比表】

┌──────────┬──────────────┬──────────────┬──────────────┐
│          │   for        │   while      │   do-while   │
├──────────┼──────────────┼──────────────┼──────────────┤
│ 判断时机 │ 先判断再执行 │ 先判断再执行 │ 先执行再判断 │
│ 最少执行 │ 0 次         │ 0 次         │ 1 次         │
│ 适用场景 │ 已知次数     │ 未知次数     │ 至少一次     │
│ 典型用途 │ 遍历、计数   │ 条件等待     │ 菜单、验证   │
│ 分号     │ 无           │ 无           │ 末尾有分号   │
└──────────┴──────────────┴──────────────┴──────────────┘

简单记忆：
  for    → "跑 5 圈"（知道次数）
  while  → "排队等奶茶"（不知道等多久）
  do-while → "自助餐拿菜"（至少拿一盘）

)" << endl;

    cout << endl;
    cout << "============================================" << endl;
    cout << "  第二部分：基础操作实践" << endl;
    cout << "============================================" << endl;

    // --------------------------------------------
    // 2.1 最简单的 do-while
    // --------------------------------------------

    cout << endl;
    cout << "2.1 最简单的 do-while" << endl;
    cout << string(60, '-') << endl;

    int i = 1;

    do {
        // 循环体：先执行
        cout << "  第 " << i << " 次循环" << endl;
        i++;
    } while (i <= 5);  // 后判断：i <= 5 时继续
    // 注意末尾的分号！这是 do-while 独有的

    // --------------------------------------------
    // 2.2 do-while vs while 的关键区别
    // --------------------------------------------

    cout << endl;
    cout << "2.2 关键区别演示：条件一开始就是 false" << endl;
    cout << string(60, '-') << endl;

    // while 版本：条件一开始就是 false
    cout << "  while 版本（i=10, 条件 i<5）：" << endl;
    int a = 10;
    while (a < 5) {
        cout << "    执行了 while 循环体" << endl;  // 不会执行
        a++;
    }
    cout << "    （什么都没打印，循环体没执行）" << endl;

    // do-while 版本：条件一开始就是 false
    cout << "  do-while 版本（i=10, 条件 i<5）：" << endl;
    int b = 10;
    do {
        cout << "    执行了 do-while 循环体（至少一次！）" << endl;  // 会执行一次
        b++;
    } while (b < 5);
    cout << "    （打印了一次，因为 do-while 至少执行一次）" << endl;

    // --------------------------------------------
    // 2.3 菜单系统（do-while 的经典应用）
    // --------------------------------------------

    cout << endl;
    cout << "2.3 菜单系统（模拟版）" << endl;
    cout << string(60, '-') << endl;

    // 模拟用户选择（不需要真正输入）
    // 模拟的选择序列：1 → 2 → 3（退出）
    int choices[] = {1, 2, 3};  // 模拟用户的选择
    int choiceIndex = 0;        // 当前选择的索引
    int choice;                 // 存储当前选择

    do {
        // 先显示菜单（至少显示一次）
        cout << "  ┌─────────────────┐" << endl;
        cout << "  │  简易计算器菜单  │" << endl;
        cout << "  ├─────────────────┤" << endl;
        cout << "  │  1. 加法         │" << endl;
        cout << "  │  2. 减法         │" << endl;
        cout << "  │  3. 退出         │" << endl;
        cout << "  └─────────────────┘" << endl;

        // 模拟用户输入
        choice = choices[choiceIndex];
        cout << "  模拟选择：" << choice << endl;
        choiceIndex++;

        // 根据选择执行操作
        switch (choice) {
            case 1:
                cout << "  → 执行加法运算..." << endl;
                break;
            case 2:
                cout << "  → 执行减法运算..." << endl;
                break;
            case 3:
                cout << "  → 退出程序" << endl;
                break;
            default:
                cout << "  → 无效选择！" << endl;
        }
        cout << endl;

    } while (choice != 3);  // 用户选择 3 时退出
    // 这里用 do-while 很合适：菜单至少要显示一次

    // --------------------------------------------
    // 2.4 输入验证（模拟版）
    // --------------------------------------------

    cout << "2.4 输入验证（模拟版）" << endl;
    cout << string(60, '-') << endl;

    // 模拟：要求输入 1-100 之间的数
    // 模拟输入序列：-5 → 200 → 42（合法）
    int inputs[] = {-5, 200, 42};
    int inputIndex = 0;
    int userInput;

    do {
        // 先让用户输入（至少输入一次）
        userInput = inputs[inputIndex];
        cout << "  模拟输入：" << userInput;

        if (userInput < 1 || userInput > 100) {
            cout << " → 无效！请输入 1-100 之间的数" << endl;
        } else {
            cout << " → 有效！" << endl;
        }

        inputIndex++;
    } while (userInput < 1 || userInput > 100);
    // 条件：输入不合法时继续循环

    cout << "  最终接受的输入：" << userInput << endl;

    // --------------------------------------------
    // 2.5 数字反转
    // --------------------------------------------

    cout << endl;
    cout << "2.5 数字反转" << endl;
    cout << string(60, '-') << endl;

    int original = 12345;
    int temp = original;
    int reversed = 0;

    // 用 do-while 确保 0 也能正确处理
    do {
        // 取出最后一位
        int digit = temp % 10;
        // 把它放到 reversed 的末尾
        // reversed * 10 腾出一个位置，再加上 digit
        reversed = reversed * 10 + digit;
        // 去掉最后一位
        temp /= 10;
    } while (temp > 0);

    cout << "  原始数字：" << original << endl;
    cout << "  反转结果：" << reversed << endl;

    cout << endl;
    cout << "============================================" << endl;
    cout << "  第三部分：深入理解（选学）" << endl;
    cout << "============================================" << endl;

    cout << R"(
🔍 深入理解：do-while 的实际应用场景

在实际开发中，do-while 用得比 for 和 while 少，
但在以下场景中它是最佳选择：

1. 菜单系统：菜单至少要显示一次
2. 输入验证：至少要让用户输入一次
3. 游戏回合：至少要玩一个回合
4. 重试机制：至少要尝试一次操作

一个实用的判断标准：
  如果你发现自己在 while 循环前复制了一份循环体的代码，
  那就应该改用 do-while。

  ❌ 不好的写法：
  showMenu();           // 先执行一次
  while (choice != 0) { // 再循环
      showMenu();       // 重复代码！
  }

  ✅ 好的写法：
  do {
      showMenu();       // 只写一次
  } while (choice != 0);

如果现在不理解，可以跳过，不影响后续学习。
)" << endl;

    cout << endl;
    cout << "============================================" << endl;
    cout << "  第四部分：常见错误和陷阱 ⭐⭐⭐⭐⭐" << endl;
    cout << "============================================" << endl;

    cout << endl;
    cout << "4.1 错误1：忘记末尾的分号" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
❌ 错误代码：
do {
    cout << "hello" << endl;
} while (条件)   // 缺少分号！
// 结果：编译错误

原因：do-while 的语法要求 while (条件) 后面必须有分号

✅ 正确代码：
do {
    cout << "hello" << endl;
} while (条件);  // 别忘了分号！

记忆技巧：
- do-while 是三种循环中唯一需要末尾分号的
- 因为 while (条件) 不是循环的开始，而是结束
)" << endl;

    cout << "4.2 错误2：和 while 混淆" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
❌ 混淆场景：
// 想用 do-while，但写成了 while
int input = -1;
while (input < 0) {
    // 如果 input 初始值恰好 >= 0，循环体不执行
    // 用户没有机会输入
}

✅ 正确理解：
// 需要"至少执行一次" → 用 do-while
int input;
do {
    cout << "请输入正数：";
    // cin >> input;
} while (input < 0);

判断方法：
  问自己："循环体是否必须至少执行一次？"
  是 → do-while
  否 → while 或 for
)" << endl;

    cout << "4.3 错误3：条件逻辑写反" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
❌ 错误代码：
int choice;
do {
    // 显示菜单...
    // cin >> choice;
} while (choice == 0);  // 想在选择 0 时退出

问题：这个条件的意思是"当 choice 等于 0 时继续循环"
      实际效果：选 0 继续，选其他退出（和预期相反！）

✅ 正确代码：
do {
    // 显示菜单...
    // cin >> choice;
} while (choice != 0);  // 当 choice 不等于 0 时继续

记忆技巧：
- while 里的条件是"继续循环的条件"，不是"退出条件"
- 想在 X 时退出 → 条件写 != X
- 想在 X 时继续 → 条件写 == X
)" << endl;

    cout << "4.4 错误4：do-while 中的变量作用域" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
❌ 错误代码：
do {
    int x = 10;  // x 在循环体内部声明
} while (x > 0);  // 错误！x 在这里不可见

原因：x 在 {} 内部声明，出了 {} 就不存在了
      while (x > 0) 在 {} 外面，访问不到 x

✅ 正确代码：
int x;  // 在 do-while 外部声明
do {
    x = 10;  // 在循环体内赋值
} while (x > 0);  // 可以访问 x

预防措施：
- 需要在 while 条件中使用的变量，必须在 do 之前声明
- 这是 do-while 特有的陷阱
)" << endl;

    cout << "4.5 错误5：不必要地使用 do-while" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
❌ 不推荐：
// 已知循环 5 次，却用 do-while
int i = 0;
do {
    cout << i << " ";
    i++;
} while (i < 5);

✅ 推荐：
// 已知循环次数，用 for 更清晰
for (int i = 0; i < 5; i++) {
    cout << i << " ";
}

选择原则：
- 已知次数 → for
- 未知次数，可能 0 次 → while
- 未知次数，至少 1 次 → do-while
- 不要为了用 do-while 而用 do-while
)" << endl;

    cout << endl;
    cout << "============================================" << endl;
    cout << "  第五部分：函数卡片速查" << endl;
    cout << "============================================" << endl;

    cout << R"(

【结构卡片：do-while 循环】

语法：
  do {
      循环体;
  } while (条件);  ← 注意分号！

执行流程：执行循环体 → 判断条件 → true 继续，false 结束
特点：先执行，后判断（至少执行一次）

────────────────────────────────────

【三种循环选择速查】

已知循环次数？
  → 是：用 for
  → 否：循环体至少执行一次？
         → 是：用 do-while
         → 否：用 while

)" << endl;

    cout << endl;
    cout << "============================================" << endl;
    cout << "  学习完成！" << endl;
    cout << "============================================" << endl;

    cout << R"(
本节你学到了：
✅ do-while 循环的基本语法（别忘了末尾分号！）
✅ do-while 的执行流程（先执行，后判断）
✅ do-while vs while 的关键区别（至少执行一次）
✅ 三种循环的完整对比和选择指南
✅ 菜单系统、输入验证等经典应用
✅ 5 个常见错误和预防措施

下一步：学习 11_loop_control.cpp（break 和 continue）！
)" << endl;

    return 0;
}
