// ============================================
// 96_modules_vs_headers.cpp - Modules vs 传统头文件对比
// 功能：全面对比模块和传统头文件的优劣
// 注意：本文件以概念讲解为主，包含对比示例
// 编译：g++ -std=c++20 -o 96_modules_vs_headers.exe 96_modules_vs_headers.cpp
// ============================================

#include <iostream>
#include <string>
using namespace std;

int main() {
    cout << "=== Modules vs 传统头文件对比 ===" << endl;

    // --------------------------------------------
    // 对比1：传统头文件的问题
    // --------------------------------------------

    cout << "\n【对比1】传统头文件的问题" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【问题1：重复包含（Multiple Inclusion）】

传统头文件方式：
------------------------------------------------------------
// math.h
#ifndef MATH_H
#define MATH_H

int add(int a, int b);

#endif

// utils.h
#include "math.h"

void print_sum(int a, int b);

// main.cpp
#include "math.h"   // 第1次包含 math.h
#include "utils.h"  // 第2次包含 math.h（通过 utils.h）

问题：
- math.h 被包含了2次
- 需要使用 #ifndef 防护（Include Guard）
- 如果忘记防护，会导致重复定义错误
- 即使有防护，预处理器仍然要处理多次

模块方式：
------------------------------------------------------------
// math.cppm
export module math;

export int add(int a, int b) {
    return a + b;
}

// utils.cppm
import math;
export module utils;

export void print_sum(int a, int b);

// main.cpp
import math;
import utils;

优势：
- 模块只编译一次，不会重复包含
- 不需要 Include Guard
- 编译器自动处理依赖关系

────────────────────────────────────

【问题2：编译速度慢】

传统头文件方式：
------------------------------------------------------------
// big_header.h（假设有10000行代码）
class ClassA { ... };
class ClassB { ... };
// ... 很多类和函数

// file1.cpp
#include "big_header.h"  // 编译10000行

// file2.cpp
#include "big_header.h"  // 再次编译10000行

// file3.cpp
#include "big_header.h"  // 再次编译10000行

问题：
- 每个 .cpp 文件都要重新编译头文件
- 如果头文件很大，编译时间很长
- 修改头文件，所有包含它的文件都要重新编译

示例：
- 100个 .cpp 文件包含同一个10000行的头文件
- 总共要编译 100 × 10000 = 1,000,000 行代码
- 编译时间：假设每行0.001秒，总共1000秒（16分钟）

模块方式：
------------------------------------------------------------
// big_module.cppm（假设有10000行代码）
export module big_module;

export class ClassA { ... };
export class ClassB { ... };
// ... 很多类和函数

// file1.cpp
import big_module;  // 使用 BMI，不需要重新编译

// file2.cpp
import big_module;  // 使用 BMI，不需要重新编译

// file3.cpp
import big_module;  // 使用 BMI，不需要重新编译

优势：
- 模块只编译一次，生成 BMI
- 其他文件直接使用 BMI，不需要重新编译
- 修改模块实现（不改接口），不影响使用模块的文件

示例：
- 100个 .cpp 文件导入同一个10000行的模块
- 模块编译一次：10000行
- 其他文件使用 BMI：几乎不需要时间
- 编译时间：假设每行0.001秒，总共10秒

编译速度对比：
- 传统头文件：1000秒（16分钟）
- 模块：10秒
- 提速：100倍！

────────────────────────────────────

【问题3：符号污染（Symbol Pollution）】

传统头文件方式：
------------------------------------------------------------
// math.h
#define MAX 100  // 宏定义

int add(int a, int b);

// main.cpp
#include "math.h"

int main() {
    int MAX = 200;  // 错误：MAX 已经被定义为宏
    return 0;
}

问题：
- 头文件中的宏会污染全局命名空间
- 容易导致命名冲突
- 难以调试

模块方式：
------------------------------------------------------------
// math.cppm
export module math;

#define MAX 100  // 宏定义（不会导出）

export int add(int a, int b) {
    return a + b;
}

// main.cpp
import math;

int main() {
    int MAX = 200;  // 正确：模块的宏不会污染
    return 0;
}

优势：
- 模块不会导出宏定义
- 避免宏污染
- 更安全的代码

────────────────────────────────────

【问题4：依赖关系不清晰】

传统头文件方式：
------------------------------------------------------------
// a.h
#include "b.h"

// b.h
#include "c.h"

// c.h
#include "a.h"  // 循环依赖！

问题：
- 容易出现循环依赖
- 依赖关系不清晰
- 难以维护

模块方式：
------------------------------------------------------------
// a.cppm
import b;
export module a;

// b.cppm
import c;
export module b;

// c.cppm
// import a;  // 编译错误：循环依赖
export module c;

优势：
- 编译器检测循环依赖
- 依赖关系清晰
- 更容易维护

────────────────────────────────────

【问题5：顺序依赖（Order Dependency）】

传统头文件方式：
------------------------------------------------------------
// main.cpp
#include "b.h"  // 错误：b.h 依赖 a.h
#include "a.h"

// 正确的顺序：
#include "a.h"
#include "b.h"

问题：
- 包含顺序很重要
- 容易出错
- 难以维护

模块方式：
------------------------------------------------------------
// main.cpp
import b;  // 正确：模块自动处理依赖
import a;

// 或者
import a;
import b;  // 顺序无关紧要

优势：
- 导入顺序无关紧要
- 编译器自动处理依赖
- 更容易使用
)" << endl;

    // --------------------------------------------
    // 对比2：模块的优势
    // --------------------------------------------

    cout << "\n【对比2】模块的优势" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【优势1：编译速度快】

传统头文件：
- 每个 .cpp 文件都要重新编译头文件
- 大型项目编译时间长（几分钟到几小时）

模块：
- 模块只编译一次，生成 BMI
- 其他文件直接使用 BMI
- 编译速度提升 10-100 倍

实际案例：
- Google Chrome：使用模块后，编译时间从 40 分钟减少到 4 分钟
- LLVM：使用模块后，编译时间减少 50%

────────────────────────────────────

【优势2：封装性强】

传统头文件：
- 所有内容都公开
- 容易出现符号冲突
- 难以隐藏实现细节

模块：
- 默认私有，只有 export 的内容才公开
- 避免符号污染
- 更好的封装性

示例：
------------------------------------------------------------
// 传统头文件
// math.h
int add(int a, int b);
int helper();  // 不想公开，但无法隐藏

// 模块
// math.cppm
export module math;

export int add(int a, int b) {
    return a + b;
}

int helper() {  // 私有函数，外部不可见
    return 42;
}

────────────────────────────────────

【优势3：依赖管理清晰】

传统头文件：
- 依赖关系隐式
- 容易出现循环依赖
- 难以维护

模块：
- 依赖关系显式（import）
- 编译器检测循环依赖
- 更容易维护

────────────────────────────────────

【优势4：无宏污染】

传统头文件：
- 宏会污染全局命名空间
- 容易导致命名冲突

模块：
- 模块不会导出宏定义
- 避免宏污染

────────────────────────────────────

【优势5：更好的工具支持】

模块：
- IDE 可以更好地理解代码结构
- 更好的代码补全
- 更好的重构支持
- 更好的错误提示
)" << endl;

    // --------------------------------------------
    // 对比3：模块的劣势
    // --------------------------------------------

    cout << "\n【对比3】模块的劣势" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【劣势1：编译器支持不完善】

现状：
- GCC 11+ 支持，但仍有 bug
- Clang 16+ 支持，但仍有限制
- MSVC 2019+ 支持，但语法略有不同
- 不同编译器的模块文件扩展名不同（.cppm、.ixx、.mpp）

问题：
- 跨编译器兼容性差
- 需要特殊的编译器标志
- 编译错误信息不够清晰

────────────────────────────────────

【劣势2：学习曲线陡峭】

传统头文件：
- 概念简单，容易理解
- 大量的教程和示例

模块：
- 概念较新，学习资料少
- 需要理解 BMI、模块接口单元、模块实现单元等概念
- 需要理解模块分区、子模块等高级特性

────────────────────────────────────

【劣势3：构建系统支持不完善】

现状：
- CMake 3.20+ 支持，但配置复杂
- Make、Ninja 等需要手动配置
- 很多第三方库还不支持模块

问题：
- 构建配置复杂
- 与现有项目集成困难
- 生态系统不成熟

────────────────────────────────────

【劣势4：调试困难】

问题：
- 调试器对模块的支持不完善
- 错误信息不够清晰
- 难以定位问题

────────────────────────────────────

【劣势5：与现有代码集成困难】

问题：
- 现有的大量代码使用头文件
- 迁移到模块需要大量工作
- 模块和头文件混用可能出现问题
)" << endl;

    // --------------------------------------------
    // 对比4：全面对比总结
    // --------------------------------------------

    cout << "\n【对比4】全面对比总结" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
┌──────────────┬────────────────┬────────────────┐
│   特性       │   传统头文件   │   模块         │
├──────────────┼────────────────┼────────────────┤
│ 编译速度     │ 慢             │ 快（10-100倍） │
│ 封装性       │ 弱（全部公开） │ 强（默认私有） │
│ 依赖管理     │ 隐式、复杂     │ 显式、清晰     │
│ 宏污染       │ 有             │ 无             │
│ 符号冲突     │ 容易           │ 不容易         │
│ 顺序依赖     │ 有             │ 无             │
│ 编译器支持   │ 完善           │ 不完善         │
│ 学习曲线     │ 平缓           │ 陡峭           │
│ 生态系统     │ 成熟           │ 不成熟         │
│ 调试支持     │ 好             │ 一般           │
│ 迁移成本     │ 无             │ 高             │
└──────────────┴────────────────┴────────────────┘

【编译速度对比】

假设：
- 项目有 1000 个 .cpp 文件
- 每个文件包含一个 10000 行的头文件
- 每行编译时间：0.001 秒

传统头文件：
- 总编译行数：1000 × 10000 = 10,000,000 行
- 总编译时间：10,000,000 × 0.001 = 10,000 秒（2.8 小时）

模块：
- 模块编译：10000 行
- 其他文件使用 BMI：几乎不需要时间
- 总编译时间：10000 × 0.001 = 10 秒

提速：1000 倍！

【内存占用对比】

传统头文件：
- 每个 .cpp 文件都要加载头文件到内存
- 内存占用：文件数 × 头文件大小

模块：
- 模块只加载一次 BMI
- 内存占用：BMI 大小（通常比头文件小）

【代码组织对比】

传统头文件：
------------------------------------------------------------
project/
├── include/
│   ├── math.h
│   ├── utils.h
│   └── ...
├── src/
│   ├── math.cpp
│   ├── utils.cpp
│   └── ...
└── main.cpp

问题：
- 头文件和实现分离
- 需要维护两个文件
- 容易出现不一致

模块：
------------------------------------------------------------
project/
├── modules/
│   ├── math.cppm
│   ├── utils.cppm
│   └── ...
└── main.cpp

优势：
- 接口和实现在一个文件中
- 更容易维护
- 不容易出现不一致
)" << endl;

    // --------------------------------------------
    // 对比5：何时使用模块，何时使用头文件
    // --------------------------------------------

    cout << "\n【对比5】何时使用模块，何时使用头文件" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【使用模块的场景】

✅ 新项目：
- 从头开始的项目
- 可以充分利用模块的优势
- 不需要考虑兼容性

✅ 大型项目：
- 编译时间长的项目
- 模块可以显著提升编译速度
- 值得投入迁移成本

✅ 需要强封装的项目：
- 需要隐藏实现细节
- 需要避免符号污染
- 需要清晰的依赖关系

✅ 编译器支持良好的环境：
- 使用最新的编译器
- 不需要跨编译器兼容
- 构建系统支持模块

────────────────────────────────────

【使用头文件的场景】

✅ 现有项目：
- 已经使用头文件的项目
- 迁移成本高
- 暂时不需要模块的优势

✅ 需要跨编译器兼容：
- 需要支持多个编译器
- 编译器版本较旧
- 模块支持不完善

✅ 第三方库：
- 大量第三方库使用头文件
- 模块生态系统不成熟
- 需要与现有库集成

✅ 小型项目：
- 编译时间不是问题
- 不需要模块的优势
- 头文件更简单

────────────────────────────────────

【混合使用】

可以在同一个项目中混合使用模块和头文件：

示例：
------------------------------------------------------------
// 新代码使用模块
// math.cppm
export module math;

export int add(int a, int b) {
    return a + b;
}

// 旧代码使用头文件
// utils.h
#ifndef UTILS_H
#define UTILS_H

void print(int x);

#endif

// main.cpp
import math;        // 导入模块
#include "utils.h"  // 包含头文件

int main() {
    int sum = add(10, 20);
    print(sum);
    return 0;
}

注意事项：
- 模块不能导入头文件（需要使用 import <iostream> 等）
- 头文件可以包含在模块中
- 混合使用可能导致编译问题

────────────────────────────────────

【迁移策略】

渐进式迁移：
1. 新代码使用模块
2. 旧代码保持头文件
3. 逐步将旧代码迁移到模块
4. 最终全部使用模块

优先迁移：
1. 编译时间长的头文件
2. 经常修改的头文件
3. 需要强封装的模块
4. 独立的模块

────────────────────────────────────

【建议】

对于新项目：
- 优先使用模块
- 享受模块的优势
- 为未来做准备

对于现有项目：
- 评估迁移成本
- 优先迁移关键模块
- 渐进式迁移

对于学习：
- 理解模块的概念
- 了解模块的优势和劣势
- 为未来做准备
)" << endl;

    cout << "\n程序结束" << endl;
    return 0;
}
