// ============================================
// 05_if_else_exercise.cpp - if-else练习题
// 日期：2026-02-18（第2天）
// 难度：⭐⭐ 中级
// ============================================

/*
【练习说明】

本文件包含8道if-else练习题，涵盖：
- 基本条件判断
- 多分支选择
- 逻辑运算符组合
- 嵌套if语句

学习目标：
- 熟练使用if-else结构
- 掌握逻辑运算符的使用
- 培养逻辑思维能力

⚠️ 重要提示：
- 每道题都有详细的注释和提示
- 先自己思考，再看提示
- 完成后编译运行，验证结果
- 不提供参考答案，鼓励独立思考
*/

#include <iostream>
#include <string>
#include <cstdlib>
using namespace std;

int main() {
    system("chcp 65001");  // 设置UTF-8编码，支持中文

    cout << string(60, '=') << endl;
    cout << "        if-else 练习题" << endl;
    cout << string(60, '=') << endl;
    cout << endl;

    // ============================================
    // 练习1：奇偶判断
    // ============================================

    cout << "练习1：奇偶判断" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【题目】
输入一个整数，判断是奇数还是偶数。

【要求】
- 使用 cin 输入整数
- 使用 if-else 判断奇偶
- 输出结果："这是奇数" 或 "这是偶数"

【提示】
- 偶数：能被2整除，即 num % 2 == 0
- 奇数：不能被2整除，即 num % 2 != 0
- % 是取模运算符，返回除法的余数

【思考】
- 负数如何判断？（-3是奇数，-4是偶数）
- 0是奇数还是偶数？（0是偶数）
)" << endl;

    // 在这里编写代码
    int num;
    cout << "请输入一个整数：";
    cin >> num;

    // TODO: 使用if-else判断奇偶
    // 提示：if (num % 2 == 0) { ... } else { ... }
    if (num % 2 == 0)
    {
        cout << "这是偶数" << endl;
    }
    else
    {
        cout << "这是奇数" << endl;
    }

    cout << endl;

    // ============================================
    // 练习2：成绩等级判断
    // ============================================

    cout << "练习2：成绩等级判断" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【题目】
输入分数（0-100），输出对应的等级。

【等级标准】
- A：90-100分（优秀）
- B：80-89分（良好）
- C：70-79分（中等）
- D：60-69分（及格）
- F：0-59分（不及格）

【要求】
- 使用 if-else if-else 结构
- 输出格式："等级：X（评语）"
- 处理无效输入（分数<0或>100）

【提示】
- 条件从严格到宽松排列（先判断>=90，再判断>=80...）
- 或者使用范围判断（score >= 90 && score <= 100）
- 先判断是否在有效范围内（0-100）

【思考】
- 如果条件顺序反了会怎样？
- 如何处理小数分数（如85.5）？
)" << endl;

    // 在这里编写代码
    int score;
    cout << "请输入分数（0-100）：";
    cin >> score;

    // TODO: 使用if-else if-else判断等级
    // 提示：先判断是否在0-100范围内
    // 然后从高到低判断等级
    if (score >= 0 && score <= 100)
    {
        if (score >= 90)
        {
            cout << "优秀" << endl;
        }
        else if(score >= 80)
        {
            cout << "良好" << endl;
        }
        else if(score >= 70)
        {
            cout << "中等" << endl;
        }
        else if(score >= 60)
        {
            cout << "及格" << endl;
        }
        else
        {
            cout << "不及格" << endl;
        }
    }
    else
    {
        cout << "输入无效，分数必须在0-100之间" << endl;
    }
    

    cout << endl;

    // ============================================
    // 练习3：闰年判断
    // ============================================

    cout << "练习3：闰年判断" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【题目】
输入年份，判断是否是闰年。

【闰年规则】
1. 能被4整除但不能被100整除的年份是闰年
   例如：2020、2024（能被4整除，不能被100整除）
2. 能被400整除的年份是闰年
   例如：2000、2400（能被400整除）
3. 其他年份不是闰年
   例如：1900、2100（能被100整除但不能被400整除）

【要求】
- 使用逻辑运算符组合条件
- 输出："XXXX年是闰年" 或 "XXXX年不是闰年"

【提示】
- 条件1：(year % 4 == 0 && year % 100 != 0)
- 条件2：(year % 400 == 0)
- 两个条件满足其一即可：条件1 || 条件2

【思考】
- 为什么能被100整除的年份不一定是闰年？
- 2000年是闰年，1900年不是，为什么？
)" << endl;

    // 在这里编写代码
    int year;
    cout << "请输入年份：";
    cin >> year;

    // TODO: 使用逻辑运算符判断闰年
    // 提示：if ((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0))


    cout << endl;

    // ============================================
    // 练习4：三数比较（找最大值）
    // ============================================

    cout << "练习4：三数比较（找最大值）" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【题目】
输入三个整数，输出最大值。

【要求】
- 使用嵌套if或逻辑运算符
- 输出格式："最大值是：X"

【提示方法1：嵌套if】
if (a > b) {
    if (a > c) {
        // a最大
    } else {
        // c最大
    }
} else {
    if (b > c) {
        // b最大
    } else {
        // c最大
    }
}

【提示方法2：逻辑运算符】
if (a >= b && a >= c) {
    // a最大
} else if (b >= a && b >= c) {
    // b最大
} else {
    // c最大
}

【思考】
- 如果三个数相等怎么办？
- 哪种方法更简洁？
- 如何找最小值？
)" << endl;

    // 在这里编写代码
    int a, b, c;
    cout << "请输入三个整数（用空格分隔）：";
    cin >> a >> b >> c;

    // TODO: 找出最大值
    // 提示：可以用嵌套if或逻辑运算符


    cout << endl;

    // ============================================
    // 练习5：登录验证
    // ============================================

    cout << "练习5：登录验证" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【题目】
模拟用户登录，验证用户名和密码。

【要求】
- 预设正确的用户名和密码
  用户名：admin
  密码：123456
- 输入用户名和密码
- 使用 && 组合条件判断
- 输出："登录成功！" 或 "用户名或密码错误！"

【提示】
- 字符串比较用 ==（需要 #include <string>）
- 条件：(username == "admin" && password == "123456")
- 注意：C++中字符串用双引号，字符用单引号

【思考】
- 如何分别提示"用户名错误"和"密码错误"？
- 如何限制登录尝试次数？
)" << endl;

    // 在这里编写代码
    string username, password;
    cout << "请输入用户名：";
    cin >> username;
    cout << "请输入密码：";
    cin >> password;

    // TODO: 验证用户名和密码
    // 提示：if (username == "admin" && password == "123456")


    cout << endl;

    // ============================================
    // 练习6：简易计算器
    // ============================================

    cout << "练习6：简易计算器" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【题目】
实现一个简易计算器，支持加减乘除。

【要求】
- 输入两个数字和一个运算符（+、-、*、/）
- 使用 if-else if-else 判断运算符
- 输出计算结果
- 处理除零错误

【提示】
- 运算符用 char 类型存储
- 判断：if (op == '+') { ... }
- 除法前检查：if (op == '/' && num2 == 0) { ... }

【思考】
- 如何处理无效的运算符？
- 如何支持更多运算（如取模%、幂运算）？
)" << endl;

    // 在这里编写代码
    double num1, num2;
    char op;
    cout << "请输入第一个数字：";
    cin >> num1;
    cout << "请输入运算符（+、-、*、/）：";
    cin >> op;
    cout << "请输入第二个数字：";
    cin >> num2;

    // TODO: 根据运算符计算结果
    // 提示：if (op == '+') { result = num1 + num2; }
    // 注意：除法要检查除零错误


    cout << endl;

    // ============================================
    // 练习7：会员折扣计算
    // ============================================

    cout << "练习7：会员折扣计算" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【题目】
计算购物折扣后的价格。

【折扣规则】
- 会员：打9折
- 非会员：不打折
- 金额>=1000元：额外减50元
- 金额>=500元：额外减20元

【要求】
- 输入金额和是否是会员（1表示是，0表示否）
- 先计算会员折扣，再计算满减
- 输出原价、折扣、最终价格

【提示】
- 先判断会员：if (isMember) { price *= 0.9; }
- 再判断满减：if (price >= 1000) { price -= 50; }
- 注意：满减是在折扣后的价格基础上

【思考】
- 如果先满减再打折，结果会不同吗？
- 如何设计更复杂的折扣规则？
)" << endl;

    // 在这里编写代码
    double price;
    int isMember;
    cout << "请输入购物金额：";
    cin >> price;
    cout << "是否是会员（1是/0否）：";
    cin >> isMember;

    double originalPrice = price;  // 保存原价

    // TODO: 计算折扣后价格
    // 提示：先会员折扣，再满减


    cout << endl;

    // ============================================
    // 练习8：三角形判断
    // ============================================

    cout << "练习8：三角形判断" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【题目】
输入三条边长，判断能否构成三角形，以及三角形类型。

【三角形条件】
- 任意两边之和大于第三边
- 即：a + b > c && a + c > b && b + c > a

【三角形类型】
- 等边三角形：三条边相等（a == b && b == c）
- 等腰三角形：两条边相等（a == b || a == c || b == c）
- 普通三角形：三条边都不相等

【要求】
- 先判断是否能构成三角形
- 如果能，再判断类型
- 输出结果

【提示】
- 先判断三角形条件
- 再用嵌套if判断类型
- 注意：等边三角形也是等腰三角形，要先判断等边

【思考】
- 如何判断直角三角形？（勾股定理：a² + b² = c²）
- 边长可以是小数吗？
)" << endl;

    // 在这里编写代码
    double side1, side2, side3;
    cout << "请输入三条边长（用空格分隔）：";
    cin >> side1 >> side2 >> side3;

    // TODO: 判断是否能构成三角形，以及类型
    // 提示：先判断 side1 + side2 > side3 && ...
    // 再判断类型：等边 -> 等腰 -> 普通


    cout << endl;

    // ============================================
    // 练习完成
    // ============================================

    cout << string(60, '=') << endl;
    cout << "所有练习题已完成！" << endl;
    cout << "编译命令：g++ -o 05_if_else_exercise 05_if_else_exercise.cpp" << endl;
    cout << "运行命令：./05_if_else_exercise 或 05_if_else_exercise.exe" << endl;
    cout << string(60, '=') << endl;

    return 0;
}
