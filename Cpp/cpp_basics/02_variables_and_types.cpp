// ============================================
// 02_variables_and_types.cpp - 变量和数据类型详解
// 日期：2026-02-12（第1天）
// 难度：⭐ 初级
// ============================================

/*
【核心概念】变量和数据类型

是什么？
- 变量是用来存储数据的容器，有名字和类型
- 数据类型定义了变量可以存储什么样的数据

为什么需要？
- 程序需要存储和处理各种数据（数字、文字、真假等）
- 不同类型的数据占用不同的内存空间，有不同的操作方式

核心特点：
- C++是强类型语言，变量必须先声明类型
- 变量使用前必须初始化（赋初值）
- 类型决定了变量能存储的数据范围和操作
*/

// ============================================
// 第一部分：核心概念详解 ⭐⭐⭐⭐⭐
// ============================================

#include <iostream>
#include <string>    // 使用 string 类型需要包含这个头文件
using namespace std;

int main() {
    cout << string(60, '=') << endl;
    cout << "第一部分：核心概念详解" << endl;
    cout << string(60, '=') << endl;

    // --------------------------------------------
    // 1.1 什么是变量？
    // --------------------------------------------

    cout << "\n1.1 什么是变量？" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【变量】

是什么？
- 变量是内存中的一块存储空间，有名字和类型
- 可以把变量想象成一个"盒子"，用来装数据

为什么需要？
- 程序需要记住数据（比如用户输入的年龄）
- 需要对数据进行计算和处理

变量的三要素：
1. 类型（type）：决定能存什么数据，占多少内存
2. 名字（name）：用来引用这个变量
3. 值（value）：变量存储的具体数据

语法：
类型 变量名 = 初始值;

示例：
int age = 20;        // 整数类型，变量名是age，初始值是20
double height = 1.75; // 浮点数类型，变量名是height，初始值是1.75
)" << endl;

    // --------------------------------------------
    // 1.2 基本数据类型详解
    // --------------------------------------------

    cout << "\n1.2 基本数据类型详解" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【C++基本数据类型】

1. 整数类型（存储整数）
   - int：标准整数，通常占4字节，范围约 -21亿 到 +21亿
   - short：短整数，占2字节，范围 -32768 到 32767
   - long：长整数，至少占4字节
   - long long：超长整数，占8字节

2. 浮点数类型（存储小数）
   - float：单精度浮点数，占4字节，精度约6-7位
   - double：双精度浮点数，占8字节，精度约15-16位（推荐）

3. 字符类型（存储单个字符）
   - char：字符类型，占1字节，存储单个字符如 'A', 'b', '1'

4. 布尔类型（存储真假）
   - bool：布尔类型，占1字节，只有两个值：true 或 false

5. 字符串类型（存储文本）
   - string：字符串类型，存储一串字符如 "Hello"
   - 注意：string 不是基本类型，是C++标准库提供的类

如何选择类型？
- 存储整数 → int
- 存储小数 → double
- 存储单个字符 → char
- 存储真假 → bool
- 存储文本 → string
)" << endl;

    // ============================================
    // 第二部分：基础操作实践
    // ============================================

    cout << "\n" << string(60, '=') << endl;
    cout << "第二部分：基础操作实践" << endl;
    cout << string(60, '=') << endl;

    // --------------------------------------------
    // 2.1 整数类型（int）
    // --------------------------------------------

    cout << "\n2.1 整数类型（int）" << endl;
    cout << string(60, '-') << endl;

    // 声明并初始化整数变量
    // 语法：int 变量名 = 初始值;
    int age = 20;           // 年龄
    int score = 95;         // 分数
    int temperature = -5;   // 温度（可以是负数）

    // 输出变量的值
    cout << "年龄：" << age << endl;
    cout << "分数：" << score << endl;
    cout << "温度：" << temperature << "°C" << endl;

    // 变量可以重新赋值
    age = 21;  // 修改age的值
    cout << "一年后的年龄：" << age << endl;

    // 变量可以参与运算
    int sum = age + score;  // 计算总和
    cout << "年龄+分数 = " << sum << endl;

    // --------------------------------------------
    // 2.2 浮点数类型（double）
    // --------------------------------------------

    cout << "\n2.2 浮点数类型（double）" << endl;
    cout << string(60, '-') << endl;

    // 声明并初始化浮点数变量
    // 语法：double 变量名 = 初始值;
    double height = 1.75;      // 身高（米）
    double weight = 65.5;      // 体重（千克）
    double pi = 3.14159;       // 圆周率

    cout << "身高：" << height << " 米" << endl;
    cout << "体重：" << weight << " 千克" << endl;
    cout << "圆周率：" << pi << endl;

    // 计算BMI（体重指数）
    // BMI = 体重(kg) / 身高(m)²
    double bmi = weight / (height * height);
    cout << "BMI指数：" << bmi << endl;

    // float vs double 的区别
    float f = 3.14159265358979;   // float精度较低
    double d = 3.14159265358979;  // double精度较高
    cout << "float:  " << f << endl;   // 输出约6-7位有效数字
    cout << "double: " << d << endl;   // 输出约15-16位有效数字

    // --------------------------------------------
    // 2.3 字符类型（char）
    // --------------------------------------------

    cout << "\n2.3 字符类型（char）" << endl;
    cout << string(60, '-') << endl;

    // 声明并初始化字符变量
    // 语法：char 变量名 = '字符';
    // 注意：字符用单引号 ' ' 包围
    char grade = 'A';          // 成绩等级
    char initial = 'J';        // 姓名首字母
    char symbol = '@';         // 符号

    cout << "成绩等级：" << grade << endl;
    cout << "姓名首字母：" << initial << endl;
    cout << "符号：" << symbol << endl;

    // 字符的ASCII码值
    // 每个字符都对应一个数字（ASCII码）
    cout << "字符 'A' 的ASCII码：" << (int)'A' << endl;  // 65
    cout << "字符 'a' 的ASCII码：" << (int)'a' << endl;  // 97
    cout << "字符 '0' 的ASCII码：" << (int)'0' << endl;  // 48

    // --------------------------------------------
    // 2.4 布尔类型（bool）
    // --------------------------------------------

    cout << "\n2.4 布尔类型（bool）" << endl;
    cout << string(60, '-') << endl;

    // 声明并初始化布尔变量
    // 语法：bool 变量名 = true/false;
    // 只有两个值：true（真）或 false（假）
    bool isStudent = true;      // 是否是学生
    bool hasLicense = false;    // 是否有驾照
    bool isAdult = true;        // 是否成年

    cout << "是否是学生：" << isStudent << endl;      // 输出1（true显示为1）
    cout << "是否有驾照：" << hasLicense << endl;     // 输出0（false显示为0）
    cout << "是否成年：" << isAdult << endl;

    // 使用 boolalpha 让 true/false 显示为文字
    cout << boolalpha;  // 设置布尔值以文字形式输出
    cout << "是否是学生：" << isStudent << endl;      // 输出 true
    cout << "是否有驾照：" << hasLicense << endl;     // 输出 false

    // --------------------------------------------
    // 2.5 字符串类型（string）
    // --------------------------------------------

    cout << "\n2.5 字符串类型（string）" << endl;
    cout << string(60, '-') << endl;

    // 声明并初始化字符串变量
    // 语法：string 变量名 = "字符串内容";
    // 注意：字符串用双引号 " " 包围
    string name = "Zhang San";           // 姓名
    string city = "Beijing";             // 城市
    string greeting = "Hello, World!";   // 问候语

    cout << "姓名：" << name << endl;
    cout << "城市：" << city << endl;
    cout << "问候语：" << greeting << endl;

    // 字符串可以拼接
    string fullGreeting = greeting + " Welcome to " + city;
    cout << "完整问候：" << fullGreeting << endl;

    // 字符串长度
    cout << "姓名长度：" << name.length() << " 个字符" << endl;

    // --------------------------------------------
    // 2.6 变量命名规则
    // --------------------------------------------

    cout << "\n2.6 变量命名规则" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【变量命名规则】

必须遵守的规则（否则编译错误）：
1. 只能包含字母、数字、下划线
2. 不能以数字开头
3. 不能使用C++关键字（如 int, double, if, while 等）
4. 区分大小写（age 和 Age 是不同的变量）

推荐的命名规范：
1. 使用有意义的名字（age 比 a 好）
2. 驼峰命名法：studentAge, firstName
3. 下划线命名法：student_age, first_name
4. 常量用大写：PI, MAX_SIZE

✅ 好的命名：
int studentAge;
double accountBalance;
string userName;

❌ 不好的命名：
int a;           // 太简短，不知道是什么
int 123abc;      // 不能以数字开头
int student-age; // 不能使用连字符
int int;         // 不能使用关键字
)" << endl;

    // 示例：好的变量命名
    int studentAge = 18;              // 驼峰命名法
    double account_balance = 1000.0;  // 下划线命名法
    string user_name = "Alice";       // 下划线命名法

    // --------------------------------------------
    // 2.7 类型转换
    // --------------------------------------------

    cout << "\n2.7 类型转换" << endl;
    cout << string(60, '-') << endl;

    // 隐式类型转换（自动转换）
    int intNum = 10;
    double doubleNum = intNum;  // int 自动转换为 double
    cout << "整数转浮点数：" << intNum << " -> " << doubleNum << endl;

    // 显式类型转换（强制转换）
    double pi2 = 3.14159;
    int piInt = (int)pi2;  // 强制转换为 int，小数部分被截断
    cout << "浮点数转整数：" << pi2 << " -> " << piInt << endl;

    // 整数除法 vs 浮点数除法
    int a = 7, b = 2;
    cout << "整数除法：7 / 2 = " << a / b << endl;           // 结果是3（整数）
    cout << "浮点数除法：7.0 / 2.0 = " << 7.0 / 2.0 << endl; // 结果是3.5

    // 如何让整数除法得到小数结果？
    cout << "转换后的除法：7 / 2 = " << (double)a / b << endl;  // 结果是3.5

    // --------------------------------------------
    // 2.8 常量（const）
    // --------------------------------------------

    cout << "\n2.8 常量（const）" << endl;
    cout << string(60, '-') << endl;

    // 常量：值不能被修改的变量
    // 语法：const 类型 变量名 = 值;
    const double PI = 3.14159;        // 圆周率
    const int MAX_SCORE = 100;        // 最高分
    const string SCHOOL_NAME = "ABC School";  // 学校名称

    cout << "圆周率：" << PI << endl;
    cout << "最高分：" << MAX_SCORE << endl;
    cout << "学校名称：" << SCHOOL_NAME << endl;

    // PI = 3.14;  // 错误！常量不能被修改

    cout << R"(
【常量的作用】
1. 防止意外修改重要的值
2. 提高代码可读性（用名字代替魔法数字）
3. 方便维护（只需修改一处）

推荐：常量名使用全大写字母，单词间用下划线分隔
)" << endl;

    // ============================================
    // 第三部分：深入理解（可选）
    // ============================================

    cout << "\n" << string(60, '=') << endl;
    cout << "🔍 深入理解：内存和数据类型（选学）" << endl;
    cout << string(60, '=') << endl;

    cout << R"(
如果你想知道"为什么"，可以阅读这部分。
如果觉得难，可以跳过，不影响后续学习。

【深入内容】

1. 为什么需要不同的数据类型？
   - 不同类型占用不同的内存空间
   - int 占4字节，double 占8字节，char 占1字节
   - 选择合适的类型可以节省内存，提高效率

2. 变量在内存中是如何存储的？
   - 每个变量都有一个内存地址
   - 变量名是地址的别名，方便我们使用
   - 例如：int age = 20;
     → 在内存地址0x1000处存储了数字20
     → age 是这个地址的名字

3. 整数和浮点数的存储方式不同
   - 整数：直接存储二进制形式
   - 浮点数：使用科学计数法存储（符号位+指数+尾数）
   - 这就是为什么浮点数有精度限制

4. 为什么浮点数不精确？
   - 计算机用二进制存储，有些十进制小数无法精确表示
   - 例如：0.1 在二进制中是无限循环小数
   - 所以：0.1 + 0.2 可能不等于 0.3（有微小误差）

5. sizeof 运算符
   - 可以查看类型或变量占用的字节数
)" << endl;

    // 使用 sizeof 查看各类型的大小
    cout << "int 占用字节数：" << sizeof(int) << endl;
    cout << "double 占用字节数：" << sizeof(double) << endl;
    cout << "char 占用字节数：" << sizeof(char) << endl;
    cout << "bool 占用字节数：" << sizeof(bool) << endl;

    return 0;
}

// ============================================
// 第四部分：常见错误和陷阱 ⭐⭐⭐⭐⭐
// ============================================

/*
常见错误1：使用未初始化的变量
❌ 错误代码：
int age;           // 声明但未初始化
cout << age;       // 使用未初始化的变量

结果：输出随机值（垃圾数据）

原因：C++不会自动初始化局部变量，变量中是内存中的随机数据

✅ 正确代码：
int age = 0;       // 声明时立即初始化
cout << age;       // 输出0

记忆技巧：声明变量时立即初始化，养成好习惯

────────────────────────────────────

常见错误2：整数除法得到意外结果
❌ 错误代码：
int a = 7, b = 2;
double result = a / b;  // 期望得到3.5
cout << result;         // 实际输出3.0

结果：输出3.0而不是3.5

原因：a / b 是整数除法，结果是整数3，然后才转换为double

✅ 正确代码：
int a = 7, b = 2;
double result = (double)a / b;  // 先将a转换为double
cout << result;                  // 输出3.5

记忆技巧：整数除法会截断小数部分，需要至少一个操作数是浮点数

────────────────────────────────────

常见错误3：字符和字符串混淆
❌ 错误代码：
char name = "John";     // 错误：字符变量不能存储字符串
string initial = 'J';   // 错误：字符串变量不应该用单引号

结果：编译错误

原因：char 只能存储单个字符，string 存储字符串

✅ 正确代码：
char initial = 'J';     // 字符用单引号
string name = "John";   // 字符串用双引号

记忆技巧：
- 单引号 ' ' → 单个字符 → char
- 双引号 " " → 字符串 → string

────────────────────────────────────

常见错误4：变量名使用关键字
❌ 错误代码：
int int = 10;       // 错误：int是关键字
double double = 3.14;  // 错误：double是关键字

结果：编译错误，提示语法错误

原因：C++关键字不能用作变量名

✅ 正确代码：
int number = 10;
double value = 3.14;

常见关键字：int, double, char, bool, if, else, while, for, return 等

记忆技巧：使用有意义的英文单词作为变量名，避免使用关键字

────────────────────────────────────

常见错误5：修改常量的值
❌ 错误代码：
const int MAX_SIZE = 100;
MAX_SIZE = 200;  // 错误：不能修改常量

结果：编译错误，提示 assignment of read-only variable

原因：const 声明的常量不能被修改

✅ 正确代码：
const int MAX_SIZE = 100;
// 不要尝试修改MAX_SIZE

记忆技巧：const = constant（常量），常量不可变

────────────────────────────────────

常见错误6：浮点数比较相等
❌ 错误代码：
double a = 0.1 + 0.2;
if (a == 0.3) {  // 可能为false
    cout << "相等";
}

结果：可能不会输出"相等"

原因：浮点数有精度误差，0.1 + 0.2 可能不完全等于 0.3

✅ 正确代码：
double a = 0.1 + 0.2;
double epsilon = 0.00001;  // 误差范围
if (abs(a - 0.3) < epsilon) {  // 判断差值是否很小
    cout << "相等";
}

记忆技巧：不要直接比较浮点数是否相等，应该判断差值是否足够小

────────────────────────────────────

常见错误7：忘记包含 <string> 头文件
❌ 错误代码：
#include <iostream>
// 忘记包含 <string>
using namespace std;
int main() {
    string name = "John";  // 可能编译错误
    return 0;
}

结果：某些编译器会报错，提示 'string' was not declared

原因：string 类型定义在 <string> 头文件中

✅ 正确代码：
#include <iostream>
#include <string>  // 包含string头文件
using namespace std;
int main() {
    string name = "John";
    return 0;
}

记忆技巧：使用 string 类型必须包含 <string> 头文件
*/

// ============================================
// 第五部分：函数卡片速查
// ============================================

/*
【sizeof 运算符】

语法：sizeof(类型) 或 sizeof(变量)
作用：返回类型或变量占用的字节数
返回：size_t 类型（无符号整数）

示例：
cout << sizeof(int) << endl;      // 输出4（通常）
cout << sizeof(double) << endl;   // 输出8
int x = 10;
cout << sizeof(x) << endl;        // 输出4

────────────────────────────────────

【类型转换】

语法：(目标类型)变量 或 目标类型(变量)
作用：将变量转换为指定类型
返回：转换后的值

示例：
double d = 3.14;
int i = (int)d;           // C风格转换
int j = int(d);           // C++风格转换
cout << i << endl;        // 输出3
*/

// ============================================
// 练习题
// ============================================

/*
1. 声明并初始化变量
   要求：声明以下变量并赋予合适的初始值
   - 你的年龄（整数）
   - 你的身高（浮点数，单位：米）
   - 你的姓名首字母（字符）
   - 你是否是学生（布尔值）
   - 你的完整姓名（字符串）
   提示：选择合适的数据类型

2. 计算圆的面积和周长
   要求：
   - 定义常量 PI = 3.14159
   - 定义变量 radius（半径）= 5.0
   - 计算面积（area = PI * radius * radius）
   - 计算周长（circumference = 2 * PI * radius）
   - 输出结果
   提示：使用 const 定义常量

3. 类型转换练习
   要求：
   - 定义 int a = 10, b = 3
   - 计算 a / b 的整数结果
   - 计算 a / b 的浮点数结果（保留小数）
   - 输出两个结果
   提示：使用类型转换

4. 变量交换
   要求：
   - 定义 int x = 5, y = 10
   - 不使用第三个变量，交换 x 和 y 的值
   - 输出交换后的结果
   提示：使用算术运算（x = x + y; y = x - y; x = x - y;）

5. 温度转换
   要求：
   - 定义摄氏温度 celsius = 25.0
   - 转换为华氏温度（fahrenheit = celsius * 9.0 / 5.0 + 32.0）
   - 输出结果
   提示：注意使用浮点数运算

6. 字符ASCII码
   要求：
   - 定义字符变量 ch = 'A'
   - 输出该字符
   - 输出该字符的ASCII码值
   - 输出下一个字符（'B'）
   提示：使用 (int)ch 获取ASCII码，ch + 1 获取下一个字符

7. 变量命名练习
   要求：判断以下变量名是否合法，如果不合法说明原因
   - student_age
   - 2ndPlace
   - my-name
   - _count
   - int
   - MAX_SIZE
   提示：回顾变量命名规则
*/

// ============================================
// 编译和运行
// ============================================

/*
【编译命令】
g++ -Wall -std=c++11 -o variables 02_variables_and_types.cpp

【运行命令】
Windows: variables.exe
Linux/Mac: ./variables
*/
