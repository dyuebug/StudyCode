// ============================================
// 05_if_else.cpp - 条件判断详解
// 日期：2026-02-18（第2天）
// 难度：⭐⭐ 中级
// ============================================

/*
【核心概念】条件判断（if-else）

是什么？
- 根据条件的真假（true/false）来决定执行哪段代码
- 程序的"决策机制"，让程序能够根据不同情况做出不同反应

为什么需要？
- 现实中充满选择：如果下雨就带伞，否则不带
- 程序需要根据不同情况执行不同逻辑
- 是编程中最基础、最重要的控制结构之一

核心特点：
- 条件表达式结果必须是bool类型（true或false）
- 可以嵌套使用，处理复杂逻辑
- 配合逻辑运算符（&&、||、!）实现复杂条件
*/

// ============================================
// 第一部分：核心概念详解 ⭐⭐⭐⭐⭐
// ============================================

#include <iostream>
#include <string>
#include <cstdlib>
using namespace std;

int main() {
    system("chcp 65001");  // 设置UTF-8编码，支持中文

    cout << string(60, '=') << endl;
    cout << "第一部分：核心概念详解 ⭐⭐⭐⭐⭐" << endl;
    cout << string(60, '=') << endl;
    cout << endl;

    // --------------------------------------------
    // 1.1 概念1：比较运算符
    // --------------------------------------------

    cout << "1.1 比较运算符" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【比较运算符】

是什么？
- 用于比较两个值的大小或相等关系
- 返回bool类型结果（true或false）

六大比较运算符：
1. ==  等于（注意：不是赋值的=）
2. !=  不等于
3. >   大于
4. <   小于
5. >=  大于或等于
6. <=  小于或等于

为什么需要？
- 条件判断的基础，用于构建判断条件
- 例如：if (age >= 18) 判断是否成年

⚠️ 常见错误：
- 把 == 写成 =（赋值而非比较）
- 浮点数不要用 == 比较（精度问题）
)" << endl;

    // 示例：比较运算符的使用
    int a = 10;
    int b = 20;

    cout << "a = " << a << ", b = " << b << endl;
    cout << "a == b: " << (a == b) << " (0表示false)" << endl;  // false，输出0
    cout << "a != b: " << (a != b) << " (1表示true)" << endl;   // true，输出1
    cout << "a > b: " << (a > b) << endl;    // false
    cout << "a < b: " << (a < b) << endl;    // true
    cout << "a >= b: " << (a >= b) << endl;  // false
    cout << "a <= b: " << (a <= b) << endl;  // false
    cout << endl;

    // --------------------------------------------
    // 1.2 概念2：逻辑运算符
    // --------------------------------------------

    cout << "1.2 逻辑运算符" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【逻辑运算符】

是什么？
- 用于组合多个条件，构建复杂的判断逻辑
- 返回bool类型结果

三大逻辑运算符：
1. &&  逻辑与（AND）- 所有条件都为true时，结果才为true
2. ||  逻辑或（OR）- 只要有一个条件为true，结果就为true
3. !   逻辑非（NOT）- 取反，true变false，false变true

真值表：
A      B      A && B    A || B    !A
true   true   true      true      false
true   false  false     true      false
false  true   false     true      true
false  false  false     false     true

⚠️ 短路特性（重要！）：
- && 运算：如果左边为false，右边不再计算
- || 运算：如果左边为true，右边不再计算
- 原因：已经能确定最终结果，无需继续计算

优先级（从高到低）：
1. !（逻辑非）
2. &&（逻辑与）
3. ||（逻辑或）
4. 建议：复杂表达式用括号明确优先级
)" << endl;

    // 示例：逻辑运算符的使用
    bool isStudent = true;
    int age = 20;

    cout << "isStudent = " << isStudent << ", age = " << age << endl;
    cout << "isStudent && (age >= 18): " << (isStudent && (age >= 18)) << endl;  // true
    cout << "isStudent || (age < 18): " << (isStudent || (age < 18)) << endl;    // true
    cout << "!isStudent: " << (!isStudent) << endl;  // false
    cout << endl;

    // --------------------------------------------
    // 1.3 概念3：if语句的基本形式
    // --------------------------------------------

    cout << "1.3 if语句的基本形式" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【if语句】

完整语法：
if (条件表达式) {
    // 条件为true时执行的代码
}

执行流程：
1. 计算条件表达式的值（结果必须是bool类型）
2. 如果为true，执行大括号内的代码
3. 如果为false，跳过大括号内的代码

⚠️ 注意事项：
- 条件表达式必须用小括号()包围
- 执行代码必须用大括号{}包围（即使只有一行，也建议加上）
- 条件表达式后面不要加分号
)" << endl;

    cout << endl;

    // ============================================
    // 第二部分：基础操作实践
    // ============================================

    cout << string(60, '=') << endl;
    cout << "第二部分：基础操作实践" << endl;
    cout << string(60, '=') << endl;
    cout << endl;

    // --------------------------------------------
    // 2.1 示例1：单个if语句
    // --------------------------------------------

    cout << "2.1 示例1：单个if语句" << endl;
    cout << string(60, '-') << endl;

    // 声明变量：int类型，存储年龄
    int userAge = 20;

    cout << "你的年龄：" << userAge << endl;

    // if语句：判断是否成年
    // 条件：userAge >= 18（年龄大于或等于18）
    // 如果条件为true，执行大括号内的代码
    if (userAge >= 18) {
        cout << "你已经成年了！" << endl;
    }

    cout << endl;

    // --------------------------------------------
    // 2.2 示例2：if-else语句
    // --------------------------------------------

    cout << "2.2 示例2：if-else语句" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【if-else语句】

完整语法：
if (条件表达式) {
    // 条件为true时执行
} else {
    // 条件为false时执行
}

执行流程：
- 如果条件为true，执行if块，跳过else块
- 如果条件为false，跳过if块，执行else块
- 两个块只会执行其中一个，不会都执行
)" << endl;

    int score = 75;
    cout << "你的分数：" << score << endl;

    // if-else语句：判断是否及格
    // 条件：score >= 60（分数大于或等于60）
    if (score >= 60) {
        // 条件为true时执行
        cout << "恭喜你，及格了！" << endl;
    } else {
        // 条件为false时执行
        cout << "很遗憾，不及格。" << endl;
    }

    cout << endl;

    // --------------------------------------------
    // 2.3 示例3：if-else if-else语句（多分支）
    // --------------------------------------------

    cout << "2.3 示例3：if-else if-else语句（多分支）" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【if-else if-else语句】

完整语法：
if (条件1) {
    // 条件1为true时执行
} else if (条件2) {
    // 条件1为false且条件2为true时执行
} else if (条件3) {
    // 条件1、2都为false且条件3为true时执行
} else {
    // 所有条件都为false时执行
}

执行流程：
- 从上到下依次判断条件
- 遇到第一个为true的条件，执行对应代码块，然后跳出整个if结构
- 如果所有条件都为false，执行else块（如果有）
- 只会执行其中一个代码块

⚠️ 注意：
- else if可以有多个
- else是可选的，可以省略
- 条件判断有顺序，先判断的条件优先级更高
)" << endl;

    int examScore = 85;
    cout << "你的考试分数：" << examScore << endl;

    // if-else if-else语句：判断成绩等级
    // 从高到低依次判断
    if (examScore >= 90) {
        // 条件1：分数 >= 90
        cout << "等级：A（优秀）" << endl;
    } else if (examScore >= 80) {
        // 条件2：分数 >= 80（且 < 90，因为前面已经判断过了）
        cout << "等级：B（良好）" << endl;
    } else if (examScore >= 70) {
        // 条件3：分数 >= 70（且 < 80）
        cout << "等级：C（中等）" << endl;
    } else if (examScore >= 60) {
        // 条件4：分数 >= 60（且 < 70）
        cout << "等级：D（及格）" << endl;
    } else {
        // 所有条件都不满足：分数 < 60
        cout << "等级：F（不及格）" << endl;
    }

    cout << endl;

    // --------------------------------------------
    // 2.4 示例4：嵌套if语句
    // --------------------------------------------

    cout << "2.4 示例4：嵌套if语句" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【嵌套if语句】

是什么？
- 在if语句的代码块内，再写一个if语句
- 用于处理多层条件判断

语法：
if (外层条件) {
    if (内层条件) {
        // 外层和内层条件都为true时执行
    }
}

使用场景：
- 需要先满足一个条件，再判断另一个条件
- 例如：先判断是否登录，再判断是否有权限
)" << endl;

    int studentAge = 20;
    bool hasTicket = true;

    cout << "年龄：" << studentAge << "，是否有票：" << (hasTicket ? "是" : "否") << endl;

    // 外层if：判断年龄是否满足
    if (studentAge >= 18) {
        cout << "年龄符合要求（>=18岁）" << endl;

        // 内层if：判断是否有票
        // 只有外层条件为true时，才会执行到这里
        if (hasTicket) {
            cout << "有票，可以入场！" << endl;
        } else {
            cout << "没有票，不能入场。" << endl;
        }
    } else {
        cout << "年龄不符合要求（<18岁），不能入场。" << endl;
    }

    cout << endl;

    // --------------------------------------------
    // 2.5 示例5：使用逻辑运算符组合条件
    // --------------------------------------------

    cout << "2.5 示例5：使用逻辑运算符组合条件" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【逻辑运算符组合条件】

为什么需要？
- 避免嵌套if，代码更简洁
- 多个条件可以用 && 或 || 组合

对比：
嵌套if：
if (age >= 18) {
    if (hasTicket) {
        // 可以入场
    }
}

逻辑运算符：
if (age >= 18 && hasTicket) {
    // 可以入场
}

推荐：简单条件用逻辑运算符，复杂逻辑用嵌套if
)" << endl;

    int memberAge = 25;
    bool isMember = true;

    cout << "年龄：" << memberAge << "，是否会员：" << (isMember ? "是" : "否") << endl;

    // 使用 && 组合条件：两个条件都必须为true
    // 条件1：memberAge >= 18（年龄>=18）
    // 条件2：isMember（是会员）
    if (memberAge >= 18 && isMember) {
        cout << "年龄符合且是会员，享受会员折扣！" << endl;
    } else {
        cout << "不满足会员折扣条件。" << endl;
    }

    cout << endl;

    // 使用 || 组合条件：只要有一个条件为true即可
    int vipAge = 15;
    bool isVip = true;

    cout << "年龄：" << vipAge << "，是否VIP：" << (isVip ? "是" : "否") << endl;

    // 条件1：vipAge >= 18（年龄>=18）
    // 条件2：isVip（是VIP）
    // 只要满足其中一个条件，就可以进入
    if (vipAge >= 18 || isVip) {
        cout << "可以进入VIP区域！" << endl;
    } else {
        cout << "不能进入VIP区域。" << endl;
    }

    cout << endl;

    // --------------------------------------------
    // 2.6 示例6：三元运算符（条件运算符）
    // --------------------------------------------

    cout << "2.6 示例6：三元运算符（条件运算符）" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【三元运算符】

完整语法：
条件表达式 ? 值1 : 值2

执行流程：
- 如果条件为true，返回值1
- 如果条件为false，返回值2

使用场景：
- 简单的二选一赋值
- 代替简单的if-else语句

对比：
if-else：
string result;
if (score >= 60) {
    result = "及格";
} else {
    result = "不及格";
}

三元运算符：
string result = (score >= 60) ? "及格" : "不及格";

⚠️ 注意：
- 只适合简单的二选一场景
- 复杂逻辑不要用三元运算符，会降低可读性
)" << endl;

    int testScore = 88;
    cout << "测试分数：" << testScore << endl;

    // 三元运算符：根据分数判断是否及格
    // 条件：testScore >= 60
    // 如果为true，返回"及格"；如果为false，返回"不及格"
    string result = (testScore >= 60) ? "及格" : "不及格";
    cout << "结果：" << result << endl;

    cout << endl;

    // 三元运算符可以嵌套（不推荐，可读性差）
    int finalScore = 75;
    cout << "期末分数：" << finalScore << endl;

    // 嵌套三元运算符：判断等级
    // 不推荐这样写，仅作演示
    string grade = (finalScore >= 90) ? "A" :
                   (finalScore >= 80) ? "B" :
                   (finalScore >= 70) ? "C" :
                   (finalScore >= 60) ? "D" : "F";
    cout << "等级：" << grade << endl;
    cout << "（注意：嵌套三元运算符可读性差，建议用if-else if-else）" << endl;

    cout << endl;

    // ============================================
    // 第三部分：深入理解（可选）
    // ============================================

    cout << string(60, '=') << endl;
    cout << "🔍 深入理解：条件判断（选学）" << endl;
    cout << string(60, '=') << endl;
    cout << "如果你想知道\"为什么\"，可以阅读这部分。" << endl;
    cout << "如果觉得难，可以跳过，不影响后续学习。" << endl;
    cout << string(60, '=') << endl;
    cout << endl;

    cout << R"(
【深入1：短路特性的实际应用】

为什么需要短路特性？
1. 提高性能：避免不必要的计算
2. 防止错误：避免访问无效数据

示例1：防止除零错误
if (b != 0 && a / b > 10) {
    // 先判断b != 0，如果为false，不会执行a / b
    // 避免了除零错误
}

示例2：防止空指针访问
if (ptr != nullptr && ptr->value > 0) {
    // 先判断ptr不为空，再访问ptr->value
    // 避免了空指针访问错误
}

⚠️ 注意顺序：
- 把可能导致错误的条件放在后面
- 把简单的条件放在前面（性能优化）

────────────────────────────────────

【深入2：bool类型的隐式转换】

C++中的隐式转换规则：
- 数字 → bool：0为false，非0为true
- 指针 → bool：nullptr为false，非空指针为true

示例：
int x = 5;
if (x) {  // 等价于 if (x != 0)
    cout << "x不为0" << endl;
}

⚠️ 不推荐：
- 虽然可以这样写，但不如显式比较清晰
- 推荐写法：if (x != 0) 或 if (x > 0)

────────────────────────────────────

【深入3：条件判断的编译优化】

编译器优化技巧：
1. 常量折叠：编译时计算常量表达式
   if (2 + 3 > 4) { }  // 编译时直接变成 if (true) { }

2. 死代码消除：删除永远不会执行的代码
   if (false) {
       // 这段代码会被编译器删除
   }

3. 分支预测：CPU会预测哪个分支更可能执行
   - 把更常见的情况放在if块
   - 把罕见的情况放在else块

────────────────────────────────────

【深入4：if语句的汇编实现】

底层原理：
- if语句会被编译成条件跳转指令（jmp）
- CPU通过比较指令（cmp）设置标志位
- 根据标志位决定是否跳转

伪汇编示例：
if (a > b) {
    x = 1;
} else {
    x = 2;
}

编译后（简化）：
cmp  a, b       ; 比较a和b
jle  else_label ; 如果a <= b，跳转到else
mov  x, 1       ; a > b，执行if块
jmp  end_label  ; 跳过else块
else_label:
mov  x, 2       ; 执行else块
end_label:

理解意义：
- if-else不是"免费"的，有性能开销
- 但现代CPU的分支预测很强，通常不用担心
)" << endl;

    cout << endl;

    // ============================================
    // 第四部分：常见错误和陷阱 ⭐⭐⭐⭐⭐
    // ============================================

    cout << string(60, '=') << endl;
    cout << "常见错误和陷阱 ⭐⭐⭐⭐⭐" << endl;
    cout << string(60, '=') << endl;
    cout << endl;

    cout << R"(
────────────────────────────────────
❌ 错误1：把 == 写成 =（赋值而非比较）
────────────────────────────────────

错误代码：
int x = 5;
if (x = 10) {  // 错误！这是赋值，不是比较
    cout << "x等于10" << endl;
}

结果：
- x被赋值为10
- 条件表达式的值是10（非0），为true
- 总是执行if块，无论x原来是什么值

原因：
- = 是赋值运算符
- == 是比较运算符
- C++允许在if中使用赋值表达式（历史遗留问题）

✅ 正确代码：
int x = 5;
if (x == 10) {  // 正确！这是比较
    cout << "x等于10" << endl;
}

记忆技巧：
- 比较用两个等号 ==（"相等"需要两个等号）
- 赋值用一个等号 =
- 现代编译器会警告这种错误，开启警告选项（-Wall）

────────────────────────────────────
❌ 错误2：浮点数用 == 比较
────────────────────────────────────

错误代码：
double a = 0.1 + 0.2;
if (a == 0.3) {  // 错误！浮点数精度问题
    cout << "a等于0.3" << endl;
} else {
    cout << "a不等于0.3" << endl;  // 实际会执行这里
}

结果：
- 输出"a不等于0.3"
- 因为0.1 + 0.2在计算机中不精确等于0.3

原因：
- 浮点数在计算机中是二进制表示
- 很多十进制小数无法精确表示为二进制
- 0.1 + 0.2 = 0.30000000000000004（近似值）

✅ 正确代码：
double a = 0.1 + 0.2;
double epsilon = 1e-9;  // 误差范围
if (abs(a - 0.3) < epsilon) {  // 判断差值是否小于误差范围
    cout << "a约等于0.3" << endl;
}

记忆技巧：
- 浮点数永远不要用 == 或 != 比较
- 用差值的绝对值 < 误差范围来判断
- 需要 #include <cmath> 使用abs()函数

────────────────────────────────────
❌ 错误3：if后面加分号
────────────────────────────────────

错误代码：
int x = 5;
if (x > 0);  // 错误！多了一个分号
{
    cout << "x大于0" << endl;
}

结果：
- 无论x是什么值，都会输出"x大于0"
- 因为分号表示空语句，if判断的是空语句
- 大括号内的代码不属于if，总是执行

原因：
- if (x > 0); 等价于 if (x > 0) { }（空语句）
- 后面的大括号是独立的代码块，不属于if

✅ 正确代码：
int x = 5;
if (x > 0) {  // 正确！没有分号
    cout << "x大于0" << endl;
}

记忆技巧：
- if、else、while、for后面不要加分号
- 只有语句结束才加分号
- 使用IDE的自动格式化功能可以避免这个错误

────────────────────────────────────
❌ 错误4：忘记大括号导致逻辑错误
────────────────────────────────────

错误代码：
int x = 5;
if (x > 0)
    cout << "x大于0" << endl;
    cout << "这是第二行" << endl;  // 错误！不属于if

结果：
- 无论x是什么值，都会输出"这是第二行"
- 因为没有大括号，if只控制第一行

原因：
- 没有大括号时，if只控制紧跟的一条语句
- 第二条语句不属于if，总是执行

✅ 正确代码：
int x = 5;
if (x > 0) {  // 加上大括号
    cout << "x大于0" << endl;
    cout << "这是第二行" << endl;
}

记忆技巧：
- 即使只有一条语句，也建议加大括号
- 提高代码可读性，避免后续修改时出错
- 养成良好的编码习惯

────────────────────────────────────
❌ 错误5：逻辑运算符优先级错误
────────────────────────────────────

错误代码：
int a = 5, b = 10, c = 15;
if (a < b && b < c || c < a) {  // 优先级不明确
    cout << "条件成立" << endl;
}

问题：
- && 和 || 的优先级不同（&& 高于 ||）
- 实际执行：(a < b && b < c) || (c < a)
- 但可读性差，容易误解

✅ 正确代码：
int a = 5, b = 10, c = 15;
if ((a < b && b < c) || (c < a)) {  // 用括号明确优先级
    cout << "条件成立" << endl;
}

记忆技巧：
- 复杂表达式一定要用括号
- 不要依赖运算符优先级
- 提高代码可读性

────────────────────────────────────
❌ 错误6：条件判断顺序错误
────────────────────────────────────

错误代码：
int score = 85;
if (score >= 60) {
    cout << "及格" << endl;
} else if (score >= 80) {  // 错误！永远不会执行
    cout << "良好" << endl;
} else if (score >= 90) {  // 错误！永远不会执行
    cout << "优秀" << endl;
}

结果：
- score=85时，输出"及格"
- 因为第一个条件 score >= 60 就满足了
- 后面的条件不会再判断

原因：
- if-else if-else是顺序判断
- 遇到第一个为true的条件就停止
- 条件应该从严格到宽松排列

✅ 正确代码：
int score = 85;
if (score >= 90) {  // 最严格的条件放前面
    cout << "优秀" << endl;
} else if (score >= 80) {
    cout << "良好" << endl;
} else if (score >= 60) {
    cout << "及格" << endl;
} else {
    cout << "不及格" << endl;
}

记忆技巧：
- 条件从严格到宽松排列
- 或者用互斥条件（score >= 90 && score < 100）
- 画流程图帮助理解逻辑

────────────────────────────────────
❌ 错误7：悬空else（dangling else）
────────────────────────────────────

错误代码：
int x = 5, y = 10;
if (x > 0)
    if (y > 0)
        cout << "x和y都大于0" << endl;
else  // 错误！这个else属于哪个if？
    cout << "x不大于0" << endl;

问题：
- else属于最近的if（即 if (y > 0)）
- 实际逻辑：如果x>0且y<=0，输出"x不大于0"（错误）
- 与预期不符

原因：
- else总是与最近的if配对
- 没有大括号时容易产生歧义

✅ 正确代码：
int x = 5, y = 10;
if (x > 0) {
    if (y > 0) {
        cout << "x和y都大于0" << endl;
    }
} else {
    cout << "x不大于0" << endl;
}

记忆技巧：
- 嵌套if一定要用大括号
- 明确每个else的归属
- 使用缩进和格式化工具
)" << endl;

    cout << endl;

    // ============================================
    // 第五部分：函数卡片速查
    // ============================================

    cout << string(60, '=') << endl;
    cout << "第五部分：函数卡片速查" << endl;
    cout << string(60, '=') << endl;
    cout << endl;

    cout << R"(
【运算符1：比较运算符】

语法：
a == b  // 等于
a != b  // 不等于
a > b   // 大于
a < b   // 小于
a >= b  // 大于或等于
a <= b  // 小于或等于

参数：
  - a, b: 任意可比较的类型（int、double、char等）

返回：bool类型（true或false）

示例：
int x = 10, y = 20;
bool result = (x < y);  // true

⚠️ 注意：
- 浮点数不要用 == 或 != 比较
- 不要把 == 写成 =

────────────────────────────────────

【运算符2：逻辑运算符】

语法：
a && b  // 逻辑与（AND）
a || b  // 逻辑或（OR）
!a      // 逻辑非（NOT）

参数：
  - a, b: bool类型或可转换为bool的类型

返回：bool类型（true或false）

示例：
bool isStudent = true;
int age = 20;
bool canEnter = (age >= 18 && isStudent);  // true

⚠️ 短路特性：
- && 左边为false时，右边不计算
- || 左边为true时，右边不计算

────────────────────────────────────

【运算符3：三元运算符】

语法：
条件 ? 值1 : 值2

参数：
  - 条件: bool类型表达式
  - 值1: 条件为true时返回的值
  - 值2: 条件为false时返回的值

返回：值1或值2（类型必须相同或可转换）

示例：
int score = 75;
string result = (score >= 60) ? "及格" : "不及格";

⚠️ 注意：
- 只适合简单的二选一场景
- 复杂逻辑用if-else，不要嵌套三元运算符
)" << endl;

    cout << endl;

    // ============================================
    // 第六部分：练习题
    // ============================================

    cout << string(60, '=') << endl;
    cout << "第六部分：练习题" << endl;
    cout << string(60, '=') << endl;
    cout << endl;

    cout << R"(
请在 05_if_else_exercise.cpp 中完成以下练习：

1. 奇偶判断
   要求：输入一个整数，判断是奇数还是偶数
   提示：使用取模运算符 % 判断是否能被2整除

2. 成绩等级判断
   要求：输入分数（0-100），输出等级（A/B/C/D/F）
   提示：使用if-else if-else，注意条件顺序

3. 闰年判断
   要求：输入年份，判断是否是闰年
   提示：闰年规则：
         - 能被4整除但不能被100整除
         - 或者能被400整除

4. 三数比较
   要求：输入三个整数，输出最大值
   提示：使用嵌套if或逻辑运算符

5. 登录验证
   要求：输入用户名和密码，判断是否正确
   提示：使用 && 组合条件

6. 简易计算器（预览）
   要求：输入两个数和运算符（+、-、*、/），输出结果
   提示：使用if-else if-else判断运算符

7. 会员折扣计算
   要求：输入金额和是否是会员，计算折扣后价格
   提示：会员打9折，非会员不打折

8. 三角形判断
   要求：输入三条边长，判断能否构成三角形
   提示：三角形条件：任意两边之和大于第三边
)" << endl;

    cout << endl;
    cout << string(60, '=') << endl;
    cout << "05_if_else.cpp 学习完成！" << endl;
    cout << "编译命令：g++ -o 05_if_else 05_if_else.cpp" << endl;
    cout << "运行命令：./05_if_else 或 05_if_else.exe" << endl;
    cout << string(60, '=') << endl;

    return 0;
}
