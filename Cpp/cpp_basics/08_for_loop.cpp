// ============================================
// for 循环 - 超详细注释版
// 日期：2026-02-26（第2天）
// 难度：⭐⭐ 中级
// 编译：g++ -o 08_for_loop 08_for_loop.cpp
// 运行：./08_for_loop 或 08_for_loop.exe
// C++版本：C++11 及以上
// ============================================

/*
【核心概念】for 循环

是什么？
- for 循环是一种"计数型"循环结构
- 它让一段代码重复执行指定的次数

为什么需要？
- 想象你要打印 1 到 100 的数字
- 不用循环：你需要写 100 行 cout 语句
- 用 for 循环：只需要 3 行代码
- 循环的本质就是：让计算机帮你做重复的事

核心特点：
- 三要素集中在一行：初始化、条件、更新
- 适合"已知循环次数"的场景
- C++ 中最常用的循环结构
*/

#include <iostream>
using namespace std;

int main() {

    cout << "============================================" << endl;
    cout << "  第一部分：核心概念详解 ⭐⭐⭐⭐⭐" << endl;
    cout << "============================================" << endl;

    // --------------------------------------------
    // 1.1 for 循环是什么？
    // --------------------------------------------

    cout << endl;
    cout << "1.1 for 循环是什么？" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【for 循环】

想象一个场景：体育课上，老师说"绕操场跑5圈"

你的大脑会这样处理：
  1. 记住：我现在跑了 0 圈（初始化）
  2. 检查：跑够 5 圈了吗？（条件判断）
  3. 没够 → 跑一圈（执行循环体）
  4. 跑完一圈 → 圈数 +1（更新计数）
  5. 回到第 2 步，继续检查

这就是 for 循环的思维模型！

)" << endl;

    // --------------------------------------------
    // 1.2 for 循环语法详解 ⭐⭐⭐⭐⭐
    // --------------------------------------------

    cout << "1.2 for 循环语法详解" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【语法卡片】for 循环

完整语法：
┌─────────────────────────────────────────────┐
│  for (初始化; 条件判断; 更新) {              │
│      // 循环体：要重复执行的代码             │
│  }                                           │
└─────────────────────────────────────────────┘

三要素详解：

1. 初始化（只执行一次）
   - 作用：设置循环变量的起始值
   - 示例：int i = 0  → 从 0 开始计数
   - 示例：int i = 1  → 从 1 开始计数
   - 注意：只在循环开始前执行一次！

2. 条件判断（每次循环前检查）
   - 作用：决定是否继续循环
   - 示例：i < 5    → 当 i 小于 5 时继续
   - 示例：i <= 10  → 当 i 小于等于 10 时继续
   - 注意：条件为 true 继续，false 停止

3. 更新（每次循环体执行完后执行）
   - 作用：改变循环变量的值
   - 示例：i++    → 每次加 1
   - 示例：i += 2 → 每次加 2
   - 示例：i--    → 每次减 1
   - 注意：如果忘记更新，会导致死循环！

)" << endl;

    // --------------------------------------------
    // 1.3 执行流程图解 ⭐⭐⭐⭐⭐
    // --------------------------------------------

    cout << "1.3 执行流程图解" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
【for 循环执行流程】

以 for (int i = 0; i < 3; i++) 为例：

步骤1: 初始化 → i = 0
步骤2: 条件判断 → 0 < 3 ? → true ✅
步骤3: 执行循环体（打印 i 的值：0）
步骤4: 更新 → i++ → i 变成 1
步骤5: 条件判断 → 1 < 3 ? → true ✅
步骤6: 执行循环体（打印 i 的值：1）
步骤7: 更新 → i++ → i 变成 2
步骤8: 条件判断 → 2 < 3 ? → true ✅
步骤9: 执行循环体（打印 i 的值：2）
步骤10: 更新 → i++ → i 变成 3
步骤11: 条件判断 → 3 < 3 ? → false ❌
步骤12: 循环结束！

总结：循环体执行了 3 次（i = 0, 1, 2）

关键规律：
- for (int i = 0; i < n; i++) → 循环 n 次
- i 的值从 0 到 n-1

)" << endl;

    cout << endl;
    cout << "============================================" << endl;
    cout << "  第二部分：基础操作实践" << endl;
    cout << "============================================" << endl;

    // --------------------------------------------
    // 2.1 最简单的 for 循环
    // --------------------------------------------

    cout << endl;
    cout << "2.1 最简单的 for 循环" << endl;
    cout << string(60, '-') << endl;

    // for 循环三要素：
    //   int i = 0  → 初始化：从 0 开始
    //   i < 5      → 条件：i 小于 5 时继续
    //   i++        → 更新：每次 i 加 1
    for (int i = 0; i < 5; i++) {
        // 循环体：每次打印 i 的当前值
        // 第1次：i=0，第2次：i=1，...，第5次：i=4
        cout << "  第 " << i + 1 << " 次循环，i = " << i << endl;
    }
    // 循环结束后，i 已经不存在了（因为 i 是在 for 内部声明的）

    // --------------------------------------------
    // 2.2 从 1 开始计数
    // --------------------------------------------

    cout << endl;
    cout << "2.2 从 1 开始计数（更符合人类习惯）" << endl;
    cout << string(60, '-') << endl;

    // 从 1 开始，到 5 结束（包含 5）
    // 注意：条件是 i <= 5，不是 i < 5
    for (int i = 1; i <= 5; i++) {
        cout << "  数字：" << i << endl;
    }
    // 循环了 5 次：i = 1, 2, 3, 4, 5

    // --------------------------------------------
    // 2.3 倒序循环
    // --------------------------------------------

    cout << endl;
    cout << "2.3 倒序循环（从大到小）" << endl;
    cout << string(60, '-') << endl;

    // 从 5 开始，每次减 1，到 1 结束
    // i-- 表示每次 i 减 1
    for (int i = 5; i >= 1; i--) {
        cout << "  倒计时：" << i << endl;
    }
    cout << "  发射！🚀" << endl;

    // --------------------------------------------
    // 2.4 步长不为 1 的循环
    // --------------------------------------------

    cout << endl;
    cout << "2.4 步长不为 1 的循环" << endl;
    cout << string(60, '-') << endl;

    // 打印 0 到 20 之间的偶数
    // i += 2 表示每次 i 加 2（步长为 2）
    cout << "  偶数：";
    for (int i = 0; i <= 20; i += 2) {
        cout << i << " ";
    }
    cout << endl;

    // 打印 1 到 20 之间的奇数
    // 从 1 开始，每次加 2
    cout << "  奇数：";
    for (int i = 1; i <= 20; i += 2) {
        cout << i << " ";
    }
    cout << endl;

    // --------------------------------------------
    // 2.5 累加求和
    // --------------------------------------------

    cout << endl;
    cout << "2.5 累加求和（1 + 2 + 3 + ... + 100）" << endl;
    cout << string(60, '-') << endl;

    // sum 用来存储累加的结果，初始值为 0
    int sum = 0;

    // 从 1 加到 100
    for (int i = 1; i <= 100; i++) {
        // 每次循环，把当前的 i 加到 sum 上
        // 第1次：sum = 0 + 1 = 1
        // 第2次：sum = 1 + 2 = 3
        // 第3次：sum = 3 + 3 = 6
        // ...以此类推
        sum += i;  // 等价于 sum = sum + i
    }

    cout << "  1 + 2 + 3 + ... + 100 = " << sum << endl;
    // 结果应该是 5050（高斯公式：100 * 101 / 2 = 5050）

    // --------------------------------------------
    // 2.6 求阶乘
    // --------------------------------------------

    cout << endl;
    cout << "2.6 求阶乘（5! = 5 × 4 × 3 × 2 × 1）" << endl;
    cout << string(60, '-') << endl;

    int n = 5;
    // factorial 存储阶乘结果，初始值为 1（不能是 0，因为任何数乘 0 都是 0）
    int factorial = 1;

    for (int i = 1; i <= n; i++) {
        // 每次循环，把当前的 i 乘到 factorial 上
        // 第1次：factorial = 1 * 1 = 1
        // 第2次：factorial = 1 * 2 = 2
        // 第3次：factorial = 2 * 3 = 6
        // 第4次：factorial = 6 * 4 = 24
        // 第5次：factorial = 24 * 5 = 120
        factorial *= i;  // 等价于 factorial = factorial * i
    }

    cout << "  " << n << "! = " << factorial << endl;

    // --------------------------------------------
    // 2.7 打印简单图案
    // --------------------------------------------

    cout << endl;
    cout << "2.7 打印简单图案（嵌套循环预览）" << endl;
    cout << string(60, '-') << endl;

    // 打印一个 5x5 的星号方阵
    // 外层循环控制行数（共 5 行）
    for (int row = 0; row < 5; row++) {
        // 内层循环控制每行的列数（每行 5 个星号）
        for (int col = 0; col < 5; col++) {
            cout << "* ";
        }
        // 每行打印完后换行
        cout << endl;
    }

    cout << endl;

    // 打印直角三角形
    cout << "  直角三角形：" << endl;
    // 外层循环：控制行数（共 5 行）
    for (int row = 1; row <= 5; row++) {
        cout << "  ";
        // 内层循环：第 row 行打印 row 个星号
        // 第1行：1个，第2行：2个，...，第5行：5个
        for (int col = 0; col < row; col++) {
            cout << "* ";
        }
        cout << endl;
    }

    cout << endl;
    cout << "============================================" << endl;
    cout << "  第三部分：深入理解（选学）" << endl;
    cout << "============================================" << endl;

    /*
    ==============================================
    🔍 深入理解：for 循环的变体和高级用法（选学）
    ==============================================

    如果你想知道更多关于 for 循环的细节，可以阅读这部分。
    如果觉得难，可以跳过，不影响后续学习。

    【1. 循环变量的作用域】

    // 方式1：在 for 内部声明（推荐）
    for (int i = 0; i < 5; i++) {
        cout << i;  // ✅ 可以使用
    }
    // cout << i;  // ❌ 错误！i 在循环外不存在

    // 方式2：在 for 外部声明
    int j;
    for (j = 0; j < 5; j++) {
        cout << j;  // ✅ 可以使用
    }
    cout << j;  // ✅ 可以使用，j = 5

    为什么推荐方式1？
    - 变量的生命周期越短越好（KISS原则）
    - 避免循环变量在循环外被误用
    - 代码更清晰，意图更明确

    【2. for 循环的省略写法】

    // 三个部分都可以省略，但分号不能省
    int k = 0;
    for (; k < 5; k++) { }     // 省略初始化
    for (int k = 0; ; k++) { } // 省略条件（死循环！）
    for (int k = 0; k < 5; ) { k++; } // 省略更新

    // 全部省略 = 死循环
    for (;;) { }  // 等价于 while(true)

    不推荐省略写法，会降低代码可读性。

    【3. 多变量 for 循环】

    // 可以同时初始化和更新多个变量
    for (int i = 0, j = 10; i < j; i++, j--) {
        cout << "i=" << i << " j=" << j << endl;
    }
    // 输出：i=0 j=10, i=1 j=9, ..., i=4 j=6

    ==============================================
    */

    cout << endl;
    cout << R"(
🔍 深入理解：for 循环变量的作用域

在 for 循环内部声明的变量（如 int i = 0），
只在循环内部有效，循环结束后就"消失"了。

这就像：你在教室里借了一支笔（声明变量），
下课后笔就还回去了（变量销毁），
你在走廊上就不能再用那支笔了。

这是 C++ 的"作用域"概念，后面会详细学习。
如果现在不理解，可以跳过，不影响后续学习。
)" << endl;

    cout << endl;
    cout << "============================================" << endl;
    cout << "  第四部分：常见错误和陷阱 ⭐⭐⭐⭐⭐" << endl;
    cout << "============================================" << endl;

    // --------------------------------------------
    // 4.1 错误1：差一错误（Off-by-one Error）
    // --------------------------------------------

    cout << endl;
    cout << "4.1 错误1：差一错误（Off-by-one Error）" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
❌ 错误代码：
// 想打印 1 到 5，但少打印了一个
for (int i = 1; i < 5; i++) {
    cout << i << " ";
}
// 输出：1 2 3 4（少了 5！）

原因：i < 5 不包含 5，当 i = 5 时条件为 false

✅ 正确代码：
for (int i = 1; i <= 5; i++) {  // 用 <= 包含 5
    cout << i << " ";
}
// 输出：1 2 3 4 5

记忆技巧：
- 从 0 开始用 <  ：for (i = 0; i < n; i++)  → 循环 n 次
- 从 1 开始用 <= ：for (i = 1; i <= n; i++) → 循环 n 次
)" << endl;

    // --------------------------------------------
    // 4.2 错误2：死循环
    // --------------------------------------------

    cout << "4.2 错误2：死循环" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
❌ 错误代码1：忘记更新循环变量
for (int i = 0; i < 5; ) {  // 没有 i++！
    cout << i;
}
// 结果：无限打印 0，程序卡死

❌ 错误代码2：更新方向错误
for (int i = 0; i < 5; i--) {  // 应该是 i++，写成了 i--
    cout << i;
}
// 结果：i 越来越小（0, -1, -2, ...），永远小于 5

✅ 正确代码：
for (int i = 0; i < 5; i++) {  // 初始化、条件、更新方向一致
    cout << i << " ";
}

预防措施：
- 写完 for 循环后，检查三要素是否匹配
- 初始值 < 终止值 → 用 i++（递增）
- 初始值 > 终止值 → 用 i--（递减）
)" << endl;

    // --------------------------------------------
    // 4.3 错误3：分号位置错误
    // --------------------------------------------

    cout << "4.3 错误3：分号位置错误（最隐蔽的错误！）" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
❌ 错误代码：
for (int i = 0; i < 5; i++);  // 注意这里多了一个分号！
{
    cout << i << " ";
}
// 结果：只打印一次，而且可能编译错误

原因：分号 ; 被当作空的循环体
      for 循环执行了 5 次"什么都不做"
      后面的 {} 只执行一次，且 i 已不存在

✅ 正确代码：
for (int i = 0; i < 5; i++) {  // 没有多余的分号
    cout << i << " ";
}

记忆技巧：
- for (...) 后面直接跟 {，不要加分号
- 这个错误编译器不会报错，非常隐蔽！
)" << endl;

    // --------------------------------------------
    // 4.4 错误4：在循环体内修改循环变量
    // --------------------------------------------

    cout << "4.4 错误4：在循环体内意外修改循环变量" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
❌ 错误代码：
for (int i = 0; i < 10; i++) {
    cout << i << " ";
    i = i + 1;  // 循环体内又修改了 i！
}
// 输出：0 2 4 6 8（跳着打印，不是预期的 0-9）

原因：循环体内 i+1，更新部分又 i++，相当于每次加了 2

✅ 正确代码：
for (int i = 0; i < 10; i++) {
    cout << i << " ";
    // 不要在循环体内修改循环变量
}

预防措施：
- 循环变量的更新只在 for 的第三部分做
- 循环体内不要修改循环变量（除非你明确知道自己在做什么）
)" << endl;

    // --------------------------------------------
    // 4.5 错误5：整数溢出
    // --------------------------------------------

    cout << "4.5 错误5：累加时整数溢出" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
❌ 错误代码：
int sum = 0;
for (int i = 1; i <= 1000000; i++) {
    sum += i * i;  // i*i 可能超出 int 范围！
}
// 结果：得到错误的负数或随机值

原因：int 最大值约 21 亿，i*i 在 i 较大时会溢出

✅ 正确代码：
long long sum = 0;  // 用 long long 存储大数
for (int i = 1; i <= 1000000; i++) {
    sum += (long long)i * i;  // 强制转换避免溢出
}

预防措施：
- 累加、阶乘等操作要考虑数值范围
- 不确定时用 long long 代替 int
- int 范围：约 -21亿 到 21亿
- long long 范围：约 -922亿亿 到 922亿亿
)" << endl;

    // --------------------------------------------
    // 4.6 错误6：嵌套循环变量名冲突
    // --------------------------------------------

    cout << "4.6 错误6：嵌套循环变量名冲突" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
❌ 错误代码：
for (int i = 0; i < 3; i++) {
    for (int i = 0; i < 3; i++) {  // 内层也用 i！
        cout << i;
    }
}
// 结果：内层的 i 会"遮蔽"外层的 i，逻辑混乱

✅ 正确代码：
for (int i = 0; i < 3; i++) {      // 外层用 i
    for (int j = 0; j < 3; j++) {  // 内层用 j
        cout << "(" << i << "," << j << ") ";
    }
    cout << endl;
}

记忆技巧：
- 外层循环用 i，内层用 j，再内层用 k
- 或者用有意义的名字：row、col、layer
)" << endl;

    // --------------------------------------------
    // 4.7 错误7：浮点数作为循环变量
    // --------------------------------------------

    cout << "4.7 错误7：用浮点数作为循环变量" << endl;
    cout << string(60, '-') << endl;

    cout << R"(
❌ 错误代码：
for (float f = 0.0; f != 1.0; f += 0.1) {
    cout << f << " ";
}
// 结果：可能死循环！

原因：浮点数有精度误差
      0.1 在计算机中不能精确表示
      累加 10 次后可能是 0.999999... 而不是 1.0
      所以 f != 1.0 永远为 true

✅ 正确代码：
// 方法1：用整数循环，内部转换
for (int i = 0; i < 10; i++) {
    float f = i * 0.1;
    cout << f << " ";
}

// 方法2：用近似比较
for (float f = 0.0; f < 0.95; f += 0.1) {
    cout << f << " ";
}

预防措施：
- 尽量用整数作为循环变量
- 浮点数比较不要用 == 或 !=
- 浮点数条件用 < 或 > 代替
)" << endl;

    cout << endl;
    cout << "============================================" << endl;
    cout << "  第五部分：函数卡片速查" << endl;
    cout << "============================================" << endl;

    cout << R"(

【结构卡片：for 循环】

语法：for (初始化; 条件; 更新) { 循环体; }

三要素：
  - 初始化 (执行1次): 设置循环变量起始值
  - 条件   (每次检查): true 继续，false 停止
  - 更新   (每次执行后): 改变循环变量

常用模式：
  从0开始：for (int i = 0; i < n; i++)    → 循环 n 次
  从1开始：for (int i = 1; i <= n; i++)   → 循环 n 次
  倒序：  for (int i = n; i >= 1; i--)    → 从 n 到 1
  步长2： for (int i = 0; i < n; i += 2)  → 每次跳 2

────────────────────────────────────

【运算符卡片：++ 和 --】

语法：i++ 或 ++i（自增1），i-- 或 --i（自减1）
作用：让变量的值加1或减1

区别（在表达式中）：
  - i++（后置）：先使用当前值，再加1
  - ++i（前置）：先加1，再使用新值

示例：
  int a = 5;
  int b = a++;  // b = 5, a = 6（先用后加）
  int c = ++a;  // c = 7, a = 7（先加后用）

在 for 循环中：i++ 和 ++i 效果相同

────────────────────────────────────

【运算符卡片：+= -= *= /=】

语法：变量 运算符= 值
作用：复合赋值，简化写法

对照表：
  sum += i    等价于  sum = sum + i
  sum -= i    等价于  sum = sum - i
  sum *= i    等价于  sum = sum * i
  sum /= i    等价于  sum = sum / i
  i += 2      等价于  i = i + 2

)" << endl;

    cout << endl;
    cout << "============================================" << endl;
    cout << "  学习完成！" << endl;
    cout << "============================================" << endl;

    cout << R"(
本节你学到了：
✅ for 循环的基本语法和三要素
✅ for 循环的执行流程
✅ 正序、倒序、自定义步长循环
✅ 累加求和、求阶乘
✅ 嵌套循环打印图案
✅ 7 个常见错误和预防措施

下一步：完成 08_for_loop_exercise.cpp 练习题！
)" << endl;

    return 0;
}
