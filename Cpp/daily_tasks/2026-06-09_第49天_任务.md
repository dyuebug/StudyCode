# 📅 第49天学习任务清单

**日期**：2026-06-09  
**当前阶段**：阶段9收尾 + 阶段10开始  
**昨日完成**：Qt 网络编程进阶（文件下载器、天气查询应用）  
**昨日遗留**：无

---

## 🎯 今日学习目标

阶段9总结 + 开始阶段10：Qt 多线程编程

### 核心目标
- [ ] 整理阶段9网络编程速查表
- [ ] 掌握 QThread 创建工作线程
- [ ] 掌握 moveToThread 模式（推荐）
- [ ] 掌握线程间信号槽通信
- [ ] 理解线程安全基础

---

## 📝 具体任务

### 任务1：阶段9速查表（预计20分钟）
**文件**：`qt_basics/31_network_summary.md`

**内容**：
- QNetworkAccessManager / QNetworkRequest / QNetworkReply 速查
- GET / POST 请求模板代码
- JSON 解析速查
- 常见错误和解决方案

---

### 任务2：QThread 基础（预计50分钟）
**文件**：`qt_basics/32_thread_basics.cpp`

**学习内容**：
- [ ] 为什么需要多线程（耗时操作阻塞 UI）
- [ ] QThread 两种使用方式：
  - 方式A：继承 QThread，重写 run()（简单但有局限）
  - 方式B：Worker + moveToThread（推荐，更灵活）
- [ ] 线程间信号槽（Qt::QueuedConnection 自动跨线程）
- [ ] QMutex 互斥锁（保护共享数据）
- [ ] 线程安全的 UI 更新（只能在主线程更新 UI）

**核心代码预览**：
```cpp
// 推荐方式：Worker + moveToThread
class Worker : public QObject {
    Q_OBJECT
public slots:
    void doWork() {
        // 耗时操作（在工作线程中执行）
        for (int i = 0; i <= 100; i++) {
            QThread::msleep(50);
            emit progress(i);  // 发送进度信号
        }
        emit finished();
    }
signals:
    void progress(int value);
    void finished();
};

// 主线程中
QThread *thread = new QThread();
Worker *worker = new Worker();
worker->moveToThread(thread);  // 将 worker 移到工作线程

connect(thread, &QThread::started, worker, &Worker::doWork);
connect(worker, &Worker::finished, thread, &QThread::quit);
connect(worker, &Worker::finished, worker, &QObject::deleteLater);
connect(thread, &QThread::finished, thread, &QObject::deleteLater);
connect(worker, &Worker::progress, this, &MainWindow::updateProgress);

thread->start();  // 启动线程
```

---

### 任务3：多线程实战（预计20分钟）
**文件**：`qt_basics/33_thread_practice.cpp`

**实践内容**：
- [ ] 后台计算大量数据（不阻塞 UI）
- [ ] 进度条实时更新
- [ ] 支持取消后台任务

---

## ⏰ 时间安排

- 任务1：20分钟（速查表）
- 任务2：50分钟（QThread 基础）
- 任务3：20分钟（多线程实战）
- **总预计**：1.5小时

---

## 🎯 成功标准

- ✅ 理解为什么需要多线程
- ✅ 能够用 moveToThread 模式创建工作线程
- ✅ 线程间能够通过信号槽通信
- ✅ UI 在后台任务运行时保持响应
