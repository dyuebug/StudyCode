# 📅 第47天学习任务清单

**日期**：2026-06-07  
**当前阶段**：阶段8收尾 + 阶段9开始  
**昨日完成**：Qt 数据库进阶（事务、JOIN、QSqlRelationalTableModel、带分组联系人管理）  
**昨日遗留**：无

---

## 🎯 今日学习目标

阶段8总结 + 开始阶段9：Qt 网络编程

### 核心目标
- [ ] 整理阶段8 SQL 知识点速查表
- [ ] 掌握 QNetworkAccessManager 发送 HTTP 请求
- [ ] 掌握 QJsonDocument 解析 JSON 响应
- [ ] 完成一个调用公开 API 的小应用

---

## 📝 具体任务

### 任务1：阶段8速查表（预计20分钟）
**文件**：`qt_basics/26_sql_summary.md`

**内容**：
- QSqlDatabase 连接速查
- QSqlQuery CRUD 速查
- QSqlTableModel vs QSqlRelationalTableModel 对比
- 事务模板代码
- 常用 SQL 语句速查（JOIN/GROUP BY/聚合）

---

### 任务2：Qt 网络编程基础（预计50分钟）
**文件**：`qt_basics/27_network_basics.cpp`

**需在 .pro 添加**：`QT += network`

**学习内容**：
- [ ] QNetworkAccessManager：网络请求管理器
- [ ] QNetworkRequest：封装请求（URL、Header）
- [ ] QNetworkReply：响应对象（异步）
- [ ] GET 请求：`manager->get(request)`
- [ ] POST 请求：`manager->post(request, data)`
- [ ] 信号：`reply->finished()` 请求完成
- [ ] QJsonDocument：解析 JSON 字符串
- [ ] QJsonObject / QJsonArray：访问 JSON 数据

**核心代码预览**：
```cpp
QNetworkAccessManager *manager = new QNetworkAccessManager(this);
QNetworkRequest request(QUrl("https://api.example.com/data"));
request.setHeader(QNetworkRequest::ContentTypeHeader, "application/json");

QNetworkReply *reply = manager->get(request);
connect(reply, &QNetworkReply::finished, this, [reply]() {
    if (reply->error() == QNetworkReply::NoError) {
        QByteArray data = reply->readAll();
        QJsonDocument doc = QJsonDocument::fromJson(data);
        QJsonObject obj = doc.object();
        QString value = obj["key"].toString();
    }
    reply->deleteLater();  // 必须手动释放！
});
```

---

### 任务3：网络请求实战（预计20分钟）
**文件**：`qt_basics/28_network_practice.cpp`

**实践内容**：
- [ ] 调用公开 JSON API（如 httpbin.org/get 测试接口）
- [ ] 显示请求状态、响应内容
- [ ] 处理网络错误（超时、无网络等）
- [ ] 显示加载中状态（QProgressBar 或 QLabel）

---

## ⏰ 时间安排

- 任务1：20分钟（速查表）
- 任务2：50分钟（网络编程基础）
- 任务3：20分钟（实战）
- **总预计**：1.5小时

---

## 🎯 成功标准

- ✅ 能够发送 HTTP GET 请求并获取响应
- ✅ 能够解析 JSON 数据并显示
- ✅ 能够处理网络错误
- ✅ 理解异步请求的信号槽模式
