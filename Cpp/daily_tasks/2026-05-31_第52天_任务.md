# 📅 第52天学习任务清单

**日期**：2026-05-31  
**学习阶段**：Qt 综合项目 - 个人任务管理器（第1阶段）  
**学习主题**：基础框架搭建（项目结构、数据库设计、核心类实现）  
**本周目标**：完成个人任务管理器的基础框架

---

## 🎯 今日学习目标

- [ ] 创建 Qt 项目结构
- [ ] 设计数据库表（tasks、categories）
- [ ] 实现 DatabaseManager 单例类
- [ ] 创建 Task 和 Category 数据类
- [ ] 实现 MainWindow 基本框架
- [ ] 测试数据库连接

---

## 📝 具体任务

### 任务1：项目初始化（预计20分钟）

**步骤**：
1. 创建 Qt Widgets Application 项目
   - 项目名：TaskManager
   - 基类：QMainWindow
   - 添加模块：sql、network、concurrent

2. 创建项目目录结构：
   ```
   TaskManager/
   ├── main.cpp
   ├── MainWindow.h/cpp
   ├── models/
   │   ├── Task.h/cpp
   │   ├── Category.h/cpp
   │   └── DatabaseManager.h/cpp
   ├── dialogs/
   │   └── TaskDialog.h/cpp
   └── resources/
       └── styles/
   ```

3. 配置 .pro 文件：
   ```
   QT += core gui sql network concurrent
   greaterThan(QT_MAJOR_VERSION, 4): QT += widgets
   ```

**成功标准**：
- ✅ 项目可以成功编译
- ✅ 目录结构清晰

---

### 任务2：数据库设计（预计30分钟）

**数据库表设计**：

#### 表1：categories（分类表）
```sql
CREATE TABLE categories (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    color TEXT DEFAULT '#3498db'
);
```

**字段说明**：
- `id`：分类ID（主键，自增）
- `name`：分类名称（唯一）
- `color`：分类颜色（十六进制颜色值）

**预设数据**：
```sql
INSERT INTO categories (name, color) VALUES ('工作', '#e74c3c');
INSERT INTO categories (name, color) VALUES ('学习', '#3498db');
INSERT INTO categories (name, color) VALUES ('生活', '#2ecc71');
INSERT INTO categories (name, color) VALUES ('其他', '#95a5a6');
```

#### 表2：tasks（任务表）
```sql
CREATE TABLE tasks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    description TEXT,
    category_id INTEGER,
    priority INTEGER DEFAULT 2,
    due_date TEXT,
    completed INTEGER DEFAULT 0,
    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
    updated_at TEXT DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (category_id) REFERENCES categories(id)
);
```

**字段说明**：
- `id`：任务ID（主键，自增）
- `title`：任务标题（必填）
- `description`：任务描述
- `category_id`：分类ID（外键）
- `priority`：优先级（1-高，2-中，3-低）
- `due_date`：截止日期（TEXT 格式：YYYY-MM-DD）
- `completed`：是否完成（0-未完成，1-已完成）
- `created_at`：创建时间
- `updated_at`：更新时间

**成功标准**：
- ✅ 数据库表设计合理
- ✅ 外键关系正确
- ✅ 预设数据插入成功

---

### 任务3：核心类实现（预计40分钟）

#### 3.1 Task 数据类（models/Task.h/cpp）

**Task.h**：
```cpp
#ifndef TASK_H
#define TASK_H

#include <QString>
#include <QDate>
#include <QDateTime>

class Task {
public:
    Task();
    Task(int id, const QString &title, const QString &description,
         int categoryId, int priority, const QDate &dueDate,
         bool completed, const QDateTime &createdAt, const QDateTime &updatedAt);

    // Getters
    int id() const { return m_id; }
    QString title() const { return m_title; }
    QString description() const { return m_description; }
    int categoryId() const { return m_categoryId; }
    int priority() const { return m_priority; }
    QDate dueDate() const { return m_dueDate; }
    bool completed() const { return m_completed; }
    QDateTime createdAt() const { return m_createdAt; }
    QDateTime updatedAt() const { return m_updatedAt; }

    // Setters
    void setId(int id) { m_id = id; }
    void setTitle(const QString &title) { m_title = title; }
    void setDescription(const QString &description) { m_description = description; }
    void setCategoryId(int categoryId) { m_categoryId = categoryId; }
    void setPriority(int priority) { m_priority = priority; }
    void setDueDate(const QDate &dueDate) { m_dueDate = dueDate; }
    void setCompleted(bool completed) { m_completed = completed; }
    void setUpdatedAt(const QDateTime &updatedAt) { m_updatedAt = updatedAt; }

    // 优先级文本
    QString priorityText() const;
    
    // 是否逾期
    bool isOverdue() const;

private:
    int m_id;
    QString m_title;
    QString m_description;
    int m_categoryId;
    int m_priority;
    QDate m_dueDate;
    bool m_completed;
    QDateTime m_createdAt;
    QDateTime m_updatedAt;
};

#endif // TASK_H
```

#### 3.2 Category 数据类（models/Category.h/cpp）

**Category.h**：
```cpp
#ifndef CATEGORY_H
#define CATEGORY_H

#include <QString>

class Category {
public:
    Category();
    Category(int id, const QString &name, const QString &color);

    // Getters
    int id() const { return m_id; }
    QString name() const { return m_name; }
    QString color() const { return m_color; }

    // Setters
    void setId(int id) { m_id = id; }
    void setName(const QString &name) { m_name = name; }
    void setColor(const QString &color) { m_color = color; }

private:
    int m_id;
    QString m_name;
    QString m_color;
};

#endif // CATEGORY_H
```

#### 3.3 DatabaseManager 单例类（models/DatabaseManager.h/cpp）

**DatabaseManager.h**：
```cpp
#ifndef DATABASEMANAGER_H
#define DATABASEMANAGER_H

#include <QObject>
#include <QSqlDatabase>
#include <QList>
#include <QMap>
#include "Task.h"
#include "Category.h"

class DatabaseManager : public QObject {
    Q_OBJECT

public:
    // 单例模式
    static DatabaseManager& instance();

    // 初始化数据库
    bool initialize();

    // 任务操作
    bool addTask(const Task &task);
    bool updateTask(const Task &task);
    bool deleteTask(int taskId);
    QList<Task> getAllTasks();
    QList<Task> getTasksByCategory(int categoryId);
    Task getTaskById(int taskId);

    // 分类操作
    bool addCategory(const Category &category);
    bool updateCategory(const Category &category);
    bool deleteCategory(int categoryId);
    QList<Category> getAllCategories();
    Category getCategoryById(int categoryId);

    // 统计
    int getTotalTaskCount();
    int getCompletedTaskCount();
    QMap<int, int> getTaskCountByCategory();
    QMap<int, int> getTaskCountByPriority();

private:
    DatabaseManager();
    ~DatabaseManager();
    DatabaseManager(const DatabaseManager&) = delete;
    DatabaseManager& operator=(const DatabaseManager&) = delete;

    bool createTables();
    bool insertDefaultCategories();

    QSqlDatabase m_database;
};

#endif // DATABASEMANAGER_H
```

**成功标准**：
- ✅ Task 和 Category 类实现完整
- ✅ DatabaseManager 单例模式正确
- ✅ 数据库连接成功
- ✅ 表创建成功

---

### 任务4：MainWindow 框架（预计30分钟）

**MainWindow.h**：
```cpp
#ifndef MAINWINDOW_H
#define MAINWINDOW_H

#include <QMainWindow>
#include <QTableView>
#include <QSqlTableModel>
#include <QComboBox>
#include <QLabel>

class MainWindow : public QMainWindow {
    Q_OBJECT

public:
    MainWindow(QWidget *parent = nullptr);
    ~MainWindow();

private slots:
    void onAddTask();
    void onEditTask();
    void onDeleteTask();
    void onCompleteTask();
    void onFilterChanged();
    void onRefresh();

private:
    void setupUI();
    void setupMenuBar();
    void setupToolBar();
    void setupStatusBar();
    void loadTasks();
    void updateStatistics();

    QTableView *m_taskTableView;
    QSqlTableModel *m_taskModel;
    QComboBox *m_categoryFilter;
    QComboBox *m_priorityFilter;
    QLabel *m_statsLabel;
};

#endif // MAINWINDOW_H
```

**成功标准**：
- ✅ MainWindow 基本框架搭建完成
- ✅ 菜单栏和工具栏添加成功
- ✅ 任务列表视图显示正常
- ✅ 可以显示空的任务列表

---

## ⏰ 时间安排

**总预计**：2.0小时

- 09:00 - 09:20：项目初始化（20分钟）
- 09:20 - 09:50：数据库设计（30分钟）
- 09:50 - 10:30：核心类实现（40分钟）
- 10:30 - 11:00：MainWindow 框架（30分钟）

---

## 📚 学习资源

- `39_project_plan.md` - 综合项目规划
- Qt 官方文档：QSqlDatabase、QSqlTableModel、QMainWindow
- SQLite 语法参考

---

## 🎓 学习重点

- ⭐⭐⭐⭐⭐ **项目结构设计**：合理的模块划分
- ⭐⭐⭐⭐⭐ **数据库设计**：表结构、外键关系
- ⭐⭐⭐⭐ **单例模式**：DatabaseManager 单例实现
- ⭐⭐⭐⭐ **Model-View 架构**：QSqlTableModel + QTableView

---

## 💡 学习建议

1. 先仔细阅读项目规划文档（`39_project_plan.md`）
2. 按照任务顺序逐步实施
3. 每完成一个模块就测试一次
4. 遇到问题及时记录和解决
5. 保持代码整洁和注释清晰

---

## ✅ 成功标准

- ✅ 项目结构清晰
- ✅ 数据库表创建成功
- ✅ DatabaseManager 类可以正常连接数据库
- ✅ MainWindow 基本框架搭建完成
- ✅ 可以显示空的任务列表

---

**加油！今天开始开发 Qt 综合项目！** 🚀
