# 📅 第26天学习任务清单

**日期**：2026-05-30  
**当前阶段**：阶段5 - 项目实战（第2天）  
**项目名称**：学生管理系统 - 成绩管理功能  
**昨日完成**：完成学生管理系统基础版（Student类、StudentManager类、主程序菜单）  

---

## 🎯 今日学习目标

- [ ] 设计 Course 类（课程信息）
- [ ] 为 Student 类添加成绩管理功能
- [ ] 实现成绩录入和查询
- [ ] 实现统计功能（平均分、排名、优秀学生）
- [ ] 实现高级查询（按条件过滤）

---

## 📝 具体任务

### 任务1：设计 Course 类（预计20分钟）

**设计目标**：
- 封装课程信息
- 提供课程成绩管理
- 支持课程信息显示

**类设计**：

```cpp
class Course {
private:
    string courseCode;    // 课程代码（如：CS101）
    string courseName;    // 课程名称（如：数据结构）
    int credits;          // 学分（1-5）
    double score;         // 成绩（0-100）

public:
    // 构造函数
    Course();
    Course(const string& code, const string& name, int credits, double score);
    
    // Getter 方法
    string getCourseCode() const;
    string getCourseName() const;
    int getCredits() const;
    double getScore() const;
    
    // Setter 方法
    void setScore(double score);
    
    // 显示方法
    void display() const;
    void displayBrief() const;  // 简要显示（用于列表）
};
```

**文件**：
- `student_management/Course.h` - 课程类头文件
- `student_management/Course.cpp` - 课程类实现

**实现要点**：
- 使用成员初始化列表
- 成绩验证（0-100范围）
- 格式化输出

---

### 任务2：扩展 Student 类（预计30分钟）

**新增功能**：
1. 添加成员变量：`vector<Course> courses`
2. 添加课程：`addCourse(const Course& course)`
3. 删除课程：`removeCourse(const string& courseCode)`
4. 查询课程：`findCourse(const string& courseCode)`
5. 显示所有课程：`displayCourses()`
6. 计算平均分：`getAverageScore()`
7. 获取课程数量：`getCourseCount()`

**修改文件**：
- `student_management/Student.h` - 添加课程管理接口
- `student_management/Student.cpp` - 实现课程管理功能

**实现要点**：
- 使用 `vector<Course>` 存储课程列表
- 使用 `find_if` + Lambda 查找课程
- 计算平均分时处理空课程列表
- 提供 const 版本的查询方法

**关键代码示例**：

```cpp
// 计算平均分
double Student::getAverageScore() const {
    if (courses.empty()) {
        return 0.0;
    }
    
    double sum = 0.0;
    for (const auto& course : courses) {
        sum += course.getScore();
    }
    
    return sum / courses.size();
}

// 查找课程
Course* Student::findCourse(const string& courseCode) {
    auto it = find_if(courses.begin(), courses.end(),
        [&courseCode](const Course& c) {
            return c.getCourseCode() == courseCode;
        });
    
    return (it != courses.end()) ? &(*it) : nullptr;
}
```

---

### 任务3：扩展 StudentManager 类（预计40分钟）

**新增功能**：

1. **为学生添加成绩**
   ```cpp
   bool addGrade(const string& studentId, const Course& course);
   ```
   - 查找学生
   - 调用学生的 addCourse 方法
   - 返回操作结果

2. **查询学生成绩**
   ```cpp
   void displayStudentGrades(const string& studentId) const;
   ```
   - 查找学生
   - 显示学生的所有课程和成绩
   - 显示平均分

3. **计算班级平均分**
   ```cpp
   double getClassAverage() const;
   ```
   - 遍历所有学生
   - 计算所有学生的平均分
   - 返回班级平均分

4. **成绩排名**
   ```cpp
   vector<pair<string, double>> getRanking() const;
   ```
   - 收集所有学生的学号和平均分
   - 按平均分降序排序
   - 返回排名列表

5. **查找优秀学生**
   ```cpp
   vector<Student> findExcellentStudents(double threshold) const;
   ```
   - 遍历所有学生
   - 筛选平均分 >= threshold 的学生
   - 返回优秀学生列表

6. **按条件过滤**
   ```cpp
   vector<Student> filterStudents(function<bool(const Student&)> predicate) const;
   ```
   - 使用 Lambda 表达式作为过滤条件
   - 返回符合条件的学生列表

**修改文件**：
- `student_management/StudentManager.h` - 添加统计和查询接口
- `student_management/StudentManager.cpp` - 实现统计和查询功能

**实现要点**：
- 使用 `<algorithm>` 中的 sort 排序
- 使用 `<functional>` 中的 function 对象
- Lambda 表达式作为过滤条件
- 处理空列表和无成绩的情况

**关键代码示例**：

```cpp
// 成绩排名
vector<pair<string, double>> StudentManager::getRanking() const {
    vector<pair<string, double>> ranking;
    
    // 收集学号和平均分
    for (const auto& student : students) {
        ranking.push_back({student.getId(), student.getAverageScore()});
    }
    
    // 按平均分降序排序
    sort(ranking.begin(), ranking.end(),
        [](const pair<string, double>& a, const pair<string, double>& b) {
            return a.second > b.second;  // 降序
        });
    
    return ranking;
}

// 按条件过滤
vector<Student> StudentManager::filterStudents(function<bool(const Student&)> predicate) const {
    vector<Student> result;
    
    for (const auto& student : students) {
        if (predicate(student)) {
            result.push_back(student);
        }
    }
    
    return result;
}
```

---

### 任务4：更新主程序菜单（预计30分钟）

**新增菜单项**：

```
========== 学生管理系统 ==========
1. 添加学生
2. 删除学生
3. 修改学生信息
4. 查询学生
5. 显示所有学生
6. 添加成绩          ← 新增
7. 查询成绩          ← 新增
8. 统计信息          ← 新增
9. 高级查询          ← 新增
0. 退出系统
==================================
```

**新增功能实现**：

1. **添加成绩菜单**
   - 输入学号
   - 输入课程代码、课程名称、学分、成绩
   - 调用 addGrade 方法

2. **查询成绩菜单**
   - 输入学号
   - 显示学生的所有课程和成绩
   - 显示平均分

3. **统计信息菜单**
   - 显示班级平均分
   - 显示成绩排名（前10名）
   - 显示优秀学生（平均分 >= 90）

4. **高级查询菜单**
   - 按年龄范围查询
   - 按专业查询
   - 按平均分范围查询
   - 使用 Lambda 表达式实现

**修改文件**：
- `student_management/main.cpp` - 添加新菜单项和功能

**实现要点**：
- 使用 switch-case 处理新选项
- 提供友好的用户提示
- 处理输入错误
- 格式化输出统计信息

**关键代码示例**：

```cpp
// 高级查询菜单
void advancedQueryMenu(StudentManager& manager) {
    cout << "\n--- 高级查询 ---" << endl;
    cout << "1. 按年龄范围查询" << endl;
    cout << "2. 按专业查询" << endl;
    cout << "3. 按平均分范围查询" << endl;
    cout << "请选择：";
    
    int choice;
    cin >> choice;
    
    vector<Student> results;
    
    switch (choice) {
        case 1: {
            int minAge, maxAge;
            cout << "请输入最小年龄：";
            cin >> minAge;
            cout << "请输入最大年龄：";
            cin >> maxAge;
            
            results = manager.filterStudents(
                [minAge, maxAge](const Student& s) {
                    return s.getAge() >= minAge && s.getAge() <= maxAge;
                });
            break;
        }
        case 2: {
            string major;
            cout << "请输入专业：";
            cin.ignore();
            getline(cin, major);
            
            results = manager.filterStudents(
                [&major](const Student& s) {
                    return s.getMajor() == major;
                });
            break;
        }
        case 3: {
            double minScore, maxScore;
            cout << "请输入最低平均分：";
            cin >> minScore;
            cout << "请输入最高平均分：";
            cin >> maxScore;
            
            results = manager.filterStudents(
                [minScore, maxScore](const Student& s) {
                    return s.getAverageScore() >= minScore && 
                           s.getAverageScore() <= maxScore;
                });
            break;
        }
    }
    
    // 显示查询结果
    if (results.empty()) {
        cout << "未找到符合条件的学生。" << endl;
    } else {
        cout << "\n找到 " << results.size() << " 个学生：" << endl;
        // 显示学生列表...
    }
}
```

---

## ⏰ 时间安排

**总预计时长**：2小时

- Course 类设计：20分钟
- Student 类扩展：30分钟
- StudentManager 类扩展：40分钟
- 主程序菜单更新：30分钟

---

## 📚 学习资源

**需要用到的知识**：
- 类的设计与扩展
- vector 嵌套使用：`vector<Course>`
- STL 算法：sort、find_if
- Lambda 表达式：作为过滤条件
- function 对象：`function<bool(const Student&)>`
- pair 容器：`pair<string, double>`

**参考文件**：
- 阶段4：STL 容器与算法
- 阶段4：Lambda 表达式与函数对象
- 昨日完成：Student.h/cpp、StudentManager.h/cpp

---

## 💡 学习建议

1. **先设计 Course 类**
   - 明确课程需要哪些信息
   - 设计清晰的接口
   - 测试 Course 类功能

2. **逐步扩展 Student 类**
   - 先添加 courses 成员变量
   - 实现基本的添加、删除、查询
   - 实现计算平均分
   - 测试课程管理功能

3. **扩展 StudentManager 类**
   - 先实现基本的成绩录入和查询
   - 再实现统计功能（平均分、排名）
   - 最后实现高级查询（Lambda 过滤）
   - 逐个测试每个功能

4. **更新主程序菜单**
   - 添加新菜单项
   - 实现各个功能的交互
   - 测试完整流程

5. **使用 Lambda 表达式**
   - 理解 Lambda 的捕获列表
   - 练习不同的过滤条件
   - 体会 Lambda 的灵活性

6. **注意数据一致性**
   - 删除学生时，成绩也要删除
   - 修改学生信息时，成绩保持不变
   - 处理空课程列表的情况

7. **测试各种场景**
   - 学生没有成绩
   - 学生有多门课程
   - 计算平均分
   - 成绩排名
   - 高级查询

---

## ✅ 成功标准

- ✅ Course 类功能完整，能正确存储课程信息
- ✅ Student 类能管理多门课程，计算平均分
- ✅ StudentManager 类能实现统计功能（平均分、排名）
- ✅ 主程序菜单包含成绩管理选项
- ✅ 能正确计算班级平均分和成绩排名
- ✅ 高级查询功能正常工作（Lambda 过滤）
- ✅ 代码结构清晰，注释完整
- ✅ 程序能正常编译运行

---

## 🔗 技术要点

### 1. vector 嵌套使用

```cpp
class Student {
private:
    vector<Course> courses;  // 每个学生有多门课程
};
```

### 2. Lambda 表达式作为过滤条件

```cpp
// 过滤优秀学生
auto excellentStudents = manager.filterStudents(
    [](const Student& s) {
        return s.getAverageScore() >= 90;
    });

// 按年龄范围过滤
auto youngStudents = manager.filterStudents(
    [](const Student& s) {
        return s.getAge() >= 18 && s.getAge() <= 22;
    });
```

### 3. function 对象作为参数

```cpp
#include <functional>

vector<Student> filterStudents(function<bool(const Student&)> predicate) const {
    vector<Student> result;
    for (const auto& student : students) {
        if (predicate(student)) {
            result.push_back(student);
        }
    }
    return result;
}
```

### 4. pair 容器存储排名

```cpp
#include <utility>

vector<pair<string, double>> ranking;  // <学号, 平均分>
ranking.push_back({student.getId(), student.getAverageScore()});

// 按平均分排序
sort(ranking.begin(), ranking.end(),
    [](const pair<string, double>& a, const pair<string, double>& b) {
        return a.second > b.second;  // 降序
    });
```

### 5. 计算平均分

```cpp
double getAverageScore() const {
    if (courses.empty()) {
        return 0.0;
    }
    
    double sum = 0.0;
    for (const auto& course : courses) {
        sum += course.getScore();
    }
    
    return sum / courses.size();
}
```

---

## 📋 今日开发清单

**文件创建**：
- [ ] `Course.h` - 课程类头文件
- [ ] `Course.cpp` - 课程类实现

**文件修改**：
- [ ] `Student.h` - 添加课程管理接口
- [ ] `Student.cpp` - 实现课程管理功能
- [ ] `StudentManager.h` - 添加统计和查询接口
- [ ] `StudentManager.cpp` - 实现统计和查询功能
- [ ] `main.cpp` - 添加新菜单项和功能

**功能实现**：
- [ ] Course 类：构造函数、getter/setter、display
- [ ] Student 类：addCourse、removeCourse、findCourse、getAverageScore
- [ ] StudentManager 类：addGrade、displayStudentGrades、getClassAverage
- [ ] StudentManager 类：getRanking、findExcellentStudents、filterStudents
- [ ] 主程序：添加成绩、查询成绩、统计信息、高级查询

**测试验证**：
- [ ] 添加成绩功能测试
- [ ] 查询成绩功能测试
- [ ] 计算平均分测试
- [ ] 成绩排名测试
- [ ] 高级查询测试（Lambda 过滤）

---

## 🎯 项目进度规划

**第25天（昨天）**：✅ 已完成
- 设计类结构
- 实现基本的增删改查功能
- 完成主程序菜单

**第26天（今天）**：🎯 进行中
- 实现成绩管理功能
- 添加统计功能（平均分、排名）
- 实现高级查询（按条件过滤）

**第27天（明天）**：📝 计划中
- 实现数据持久化（文件保存/加载）
- 完善错误处理
- 优化用户体验
- 项目总结

---

**任务清单生成时间**：2026-05-29  
**预祝开发顺利！今天将让系统功能更加完善！💪🚀**
