# 📅 第53天学习任务清单

**日期**：2026-06-01  
**学习阶段**：Qt 综合项目 - 个人任务管理器（第2阶段）  
**学习主题**：核心功能开发（添加任务对话框、编辑任务功能）  
**本周目标**：完成个人任务管理器的核心功能

---

## 🎯 今日学习目标

- [ ] 创建 TaskDialog 对话框类
- [ ] 实现添加任务功能
- [ ] 实现编辑任务功能
- [ ] 测试所有功能
- [ ] 修复 Bug

---

## 📝 具体任务

### 任务1：创建 TaskDialog 对话框（预计40分钟）

**步骤1：创建 TaskDialog 类**
- 文件：TaskDialog.h/cpp
- 继承：QDialog
- 功能：添加任务、编辑任务

**步骤2：设计对话框 UI**
- 标题输入框（QLineEdit）
- 描述输入框（QTextEdit）
- 分类下拉框（QComboBox）
- 优先级下拉框（QComboBox）
- 截止日期选择器（QDateEdit）
- 确定/取消按钮（QDialogButtonBox）

**步骤3：实现两种模式**
- 添加模式：空白表单
- 编辑模式：填充现有任务数据

**步骤4：实现数据验证**
- 标题不能为空
- 截止日期不能早于今天（可选）
- 显示错误提示

**成功标准**：
- ✅ TaskDialog 对话框创建成功
- ✅ UI 布局合理美观
- ✅ 添加模式和编辑模式正常工作
- ✅ 数据验证正常

---

### 任务2：集成到 MainWindow（预计30分钟）

**步骤1：实现 onAddTask() 槽函数**
```cpp
void MainWindow::onAddTask()
{
    // 创建 TaskDialog（添加模式）
    TaskDialog dialog(this);
    
    // 显示对话框
    if (dialog.exec() == QDialog::Accepted) {
        // 获取任务数据
        Task task = dialog.getTask();
        
        // 添加到数据库
        if (DatabaseManager::instance().addTask(task)) {
            QMessageBox::information(this, "成功", "任务添加成功");
            loadTasks();
            updateStatistics();
        } else {
            QMessageBox::critical(this, "错误", "任务添加失败");
        }
    }
}
```

**步骤2：实现 onEditTask() 槽函数**
```cpp
void MainWindow::onEditTask()
{
    // 获取选中的任务
    int currentRow = m_taskTable->currentRow();
    if (currentRow < 0) {
        QMessageBox::warning(this, "警告", "请先选择一个任务");
        return;
    }
    
    // 获取任务 ID
    int taskId = m_taskTable->item(currentRow, 0)->text().toInt();
    Task task = DatabaseManager::instance().getTaskById(taskId);
    
    // 创建 TaskDialog（编辑模式）
    TaskDialog dialog(task, this);
    
    // 显示对话框
    if (dialog.exec() == QDialog::Accepted) {
        // 获取更新后的任务数据
        Task updatedTask = dialog.getTask();
        updatedTask.setId(taskId);
        
        // 更新数据库
        if (DatabaseManager::instance().updateTask(updatedTask)) {
            QMessageBox::information(this, "成功", "任务更新成功");
            loadTasks();
            updateStatistics();
        } else {
            QMessageBox::critical(this, "错误", "任务更新失败");
        }
    }
}
```

**步骤3：更新 MainWindow.h**
- 添加 `#include "TaskDialog.h"`
- 移除 onAddTask() 和 onEditTask() 中的临时提示代码

**成功标准**：
- ✅ onAddTask() 正常工作
- ✅ onEditTask() 正常工作
- ✅ 任务列表自动刷新
- ✅ 统计信息自动更新

---

### 任务3：测试和优化（预计20分钟）

**测试用例**：

1. **测试添加任务**
   - [ ] 添加一个完整的任务（所有字段都填写）
   - [ ] 添加一个最小任务（只填写标题）
   - [ ] 尝试添加空标题任务（应该失败）
   - [ ] 添加不同分类的任务
   - [ ] 添加不同优先级的任务

2. **测试编辑任务**
   - [ ] 编辑任务标题
   - [ ] 编辑任务描述
   - [ ] 修改任务分类
   - [ ] 修改任务优先级
   - [ ] 修改截止日期
   - [ ] 清空截止日期

3. **测试数据验证**
   - [ ] 标题为空时无法提交
   - [ ] 错误提示正常显示

4. **测试界面交互**
   - [ ] 对话框取消按钮正常工作
   - [ ] 对话框确定按钮正常工作
   - [ ] 任务列表自动刷新
   - [ ] 统计信息自动更新

**Bug 修复**：
- 记录发现的问题
- 逐个修复
- 重新测试

**成功标准**：
- ✅ 所有测试用例通过
- ✅ 没有明显 Bug
- ✅ 用户体验良好

---

## ⏰ 时间安排

**总预计**：1.5小时

- 09:00 - 09:40：创建 TaskDialog 对话框（40分钟）
- 09:40 - 10:10：集成到 MainWindow（30分钟）
- 10:10 - 10:30：测试和优化（20分钟）

---

## 📚 学习资源

- `TaskManager/` - 项目目录
- Qt 官方文档：QDialog、QDateEdit、QTextEdit、QDialogButtonBox
- 昨天创建的项目文件

---

## 🎓 学习重点

- ⭐⭐⭐⭐⭐ **自定义对话框**：继承 QDialog
- ⭐⭐⭐⭐⭐ **表单验证**：输入检查
- ⭐⭐⭐⭐ **QDateEdit**：日期选择控件
- ⭐⭐⭐⭐ **对话框模式**：添加 vs 编辑
- ⭐⭐⭐ **QTextEdit**：多行文本输入
- ⭐⭐⭐ **QDialogButtonBox**：标准按钮

---

## 💡 学习建议

1. 先设计 TaskDialog 的 UI 布局
2. 实现数据验证逻辑
3. 区分添加模式和编辑模式
4. 充分测试各种边界情况
5. 保持代码整洁和注释清晰

---

## ✅ 成功标准

- ✅ TaskDialog 对话框创建成功
- ✅ 可以添加新任务
- ✅ 可以编辑现有任务
- ✅ 数据验证正常工作
- ✅ 所有功能测试通过

---

**加油！今天完成核心功能开发！** 🚀
