# ============================================
# JavaScript 基础 - 超详细注释版（对比 Python）
# 日期：2026-06-10（第31天）
# 难度：⭐ 初级（前端三件套之"行为层"）
# ============================================

"""
【核心概念】简要说明

是什么？
- JavaScript（简称 JS）：运行在浏览器里的编程语言
- 它让网页"活"起来：点击按钮有反应、数据动态变化、表单实时校验

为什么需要？
- HTML 只能显示静态内容，CSS 只能美化外观
- 想让网页能"交互"（响应用户操作），必须用 JS

核心特点？
- 和 Python 一样是编程语言，有变量、函数、判断、循环
- 语法略有不同：用 let/const 声明变量、语句结尾加分号、用花括号 {}
- 唯一能在浏览器里直接运行的编程语言

【重要说明】
本文件是 Python 文件，用 print 展示 JS 代码作为教学对比。
真正写 JS 时，代码放在 HTML 的 <script> 标签里，或独立的 .js 文件。
文件末尾会生成一个可交互的 demo_js.html，用浏览器打开能点击看效果！
"""

# ============================================
# 第一部分：JS 核心概念 ⭐⭐⭐⭐⭐
# ============================================

print("=" * 60)
print("第一部分：JavaScript 核心概念")
print("=" * 60)

# --------------------------------------------
# 1.1 前端三件套：各司其职
# --------------------------------------------

print("\n1.1 HTML / CSS / JS 的分工")
print("-" * 60)

print("""
【前端三件套（终于集齐了！）】

HTML  →  结构层（网页有什么内容）   比喻：人的骨架
CSS   →  样式层（内容长什么样）     比喻：人的外貌衣着
JS    →  行为层（网页能做什么）     比喻：人的动作行为

【举个例子：一个"点赞"按钮】

HTML：<button>👍 点赞 (0)</button>   ← 定义按钮长什么样（结构）
CSS： button { color: red; }          ← 设置按钮颜色（样式）
JS：  点击后数字 +1，变成 (1)         ← 响应点击（行为）⭐

没有 JS，按钮就是个"死"的，点了没反应。
有了 JS，按钮才能真正"工作"。
""")

# --------------------------------------------
# 1.2 JS 代码写在哪里？
# --------------------------------------------

print("\n1.2 JS 代码放在哪里运行")
print("-" * 60)

print('''
【三种引入方式（和 CSS 很像）】

方式1：行内（写在标签里，不推荐）
    <button onclick="alert('你好')">点我</button>

方式2：内部（写在 <script> 标签里）
    <body>
        ...页面内容...
        <script>
            console.log("Hello JS");
        </script>
    </body>
    ⚠️ <script> 通常放在 </body> 前面（等页面加载完再执行 JS）

方式3：外部（独立 .js 文件，最推荐）⭐
    <script src="main.js"></script>

【两个最常用的调试/输出函数】

console.log("内容")   →  在浏览器控制台(F12)打印，调试用
                          类比 Python 的 print()

alert("内容")         →  弹出一个提示框
                          网页上会跳出对话框
''')

# ============================================
# 第二部分：变量与数据类型（对比 Python）⭐⭐⭐⭐⭐
# ============================================

print("\n" + "=" * 60)
print("第二部分：变量与数据类型")
print("=" * 60)

# --------------------------------------------
# 2.1 变量声明：let 和 const
# --------------------------------------------

print("\n2.1 变量声明 let / const ⭐⭐⭐⭐⭐")
print("-" * 60)

print('''
【JS 用 let 和 const 声明变量（Python 不需要关键字）】

JS 写法：
    let age = 18;            // let：可以修改的变量
    const name = "小明";      // const：常量，不能再改

Python 写法（对比）：
    age = 18                 # 直接赋值，无需关键字
    name = "小明"

【let vs const 怎么选？】

let   →  值以后会变（如计数器、用户输入）
const →  值固定不变（如圆周率、配置项）

经验法则：默认用 const，确实需要修改时才用 let。⭐

【关键差异表】

         JS                    Python
声明     let/const x = 值       x = 值（无关键字）
结尾     加分号 ;               不加分号
注释     // 单行  /* 多行 */    # 单行
''')

# --------------------------------------------
# 2.2 数据类型
# --------------------------------------------

print("\n2.2 JS 的数据类型（和 Python 对照）")
print("-" * 60)

print('''
【基本数据类型】

JS 类型          例子                Python 对应
─────────────────────────────────────────────
Number（数字）   let n = 42;         int / float（JS 不分整数小数）
String（字符串） let s = "你好";      str（单/双引号都行）
Boolean（布尔）  let b = true;       bool（注意：JS 是小写 true/false！）
Array（数组）    let a = [1, 2, 3];  list 列表
Object（对象）   let o = {age: 18};  dict 字典
null/undefined  空值                None

【最容易踩坑的对比】⚠️

布尔值：
    JS：     true / false   （小写）
    Python： True / False   （首字母大写）

空值：
    JS：     null（人为设空）/ undefined（未赋值）
    Python： None

字符串模板（拼接变量）：
    JS：     `你好，${name}`     ← 反引号 + ${变量}
    Python： f"你好，{name}"     ← f-string

【字符串模板示例】
    const name = "小明";
    const msg = `欢迎你，${name}！`;   // 反引号 ` 不是单引号 '
    // 结果："欢迎你，小明！"
    // 类比 Python 的 f"欢迎你，{name}！"
''')

# ============================================
# 第三部分：函数、判断、循环（对比 Python）⭐⭐⭐⭐⭐
# ============================================

print("\n" + "=" * 60)
print("第三部分：函数 / 判断 / 循环")
print("=" * 60)

# --------------------------------------------
# 3.1 函数
# --------------------------------------------

print("\n3.1 函数定义（三种写法）")
print("-" * 60)

print('''
【写法1：普通函数 function】
    function add(a, b) {        // function 关键字 + 花括号
        return a + b;
    }
    add(3, 5);  // 调用，得到 8

对比 Python：
    def add(a, b):              # def 关键字 + 冒号 + 缩进
        return a + b

【写法2：箭头函数（现代写法，最常用）⭐】
    const add = (a, b) => {
        return a + b;
    };
    // 单行可简写：const add = (a, b) => a + b;

【关键差异】
         JS                       Python
关键字   function 或 =>            def
代码块   用花括号 { }              用冒号 + 缩进
参数     (a, b)                   (a, b)
返回     return（同）             return（同）
''')

# --------------------------------------------
# 3.2 条件判断
# --------------------------------------------

print("\n3.2 条件判断 if-else")
print("-" * 60)

print('''
【JS 写法】
    let score = 85;
    if (score >= 90) {          // 条件用圆括号 () 包住
        console.log("优秀");
    } else if (score >= 60) {   // else if（不是 elif）
        console.log("及格");
    } else {
        console.log("不及格");
    }

【对比 Python】
    if score >= 90:             # 不用括号，用冒号
        print("优秀")
    elif score >= 60:           # elif（一个词）
        print("及格")
    else:
        print("不及格")

【差异】
判断条件：JS 要加 ()，Python 不用
分支关键字：JS 是 else if，Python 是 elif
逻辑运算：JS 用 && || !，Python 用 and or not
''')

# --------------------------------------------
# 3.3 循环
# --------------------------------------------

print("\n3.3 循环 for")
print("-" * 60)

print('''
【遍历数组：for...of（最常用）⭐】
    const fruits = ["苹果", "香蕉", "橙子"];
    for (const fruit of fruits) {   // for...of 遍历每一项
        console.log(fruit);
    }
    // 类比 Python：for fruit in fruits:

【计数循环：传统 for】
    for (let i = 0; i < 5; i++) {   // 初始;条件;步进
        console.log(i);             // 打印 0,1,2,3,4
    }
    // 类比 Python：for i in range(5):

【差异提醒】
JS 的传统 for 有三段：起点; 终点条件; 每次变化
i++ 是 i += 1 的简写（自增 1）
''')

# ============================================
# 第四部分：常见错误和陷阱 ⭐⭐⭐⭐⭐
# ============================================

print("\n" + "=" * 60)
print("第四部分：JavaScript 常见错误和陷阱")
print("=" * 60)

print("""
6个 Python 转 JS 最容易踩的坑：

❌ 错误1：布尔值大写
   错误：let ok = True;      （这是 Python 写法）
   原因：JS 的布尔值是小写 true / false
   ✅ 正确：let ok = true;

❌ 错误2：用 Python 的逻辑运算符
   错误：if (a and b)        （and 是 Python 的）
   原因：JS 用 && (与) || (或) ! (非)
   ✅ 正确：if (a && b)

❌ 错误3：忘记分号 / 花括号
   错误：if x > 5            （缺括号和花括号）
   原因：JS 条件要加 ()，代码块要加 {}
   ✅ 正确：if (x > 5) { ... }

❌ 错误4：用了未声明的变量
   错误：直接 age = 18;      （没有 let/const）
   原因：JS 推荐用 let/const 声明，否则易出 bug
   ✅ 正确：let age = 18;

❌ 错误5：== 和 === 混淆
   说明：JS 有两种相等比较
   ==   宽松比较（会自动转类型，"5" == 5 为 true）
   ===  严格比较（类型也要相同，"5" === 5 为 false）
   ✅ 建议：永远用 === （更安全，避免意外）⭐

❌ 错误6：字符串模板用错引号
   错误：let msg = "你好，{name}";   （双引号 + {} 无效）
   原因：模板字符串必须用反引号 ` 和 ${}
   ✅ 正确：let msg = `你好，${name}`;
""")

# ============================================
# 第五部分：JS 速查表 ⭐⭐⭐
# ============================================

print("\n" + "=" * 60)
print("第五部分：JavaScript 速查表")
print("=" * 60)

print('''
【语法对照速查（Python → JS）】

功能          Python              JavaScript
──────────────────────────────────────────────
声明变量      x = 5               let x = 5;  /  const x = 5;
打印          print(x)            console.log(x);
注释          # 注释              // 注释
函数          def f():            function f() {}  或  () => {}
判断          if x: elif: else:   if(){} else if(){} else{}
循环          for i in range(5)   for(let i=0;i<5;i++)
遍历          for x in arr        for(const x of arr)
逻辑          and / or / not      && / || / !
布尔          True / False        true / false
空值          None                null / undefined
字符串模板    f"{name}"           `${name}`
相等          ==                  === （严格，推荐）

【常用输出/交互】
console.log()   控制台打印（调试）
alert()         弹出提示框
prompt()        弹出输入框（返回用户输入）
confirm()       弹出确认框（返回 true/false）
''')

# ============================================
# 第六部分：练习题（不提供答案）
# ============================================

print("\n" + "=" * 60)
print("第六部分：动手练习题")
print("=" * 60)

print("""
用 JS 完成下面的题目（可以在浏览器 F12 控制台里试）：

1. 【变量与模板】
   声明一个 const 变量存你的名字，
   用 console.log 和模板字符串输出"我是XXX"。
   提示：反引号 + ${变量}

2. 【函数】
   写一个箭头函数 square(n)，返回 n 的平方。
   提示：const square = (n) => ...

3. 【判断】
   写一个函数 judge(score)，根据分数返回等级：
   >=90 返回"优秀"，>=60 返回"及格"，否则"不及格"。
   提示：注意用 () 包条件，{} 包代码块

4. 【循环】
   用 for...of 遍历数组 ["红","绿","蓝"]，逐个打印。
   提示：for (const color of 数组)

5. 【综合】
   写一个函数 sum(arr)，计算数组所有数字之和。
   提示：let total = 0; 循环累加

6. 【思考题】
   下面两行输出什么？为什么？
   console.log("5" == 5);    // ?
   console.log("5" === 5);   // ?
   提示：复习 == 和 === 的区别
""")

print("\n" + "=" * 60)
print("✅ JavaScript 基础学习完毕！")
print("下一步：DOM 操作 —— 用 JS 真正去操控网页元素")
print("=" * 60)
