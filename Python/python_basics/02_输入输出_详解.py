# ============================================
# 输入输出 - 超详细注释版
# 日期：2026-02-11（第1天）
# 难度：⭐ 初级
# ============================================

"""
【核心概念】输入输出

是什么？
- 输出（print）：程序向用户显示信息
- 输入（input）：程序从用户获取信息

为什么需要？
- 程序需要和用户交互
- 显示结果、获取数据、提示信息

核心特点？
- print()可以输出任何类型的数据
- input()总是返回字符串类型
- 可以格式化输出，让信息更美观
"""

# ============================================
# 第一部分：核心概念详解 ⭐⭐⭐⭐⭐
# ============================================

print("=" * 60)
print("第一部分：核心概念详解")
print("=" * 60)

# --------------------------------------------
# 1.1 什么是输出？
# --------------------------------------------

print("\n1.1 什么是输出？")
print("-" * 60)

print("""
【输出（print）】

是什么？
- 将信息显示在屏幕上
- 就像程序在"说话"

为什么需要？
- 显示计算结果
- 提示用户信息
- 调试程序（查看变量值）

如何使用？
- 格式：print(要输出的内容)
- 例如：print("你好")
""")

# --------------------------------------------
# 1.2 什么是输入？
# --------------------------------------------

print("\n1.2 什么是输入？")
print("-" * 60)

print("""
【输入（input）】

是什么？
- 让用户输入信息
- 程序会等待用户输入并按回车

为什么需要？
- 获取用户的数据
- 让程序更灵活、更互动

如何使用？
- 格式：变量 = input("提示信息")
- 例如：name = input("请输入你的姓名：")

⚠️ 重要：input()总是返回字符串！
""")

# --------------------------------------------
# 1.3 重要函数：print() ⭐⭐⭐⭐⭐
# --------------------------------------------

print("\n1.3 print()函数详解")
print("-" * 60)

print("""
【函数卡片】

函数名：print()
作用：在屏幕上显示信息
类型：内置函数

完整语法：
print(值1, 值2, ..., sep=' ', end='\\n')

参数详解：
1. 值（可选，可多个）
   - 类型：任何类型
   - 作用：要输出的内容
   - 示例：print("hello", 123)

2. sep（可选）
   - 类型：字符串
   - 作用：多个值之间的分隔符
   - 默认：空格
   - 示例：print("a", "b", sep="-")  # 输出：a-b

3. end（可选）
   - 类型：字符串
   - 作用：输出结束后添加的字符
   - 默认：换行符\\n
   - 示例：print("hello", end="!")  # 输出：hello!

返回值：
- 无（None）

使用示例：
```python
# 基本用法
print("你好")

# 输出多个值
print("姓名：", "张三")

# 自定义分隔符
print("a", "b", "c", sep="-")  # a-b-c
```
""")

# --------------------------------------------
# 1.4 重要函数：input() ⭐⭐⭐⭐⭐
# --------------------------------------------

print("\n1.4 input()函数详解")
print("-" * 60)

print("""
【函数卡片】

函数名：input()
作用：获取用户输入
类型：内置函数

完整语法：
input(提示信息)

参数详解：
1. 提示信息（可选）
   - 类型：字符串
   - 作用：显示给用户的提示
   - 示例：input("请输入姓名：")

返回值：
- 字符串类型（⚠️ 重要！）
- 用户输入的内容

使用示例：
```python
# 基本用法
name = input("请输入你的姓名：")

# 获取数字（需要转换）
age = int(input("请输入你的年龄："))
```

⚠️ 关键点：
- input()总是返回字符串
- 如果需要数字，必须用int()或float()转换
""")

# ============================================
# 第二部分：基础操作实践
# ============================================

print("\n" + "=" * 60)
print("第二部分：基础操作实践")
print("=" * 60)

# --------------------------------------------
# 2.1 基本输出
# --------------------------------------------

print("\n2.1 基本输出")
print("-" * 60)

# 输出字符串
# 参数：要显示的文字
print("欢迎学习Python！")

# 输出数字
# 参数：数字（不需要引号）
print(123)
print(3.14)

# 输出变量
# 参数：变量名
name = "张三"
age = 25
print(name)
print(age)

# --------------------------------------------
# 2.2 输出多个值
# --------------------------------------------

print("\n2.2 输出多个值")
print("-" * 60)

# 输出多个值，用逗号分隔
# print会自动在它们之间加空格
name = "李四"
age = 30
print("姓名：", name, "年龄：", age)

# 自定义分隔符
# 参数sep：指定分隔符
print("2026", "02", "11", sep="-")  # 输出：2026-02-11
print("a", "b", "c", sep="")        # 输出：abc（无分隔）

# --------------------------------------------
# 2.3 字符串拼接
# --------------------------------------------

print("\n2.3 字符串拼接")
print("-" * 60)

# 方法1：使用+号拼接
# 注意：只能拼接字符串
first_name = "张"
last_name = "三"
full_name = first_name + last_name
print("全名：" + full_name)

# 方法2：使用逗号（print自动转换类型）
age = 25
print("年龄：", age)  # 逗号会自动处理不同类型

# ❌ 错误示例（会报错，所以注释掉）
# print("年龄：" + age)  # 不能直接拼接字符串和数字

# ✅ 正确做法：转换为字符串
print("年龄：" + str(age))

# --------------------------------------------
# 2.4 格式化输出（f-string）⭐⭐⭐⭐⭐
# --------------------------------------------

print("\n2.4 格式化输出（f-string）⭐⭐⭐⭐⭐")
print("-" * 60)

# f-string：最推荐的格式化方式
# 格式：f"文字{变量}文字"
# 特点：简洁、直观、强大

name = "王五"
age = 28
height = 1.75

# 在字符串前加f，用{}包裹变量
print(f"姓名：{name}")
print(f"年龄：{age}岁")
print(f"身高：{height}米")

# 可以在{}中进行计算
print(f"明年年龄：{age + 1}岁")
print(f"身高（厘米）：{height * 100}cm")

# 可以包含多个变量
print(f"{name}今年{age}岁，身高{height}米")

# --------------------------------------------
# 2.5 基本输入
# --------------------------------------------

print("\n2.5 基本输入")
print("-" * 60)

# 获取用户输入
# input()会显示提示信息，等待用户输入
# 用户输入后按回车，input()返回输入的内容（字符串）

print("【演示：获取姓名】")
# user_name = input("请输入你的姓名：")
# print(f"你好，{user_name}！")

# 注意：为了让这个文件能自动运行，上面的代码被注释了
# 你可以取消注释来测试

# --------------------------------------------
# 2.6 输入数字（类型转换）⭐⭐⭐⭐⭐
# --------------------------------------------

print("\n2.6 输入数字（类型转换）⭐⭐⭐⭐⭐")
print("-" * 60)

print("""
⚠️ 重要概念：input()总是返回字符串！

如果需要数字，必须转换：
- int()：转换为整数
- float()：转换为浮点数

示例：
```python
# 错误做法
age = input("请输入年龄：")  # age是字符串"25"
next_year = age + 1  # 报错！字符串不能加数字

# 正确做法
age = int(input("请输入年龄："))  # 转换为整数25
next_year = age + 1  # 正确！26
```
""")

# 演示（注释掉，避免等待输入）
# age_str = input("请输入你的年龄：")
# age = int(age_str)  # 转换为整数
# print(f"明年你将{age + 1}岁")

# 简写形式（推荐）
# age = int(input("请输入你的年龄："))
# print(f"明年你将{age + 1}岁")

# --------------------------------------------
# 2.7 控制输出格式
# --------------------------------------------

print("\n2.7 控制输出格式")
print("-" * 60)

# 使用end参数控制结尾
# 默认end='\n'（换行）
print("第一行", end="")  # 不换行
print("第二行")           # 输出：第一行第二行

print("A", end=" -> ")
print("B", end=" -> ")
print("C")  # 输出：A -> B -> C

# 使用\n手动换行
print("第一行\n第二行\n第三行")

# 使用\t制表符（Tab）
print("姓名\t年龄\t城市")
print("张三\t25\t北京")
print("李四\t30\t上海")

# ============================================
# 🔍 深入理解：为什么input()返回字符串？（选学）
# ============================================

print("\n" + "=" * 60)
print("🔍 深入理解：为什么input()返回字符串？（选学）")
print("=" * 60)

print("""
如果你想知道"为什么"，可以阅读这部分。
如果觉得难，可以跳过，不影响后续学习。

【为什么input()总是返回字符串？】

原因：
1. 用户可能输入任何内容
   - 数字："123"
   - 文字："hello"
   - 混合："abc123"

2. Python无法自动判断用户想输入什么类型
   - 输入"123"，是数字123还是字符串"123"？
   - 只有程序员知道答案

3. 统一返回字符串最安全
   - 程序员根据需要自己转换
   - 避免自动转换出错

类比：
- 就像快递员送包裹，不管里面是什么，都用纸箱包装
- 收件人（程序员）自己打开，取出需要的东西

实际应用：
```python
# 获取年龄（需要数字）
age = int(input("年龄："))

# 获取姓名（需要字符串）
name = input("姓名：")  # 不需要转换

# 获取身高（需要小数）
height = float(input("身高："))
```
""")

# ============================================
# 第三部分：常见错误和陷阱 ⭐⭐⭐⭐⭐
# ============================================

print("\n" + "=" * 60)
print("第三部分：常见错误和陷阱 ⭐⭐⭐⭐⭐")
print("=" * 60)

# --------------------------------------------
# 3.1 错误1：忘记转换input()的类型
# --------------------------------------------

print("\n3.1 错误1：忘记转换input()的类型")
print("-" * 60)

print("""
❌ 错误代码：
```python
age = input("请输入年龄：")  # 用户输入：25
next_year = age + 1  # 想计算明年年龄
```

结果：TypeError: can only concatenate str (not "int") to str

原因：age是字符串"25"，不能直接加数字

✅ 正确代码：
```python
age = int(input("请输入年龄："))  # 转换为整数
next_year = age + 1  # 正确！
```

记忆技巧：input()返回的是"文字"，要算数必须转成"数字"
""")

# --------------------------------------------
# 3.2 错误2：字符串和数字拼接
# --------------------------------------------

print("\n3.2 错误2：字符串和数字拼接")
print("-" * 60)

print("""
❌ 错误代码：
```python
age = 25
print("年龄：" + age)
```

结果：TypeError: can only concatenate str (not "int") to str

原因：+号两边必须是相同类型

✅ 正确代码：
```python
# 方法1：转换为字符串
print("年龄：" + str(age))

# 方法2：使用逗号
print("年龄：", age)

# 方法3：使用f-string（推荐）
print(f"年龄：{age}")
```

记忆技巧：+号像胶水，只能粘相同材质的东西
""")

# --------------------------------------------
# 3.3 错误3：print()中的引号问题
# --------------------------------------------

print("\n3.3 错误3：print()中的引号问题")
print("-" * 60)

print("""
❌ 错误代码：
```python
print("他说："你好"")  # 引号冲突
```

结果：SyntaxError: invalid syntax

原因：内外引号冲突

✅ 正确代码：
```python
# 方法1：外层用双引号，内层用单引号
print("他说：'你好'")

# 方法2：外层用单引号，内层用双引号
print('他说："你好"')

# 方法3：使用转义字符
print("他说：\\"你好\\"")
```

记忆技巧：引号要"错开"，不能"撞车"
""")

# --------------------------------------------
# 3.4 错误4：int()转换非数字字符串
# --------------------------------------------

print("\n3.4 错误4：int()转换非数字字符串")
print("-" * 60)

print("""
❌ 错误代码：
```python
age = int(input("请输入年龄："))  # 用户输入：abc
```

结果：ValueError: invalid literal for int() with base 10: 'abc'

原因：int()只能转换数字字符串

✅ 正确做法：
```python
# 方法1：提示用户输入数字
age = int(input("请输入年龄（数字）："))

# 方法2：使用异常处理（后续会学）
try:
    age = int(input("请输入年龄："))
except ValueError:
    print("请输入有效的数字！")
```

记忆技巧：int()像是"数字翻译器"，只能翻译数字
""")

# --------------------------------------------
# 3.5 错误5：忘记print()的括号
# --------------------------------------------

print("\n3.5 错误5：忘记print()的括号")
print("-" * 60)

print("""
❌ 错误代码：
```python
print "hello"  # Python 2的写法
```

结果：SyntaxError: Missing parentheses in call to 'print'

原因：Python 3中print是函数，必须用括号

✅ 正确代码：
```python
print("hello")  # Python 3的写法
```

记忆技巧：Python 3中，print后面必须跟()
""")

# --------------------------------------------
# 3.6 错误6：f-string忘记前缀f
# --------------------------------------------

print("\n3.6 错误6：f-string忘记前缀f")
print("-" * 60)

print("""
❌ 错误代码：
```python
name = "张三"
print("姓名：{name}")  # 忘记f前缀
```

结果：姓名：{name}（原样输出，变量没有替换）

原因：没有f前缀，{}不会被识别为变量

✅ 正确代码：
```python
name = "张三"
print(f"姓名：{name}")  # 记得加f
```

记忆技巧：f-string的f代表"format"（格式化）
""")

# --------------------------------------------
# 3.7 错误7：input()提示信息不清晰
# --------------------------------------------

print("\n3.7 错误7：input()提示信息不清晰")
print("-" * 60)

print("""
❌ 不好的做法：
```python
age = int(input())  # 没有提示
name = input("输入")  # 提示不清楚
```

结果：用户不知道该输入什么

✅ 好的做法：
```python
age = int(input("请输入你的年龄（数字）："))
name = input("请输入你的姓名：")
```

记忆技巧：
- 提示要清晰明确
- 说明输入格式（如"数字"）
- 结尾加冒号和空格，更友好
""")

# ============================================
# 第四部分：函数卡片速查
# ============================================

print("\n" + "=" * 60)
print("第四部分：函数卡片速查")
print("=" * 60)

print("""
【函数1：print()】

语法：print(值1, 值2, ..., sep=' ', end='\\n')
参数：
  - 值: 要输出的内容（可多个）
  - sep: 分隔符（默认空格）
  - end: 结尾字符（默认换行）
返回：无

示例：
```python
print("hello")
print("a", "b", sep="-")  # a-b
```

────────────────────────────────────

【函数2：input()】

语法：input(提示信息)
参数：
  - 提示信息: 显示给用户的文字（可选）
返回：字符串（⚠️ 总是字符串！）

示例：
```python
name = input("请输入姓名：")
age = int(input("请输入年龄："))  # 需要转换
```

────────────────────────────────────

【函数3：int()】

语法：int(对象)
参数：
  - 对象: 要转换为整数的数据
返回：整数

示例：
```python
int("123")  # 123
int(3.14)   # 3
```

────────────────────────────────────

【函数4：str()】

语法：str(对象)
参数：
  - 对象: 要转换为字符串的数据
返回：字符串

示例：
```python
str(123)  # "123"
```
""")

# ============================================
# 第五部分：练习题
# ============================================

print("\n" + "=" * 60)
print("第五部分：练习题")
print("=" * 60)

print("""
1. 基本输出练习
   要求：
   - 使用print()输出你的姓名
   - 使用print()输出你的年龄
   - 使用print()同时输出姓名和年龄
   提示：可以用逗号分隔多个值

2. f-string练习
   要求：
   - 创建变量：name = "你的姓名", age = 你的年龄
   - 使用f-string输出："我叫XXX，今年XX岁"
   - 使用f-string计算并输出明年的年龄
   提示：f"文字{变量}文字"

3. 输入练习（取消注释来测试）
   要求：
   - 获取用户的姓名（字符串）
   - 获取用户的年龄（整数）
   - 输出："你好，XXX！你今年XX岁"
   提示：年龄需要用int()转换

4. 计算练习
   要求：
   - 获取用户输入的两个数字
   - 计算它们的和、差、积、商
   - 输出所有结果
   提示：使用int()或float()转换

5. 综合练习
   要求：
   - 获取用户的姓名、出生年份
   - 计算用户的年龄（2026 - 出生年份）
   - 输出："XXX，你今年XX岁"
   提示：出生年份需要转换为整数

示例代码框架：
```python
# 练习3示例
# name = input("请输入你的姓名：")
# age = int(input("请输入你的年龄："))
# print(f"你好，{name}！你今年{age}岁")
```
""")
# 1
print(f"myname: yanc")
print(f"myage： 25")
print("name：yanc, age:25")

# 2
name1 = "yanc"
age1 = 25
print(f"name: {name1}, age: {age1}")
print(f"明年的年龄：{age1}")

# 3
name2 = input("输入Name")
age2 = int(input("输入年龄"))
print(f"你好，{name2}！你今年{age2}岁")


print("\n" + "=" * 60)
print("🎉 第1天学习材料2完成！")
print("建议：取消注释input()相关代码，亲自测试输入输出")
print("下一步：完成项目 03_个人信息收集器.py")
print("=" * 60)
