# 📝 留言板项目（前后端联动）

> 第33天实战项目 - 第一次打通前后端！

## 📖 项目介绍

一个简单的留言板，演示**前后端分离**的完整数据交互：
- **后端**（Flask）：提供留言的获取和提交 API
- **前端**（HTML/CSS/JS）：用 fetch 调用后端，动态显示留言

## 🗂️ 文件结构

```
留言板项目/
├── app.py        # 后端 Flask 服务（提供 API）
├── index.html    # 前端页面（用 fetch 调用）
└── README.md     # 本说明文档
```

## 🔌 API 接口

| 方法 | 路径 | 功能 | 请求数据 | 返回数据 |
|------|------|------|---------|---------|
| GET | `/api/messages` | 获取所有留言 | 无 | 留言数组 |
| POST | `/api/messages` | 提交新留言 | `{name, content}` | 成功状态 |

## 🚀 运行步骤

### 第1步：安装依赖

```bash
pip install flask flask-cors
```

### 第2步：启动后端

```bash
cd 留言板项目
python app.py
```

看到提示：
```
留言板后端服务启动中...
访问地址：http://localhost:5000
```

### 第3步：打开前端

直接双击 `index.html`，或用浏览器打开。

### 第4步：测试功能

1. ✅ 页面自动加载已有留言
2. ✅ 输入名字和内容，点击"发表留言"
3. ✅ 留言立即显示在列表顶部（无需刷新页面）

## 🔍 数据流转图

```
┌─────────────────────────────────────────────┐
│  用户在前端输入留言，点击"发表留言"            │
└──────────────────┬──────────────────────────┘
                   │ fetch POST
                   │ body: {name, content}
                   ↓
┌─────────────────────────────────────────────┐
│  Flask 后端接收（request.get_json）           │
│  保存到 messages 列表                          │
│  返回成功状态（jsonify）                       │
└──────────────────┬──────────────────────────┘
                   │ 提交成功后
                   │ 前端再次 fetch GET
                   ↓
┌─────────────────────────────────────────────┐
│  Flask 返回所有留言（jsonify）                 │
│  前端用 DOM 渲染显示                           │
└─────────────────────────────────────────────┘
```

## 💡 核心知识点

### 后端（Flask）
- `jsonify()`：返回 JSON 数据
- `request.get_json()`：获取前端 POST 数据
- `CORS(app)`：解决跨域问题
- `methods=['GET', 'POST']`：指定请求方法

### 前端（fetch）
- `fetch(url)`：GET 请求获取数据
- `fetch(url, {method, headers, body})`：POST 提交数据
- `JSON.stringify()`：对象转 JSON 字符串
- `response.json()`：解析后端返回的 JSON

## 🎯 可以扩展的功能

学有余力可以尝试：
- [ ] 添加删除留言功能（DELETE 接口）
- [ ] 留言数据保存到文件（JSON）或数据库（SQLite）
- [ ] 添加点赞功能
- [ ] 留言分页显示
- [ ] 输入内容长度限制和敏感词过滤

## ⚠️ 常见问题

**Q：点击按钮没反应，控制台报 CORS 错误？**
A：确认后端 `app.py` 有 `CORS(app)`，并已安装 flask-cors。

**Q：提示"加载失败"？**
A：确认后端已启动（运行 `python app.py`），端口是 5000。

**Q：留言重启后消失了？**
A：正常，数据存在内存列表中。扩展功能可以保存到文件。

---

**项目完成日期**：2026-06-14（第33天）
**技术栈**：Flask + flask-cors + HTML/CSS/JavaScript + fetch
