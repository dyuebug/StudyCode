# ============================================
# 变量和数据类型 - 超详细注释版
# 日期：2026-02-11（第1天）
# 难度：⭐ 初级
# ============================================

"""
【核心概念】变量和数据类型

是什么？
- 变量：存储数据的容器，就像一个带标签的盒子
- 数据类型：数据的种类，决定了数据能做什么操作

为什么需要？
- 程序需要记住和处理各种信息（数字、文字等）
- 不同类型的数据有不同的用途和操作方式

核心特点？
- Python会自动识别数据类型（动态类型）
- 变量可以随时改变存储的值
- 同一个变量可以存储不同类型的数据
"""

# ============================================
# 第一部分：核心概念详解 ⭐⭐⭐⭐⭐
# ============================================

print("=" * 60)
print("第一部分：核心概念详解")
print("=" * 60)

# --------------------------------------------
# 1.1 什么是变量？
# --------------------------------------------

print("\n1.1 什么是变量？")
print("-" * 60)

print("""
【变量】

是什么？
- 变量就像一个带标签的盒子，用来存储数据
- 标签就是变量名，盒子里装的就是数据

为什么需要？
- 程序需要记住信息，比如用户的姓名、年龄
- 需要对数据进行计算和处理

如何使用？
- 格式：变量名 = 值
- 例如：name = "张三"
""")

# --------------------------------------------
# 1.2 Python的4种基本数据类型
# --------------------------------------------

print("\n1.2 Python的4种基本数据类型")
print("-" * 60)

print("""
【数据类型】

1. 整数（int）
   - 没有小数点的数字
   - 例如：1, 100, -5

2. 浮点数（float）
   - 有小数点的数字
   - 例如：3.14, -0.5, 2.0

3. 字符串（str）
   - 文字内容，用引号包裹
   - 例如："你好", 'Python', "123"

4. 布尔值（bool）
   - 只有两个值：True（真）或 False（假）
   - 用于判断条件
""")

# --------------------------------------------
# 1.3 重要函数：type() ⭐⭐⭐⭐⭐
# --------------------------------------------

print("\n1.3 type()函数详解")
print("-" * 60)

print("""
【函数卡片】

函数名：type()
作用：查看数据的类型
类型：内置函数

完整语法：
type(对象)

参数详解：
1. 对象（必需）
   - 类型：任何Python对象
   - 作用：要查看类型的数据
   - 示例：type(123)

返回值：
- 返回该对象的数据类型

使用示例：
```python
# 查看整数类型
type(100)  # 返回：<class 'int'>

# 查看字符串类型
type("hello")  # 返回：<class 'str'>
```
""")

# ============================================
# 第二部分：基础操作实践
# ============================================

print("\n" + "=" * 60)
print("第二部分：基础操作实践")
print("=" * 60)

# --------------------------------------------
# 2.1 创建变量
# --------------------------------------------

print("\n2.1 创建变量")
print("-" * 60)

# 创建一个整数变量
# 变量名：age
# 值：25
age = 25
print(f"年龄：{age}")

# 创建一个字符串变量
# 变量名：name
# 值："张三"
name = "张三"
print(f"姓名：{name}")

# 创建一个浮点数变量
# 变量名：height
# 值：1.75（单位：米）
height = 1.75
print(f"身高：{height}米")

# 创建一个布尔值变量
# 变量名：is_student
# 值：True（表示是学生）
is_student = True
print(f"是否是学生：{is_student}")

# --------------------------------------------
# 2.2 查看变量类型
# --------------------------------------------

print("\n2.2 查看变量类型")
print("-" * 60)

# 使用type()函数查看变量类型
# 参数：变量名
# 返回：该变量的数据类型
print(f"age的类型：{type(age)}")           # <class 'int'>
print(f"name的类型：{type(name)}")         # <class 'str'>
print(f"height的类型：{type(height)}")     # <class 'float'>
print(f"is_student的类型：{type(is_student)}")  # <class 'bool'>

# --------------------------------------------
# 2.3 变量可以重新赋值
# --------------------------------------------

print("\n2.3 变量可以重新赋值")
print("-" * 60)

# 创建变量x，初始值为10
x = 10
print(f"x的初始值：{x}，类型：{type(x)}")

# 重新赋值为20
# 变量x现在存储新的值
x = 20
print(f"x的新值：{x}，类型：{type(x)}")

# 甚至可以改变类型！
# 将x从整数改为字符串
x = "hello"
print(f"x的新值：{x}，类型：{type(x)}")

# --------------------------------------------
# 2.4 变量命名规则
# --------------------------------------------

print("\n2.4 变量命名规则")
print("-" * 60)

# ✅ 正确的变量名
user_name = "李四"      # 使用下划线连接
age2 = 30              # 可以包含数字（但不能开头）
_private = "私有"      # 可以以下划线开头

print(f"user_name: {user_name}")
print(f"age2: {age2}")
print(f"_private: {_private}")

# ❌ 错误的变量名（这些会报错，所以注释掉）
# 2age = 30            # 不能以数字开头
# user-name = "王五"   # 不能使用连字符
# for = 10             # 不能使用Python关键字

print("""
变量命名规则：
✅ 只能包含字母、数字、下划线
✅ 不能以数字开头
✅ 区分大小写（age和Age是不同的变量）
✅ 不能使用Python关键字（如if、for、while等）
✅ 建议使用有意义的名字（如user_age而不是x）
""")

# ============================================
# 🔍 深入理解：为什么Python不需要声明类型？（选学）
# ============================================

print("\n" + "=" * 60)
print("🔍 深入理解：为什么Python不需要声明类型？（选学）")
print("=" * 60)

print("""
如果你想知道"为什么"，可以阅读这部分。
如果觉得难，可以跳过，不影响后续学习。

【动态类型 vs 静态类型】

Python是动态类型语言：
- 不需要提前声明变量类型
- Python会自动识别
- 例如：x = 10  # Python自动知道这是整数

其他语言（如Java）是静态类型：
- 必须提前声明类型
- 例如：int x = 10;  # 必须写明int

优点：
- 写代码更快，更灵活
- 适合快速开发和学习

缺点：
- 可能出现类型错误（运行时才发现）
- 大型项目需要更小心

对于初学者：
- 现在只需要知道Python会自动处理类型
- 专注于学习如何使用变量即可
""")

# ============================================
# 第三部分：常见错误和陷阱 ⭐⭐⭐⭐⭐
# ============================================

print("\n" + "=" * 60)
print("第三部分：常见错误和陷阱 ⭐⭐⭐⭐⭐")
print("=" * 60)

# --------------------------------------------
# 3.1 错误1：使用未定义的变量
# --------------------------------------------

print("\n3.1 错误1：使用未定义的变量")
print("-" * 60)

print("""
❌ 错误代码：
```python
print(score)  # score还没有定义
```

结果：NameError: name 'score' is not defined

原因：变量必须先定义（赋值）才能使用

✅ 正确代码：
```python
score = 90  # 先定义
print(score)  # 再使用
```

记忆技巧：先有盒子，才能取东西
""")

# --------------------------------------------
# 3.2 错误2：变量名以数字开头
# --------------------------------------------

print("\n3.2 错误2：变量名以数字开头")
print("-" * 60)

print("""
❌ 错误代码：
```python
1st_place = "金牌"
```

结果：SyntaxError: invalid syntax

原因：变量名不能以数字开头

✅ 正确代码：
```python
first_place = "金牌"
place_1 = "金牌"
```

记忆技巧：变量名要像人名，不能以数字开头
""")

# --------------------------------------------
# 3.3 错误3：混淆字符串和数字
# --------------------------------------------

print("\n3.3 错误3：混淆字符串和数字")
print("-" * 60)

print("""
❌ 错误代码：
```python
age = "25"  # 这是字符串，不是数字
next_year = age + 1  # 字符串不能直接加数字
```

结果：TypeError: can only concatenate str (not "int") to str

原因：字符串"25"和数字25是不同的类型

✅ 正确代码：
```python
age = 25  # 数字
next_year = age + 1  # 26

# 或者转换类型
age = "25"  # 字符串
next_year = int(age) + 1  # 先转换为整数
```

记忆技巧：带引号的是字符串，不带引号的是数字
""")

# --------------------------------------------
# 3.4 错误4：变量名区分大小写
# --------------------------------------------

print("\n3.4 错误4：变量名区分大小写")
print("-" * 60)

print("""
❌ 错误代码：
```python
Name = "张三"
print(name)  # 注意大小写不同
```

结果：NameError: name 'name' is not defined

原因：Name和name是两个不同的变量

✅ 正确代码：
```python
Name = "张三"
print(Name)  # 大小写一致

# 或者统一使用小写
name = "张三"
print(name)
```

记忆技巧：Python认为Name和name是两个人
""")

# --------------------------------------------
# 3.5 错误5：使用Python关键字作为变量名
# --------------------------------------------

print("\n3.5 错误5：使用Python关键字作为变量名")
print("-" * 60)

print("""
❌ 错误代码：
```python
for = 10  # for是Python关键字
if = 20   # if是Python关键字
```

结果：SyntaxError: invalid syntax

原因：Python保留了一些关键字，不能用作变量名

✅ 正确代码：
```python
count = 10
condition = 20
```

常见关键字：if, for, while, def, class, return, True, False等

记忆技巧：避免使用看起来像Python命令的词
""")

# --------------------------------------------
# 3.6 错误6：整数除法的小数问题
# --------------------------------------------

print("\n3.6 错误6：整数除法的小数问题")
print("-" * 60)

print("""
⚠️ 注意：Python 3中的除法

在Python 3中：
- / 总是返回浮点数
- // 返回整数（向下取整）

示例：
```python
print(10 / 3)   # 3.3333333333333335（浮点数）
print(10 // 3)  # 3（整数）
```

记忆技巧：
- 一个斜杠 / = 普通除法（有小数）
- 两个斜杠 // = 整数除法（去掉小数）
""")

# 实际演示
print("实际演示：")
print(f"10 / 3 = {10 / 3}")
print(f"10 // 3 = {10 // 3}")

# --------------------------------------------
# 3.7 错误7：浮点数精度问题
# --------------------------------------------

print("\n3.7 错误7：浮点数精度问题")
print("-" * 60)

print("""
⚠️ 注意：浮点数不是完全精确的

问题：
```python
print(0.1 + 0.2)  # 期望：0.3，实际：0.30000000000000004
```

原因：计算机用二进制存储小数，有些小数无法精确表示

解决方法：
1. 对于金钱计算，使用整数（分为单位）
2. 使用round()函数四舍五入
3. 使用decimal模块（高级）

记忆技巧：浮点数像是用尺子量东西，总有微小误差
""")

# 实际演示
print("实际演示：")
print(f"0.1 + 0.2 = {0.1 + 0.2}")
print(f"round(0.1 + 0.2, 1) = {round(0.1 + 0.2, 1)}")

# ============================================
# 第四部分：函数卡片速查
# ============================================

print("\n" + "=" * 60)
print("第四部分：函数卡片速查")
print("=" * 60)

print("""
【函数1：type()】

语法：type(对象)
参数：
  - 对象: 要查看类型的数据
返回：该对象的数据类型

示例：
```python
type(123)      # <class 'int'>
type("hello")  # <class 'str'>
```

────────────────────────────────────

【函数2：int()】

语法：int(对象)
参数：
  - 对象: 要转换为整数的数据
返回：整数

示例：
```python
int("123")   # 123
int(3.14)    # 3（去掉小数部分）
```

────────────────────────────────────

【函数3：float()】

语法：float(对象)
参数：
  - 对象: 要转换为浮点数的数据
返回：浮点数

示例：
```python
float("3.14")  # 3.14
float(5)       # 5.0
```

────────────────────────────────────

【函数4：str()】

语法：str(对象)
参数：
  - 对象: 要转换为字符串的数据
返回：字符串

示例：
```python
str(123)    # "123"
str(3.14)   # "3.14"
```
""")

# ============================================
# 第五部分：练习题
# ============================================

print("\n" + "=" * 60)
print("第五部分：练习题")
print("=" * 60)

print("""
1. 创建变量练习
   要求：创建以下变量
   - 你的姓名（字符串）
   - 你的年龄（整数）
   - 你的身高（浮点数，单位：米）
   - 你是否是学生（布尔值）
   提示：使用有意义的变量名

2. 类型转换练习
   要求：
   - 创建字符串变量 num_str = "100"
   - 将它转换为整数并加上50
   - 打印结果
   提示：使用int()函数

3. 变量重新赋值练习
   要求：
   - 创建变量x = 10
   - 将x的值改为x + 5
   - 再将x的值改为x * 2
   - 打印最终的x值
   提示：x = x + 5 可以简写为 x += 5

4. 类型判断练习
   要求：
   - 创建变量 a = 3.14
   - 创建变量 b = "3.14"
   - 使用type()函数查看它们的类型
   - 思考：它们有什么区别？
   提示：一个有引号，一个没有

5. 综合练习
   要求：
   - 创建变量存储你的出生年份（整数）
   - 创建变量存储当前年份（2026）
   - 计算你的年龄
   - 打印结果
   提示：年龄 = 当前年份 - 出生年份
""")

# 1
myname = "yanc"
age = 25
height = 1.8
weight = 70
is_student = True

# 2
num_str = "100"
sum = int(num_str) + 50
print(sum)

# 3
x = 10
x = x + 5
x = x * 2
print(x)

# 4
a = 3.21
b = "3.21"
print("type(a)")
print(type(a))
print(type(b))

# 5
age_years = 2001
cur_years = 2026
myage = cur_years - age_years
print(myage)

print("\n" + "=" * 60)
print("🎉 第1天学习材料1完成！")
print("建议：运行这个文件，观察输出，理解每个概念")
print("下一步：学习 02_输入输出_详解.py")
print("=" * 60)
