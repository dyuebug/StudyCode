# ============================================
# while循环 - 超详细注释版
# 日期：2026-03-16（第6天）
# 难度：⭐⭐ 中级
# ============================================

"""
【核心概念】while循环 - 条件控制的重复执行

是什么？
- while循环是一种根据条件重复执行代码的结构
- 只要条件为True，就一直执行循环体

为什么需要？
- for循环适合遍历已知序列
- while循环适合不知道要循环多少次，只知道停止条件的情况
- 例如：用户输入验证、游戏循环、等待某个事件发生

核心特点？
- 条件控制：根据条件决定是否继续循环
- 次数不定：循环次数由条件决定，可能是0次、有限次或无限次
- 灵活性高：适合复杂的循环逻辑
"""

# ============================================
# 第一部分：核心概念详解 ⭐⭐⭐⭐⭐
# ============================================

print("=" * 60)
print("第一部分：核心概念详解")
print("=" * 60)

# --------------------------------------------
# 1.1 while循环是什么？
# --------------------------------------------

print("\n1.1 while循环的概念")
print("-" * 60)

print("""
【while循环】

是什么？
- 一种根据条件重复执行代码的结构
- 只要条件为True，就一直执行循环体

为什么需要？
- 问题：如何让用户一直输入，直到输入正确为止？
- 解决：用while循环，条件不满足就一直循环！

基本语法：
while 条件:
    代码块

执行流程：
1. 判断条件是否为True
2. 如果为True，执行代码块
3. 再次判断条件
4. 如果为True，再次执行代码块
5. 重复，直到条件为False

与for循环的区别：
- for循环：适合遍历已知序列，次数确定
- while循环：适合条件控制，次数不定
""")

# --------------------------------------------
# 1.2 重要关键字：break ⭐⭐⭐⭐⭐
# --------------------------------------------

print("\n1.2 break关键字详解")
print("-" * 60)

print("""
【关键字卡片】

关键字：break
作用：立即跳出当前循环（整个循环结束）
类型：循环控制语句

使用场景：
- 找到目标后，不需要继续循环
- 遇到错误，需要提前退出
- 用户选择退出

执行效果：
- 立即终止当前循环
- 跳到循环后的第一条语句
- 如果是嵌套循环，只跳出最内层循环

使用示例：
```python
# 查找列表中的目标值
numbers = [1, 2, 3, 4, 5]
target = 3

for num in numbers:
    if num == target:
        print(f"找到了：{num}")
        break  # 找到后立即退出循环
    print(f"检查：{num}")
```

输出：
检查：1
检查：2
找到了：3
（循环结束，不再检查4和5）
""")

# --------------------------------------------
# 1.3 重要关键字：continue ⭐⭐⭐⭐⭐
# --------------------------------------------

print("\n1.3 continue关键字详解")
print("-" * 60)

print("""
【关键字卡片】

关键字：continue
作用：跳过本次循环的剩余代码，直接进入下一次循环
类型：循环控制语句

使用场景：
- 跳过某些不需要处理的元素
- 过滤不符合条件的数据
- 避免执行某些代码

执行效果：
- 跳过本次循环的剩余代码
- 立即开始下一次循环
- 不会终止整个循环

使用示例：
```python
# 打印1-5中的奇数
for i in range(1, 6):
    if i % 2 == 0:
        continue  # 跳过偶数
    print(i)
```

输出：
1
3
5
（2和4被跳过）

break vs continue：
- break：跳出整个循环，循环结束
- continue：跳过本次循环，继续下一次
""")

# ============================================
# 第二部分：基础操作实践
# ============================================

print("\n" + "=" * 60)
print("第二部分：基础操作实践")
print("=" * 60)

# --------------------------------------------
# 2.1 基本while循环
# --------------------------------------------

print("\n2.1 基本while循环")
print("-" * 60)

# 示例1：计数循环
# 初始化计数器
count = 0

# while count < 5: 只要count小于5，就继续循环
print("示例1：打印0到4")
while count < 5:
    # 打印当前计数
    print(count, end=' ')
    # 更新计数器（重要！否则会无限循环）
    count += 1
print()

print()

# 示例2：累加求和
# 计算1+2+3+4+5的和
total = 0  # 累加器
num = 1    # 当前数字

print("示例2：计算1到5的和")
while num <= 5:
    # 累加当前数字
    total += num
    print(f"当前数字：{num}，累加和：{total}")
    # 更新数字
    num += 1

print(f"最终结果：{total}")

# --------------------------------------------
# 2.2 使用break跳出循环
# --------------------------------------------

print("\n2.2 使用break跳出循环")
print("-" * 60)

# 示例：查找列表中的目标值
numbers = [10, 20, 30, 40, 50]
target = 30

print(f"在列表{numbers}中查找{target}")

# 使用索引遍历
index = 0
while index < len(numbers):
    # 获取当前元素
    current = numbers[index]
    print(f"检查索引{index}：{current}")

    # 如果找到目标值
    if current == target:
        print(f"✓ 找到了！索引：{index}，值：{current}")
        break  # 立即退出循环

    # 更新索引
    index += 1
else:
    # 如果循环正常结束（没有break），执行else
    print("✗ 没找到")

print()

# 示例：用户输入验证
print("示例：用户输入验证（输入'quit'退出）")
print("请输入密码（正确密码：123456）：")

# 模拟用户输入（实际使用时用input()）
# 这里用列表模拟多次输入
test_inputs = ['111', '222', '123456']
input_index = 0

while True:  # 无限循环
    # 模拟用户输入
    if input_index < len(test_inputs):
        password = test_inputs[input_index]
        print(f"用户输入：{password}")
        input_index += 1
    else:
        break

    # 检查密码
    if password == '123456':
        print("✓ 密码正确！")
        break  # 密码正确，退出循环
    elif password == 'quit':
        print("用户取消")
        break  # 用户取消，退出循环
    else:
        print("✗ 密码错误，请重试")

# --------------------------------------------
# 2.3 使用continue跳过本次循环
# --------------------------------------------

print("\n2.3 使用continue跳过本次循环")
print("-" * 60)

# 示例1：打印1-10中的奇数
print("示例1：打印1-10中的奇数")
num = 0
while num < 10:
    # 先更新数字
    num += 1

    # 如果是偶数，跳过
    if num % 2 == 0:
        continue  # 跳过本次循环的剩余代码

    # 只有奇数才会执行到这里
    print(num, end=' ')
print()

print()

# 示例2：过滤负数
print("示例2：只处理正数")
numbers = [5, -2, 8, -1, 3, 0, -4, 7]
index = 0

while index < len(numbers):
    # 获取当前数字
    num = numbers[index]
    # 更新索引
    index += 1

    # 如果是负数或0，跳过
    if num <= 0:
        continue

    # 只处理正数
    print(f"处理正数：{num}，平方：{num ** 2}")

# --------------------------------------------
# 2.4 while vs for的选择
# --------------------------------------------

print("\n2.4 while vs for的选择")
print("-" * 60)

print("""
【选择原则】

使用for循环的场景：
- 遍历已知序列（列表、字符串、字典等）
- 循环次数确定
- 需要使用索引或enumerate()

使用while循环的场景：
- 循环次数不确定，由条件决定
- 需要在循环中动态改变条件
- 用户输入验证、游戏循环等

示例对比：
""")

# 场景1：遍历列表 - 推荐用for
fruits = ['苹果', '香蕉', '橙子']

print("场景1：遍历列表 - 推荐用for")
print("for循环（推荐）：")
for fruit in fruits:
    print(fruit, end=' ')
print()

print("while循环（不推荐）：")
index = 0
while index < len(fruits):
    print(fruits[index], end=' ')
    index += 1
print()

print()

# 场景2：用户输入验证 - 推荐用while
print("场景2：用户输入验证 - 推荐用while")
print("while循环（推荐）：")
print("（模拟：输入正整数，输入0退出）")

# 模拟用户输入
test_inputs = ['5', '-2', '10', '0']
input_index = 0

while True:
    # 模拟输入
    if input_index < len(test_inputs):
        num_str = test_inputs[input_index]
        print(f"输入：{num_str}")
        input_index += 1
    else:
        break

    num = int(num_str)

    if num == 0:
        print("退出")
        break
    elif num < 0:
        print("错误：请输入正整数")
        continue
    else:
        print(f"✓ 有效输入：{num}")

# ============================================
# 第三部分：深入理解（可选）
# ============================================

print("\n" + "=" * 60)
print("🔍 深入理解：while循环的执行机制（选学）")
print("=" * 60)

print("""
如果你想知道"为什么"，可以阅读这部分。
如果觉得难，可以跳过，不影响后续学习。

【while循环的执行机制】

1. 条件判断的时机：
   - 每次循环开始前都会判断条件
   - 如果条件一开始就是False，循环体一次都不执行

2. 无限循环的原理：
   - while True: 条件永远为True
   - 必须用break退出，否则会一直循环

3. while-else结构：
   - 如果循环正常结束（没有break），执行else
   - 如果用break退出，不执行else

示例：
```python
count = 0
while count < 3:
    print(count)
    count += 1
else:
    print("循环正常结束")  # 会执行

count = 0
while count < 3:
    print(count)
    if count == 1:
        break
    count += 1
else:
    print("循环正常结束")  # 不会执行
```

4. 循环变量的作用域：
   - 循环变量在循环外仍然可以访问
   - 保留的是最后一次循环的值

5. 性能考虑：
   - while循环每次都要判断条件，有一定开销
   - 如果能用for循环，优先用for（更简洁、更快）
""")

# ============================================
# 第四部分：常见错误和陷阱 ⭐⭐⭐⭐⭐
# ============================================

print("\n" + "=" * 60)
print("常见错误和陷阱 ⭐⭐⭐⭐⭐")
print("=" * 60)

# --------------------------------------------
# 4.1 错误1：无限循环（忘记更新条件）
# --------------------------------------------

print("\n4.1 错误1：无限循环（忘记更新条件）")
print("-" * 60)

print("""
❌ 错误代码：
```python
count = 0
while count < 5:
    print(count)
    # 忘记更新count
```

结果：无限打印0，程序卡死

原因：count永远是0，条件永远为True

✅ 正确代码：
```python
count = 0
while count < 5:
    print(count)
    count += 1  # 必须更新条件变量
```

结果：打印0, 1, 2, 3, 4

记忆技巧：while循环必须有"出口"，确保条件最终会变成False
""")

# --------------------------------------------
# 4.2 错误2：条件永远为True
# --------------------------------------------

print("\n4.2 错误2：条件永远为True")
print("-" * 60)

print("""
❌ 错误代码：
```python
while True:
    print("循环中")
    # 没有break
```

结果：无限循环，程序卡死

原因：True永远为True，没有退出条件

✅ 正确代码：
```python
while True:
    user_input = input("输入'quit'退出：")
    if user_input == 'quit':
        break  # 必须有break退出
    print(f"你输入了：{user_input}")
```

结果：用户输入'quit'时退出

记忆技巧：while True必须配合break使用
""")

# --------------------------------------------
# 4.3 错误3：条件判断错误
# --------------------------------------------

print("\n4.3 错误3：条件判断错误")
print("-" * 60)

print("""
❌ 错误代码：
```python
count = 0
while count <= 5:  # 应该是 <，不是 <=
    print(count)
    count += 1
```

结果：打印0, 1, 2, 3, 4, 5（多打印了一次）

原因：<=包含5，应该用<

✅ 正确代码：
```python
count = 0
while count < 5:  # 正确：<
    print(count)
    count += 1
```

结果：打印0, 1, 2, 3, 4

记忆技巧：
- < 和 <= 差一个元素
- 循环5次用 < 5，不是 <= 5
""")

# --------------------------------------------
# 4.4 错误4：在循环中修改列表
# --------------------------------------------

print("\n4.4 错误4：在循环中修改列表")
print("-" * 60)

print("""
❌ 错误代码：
```python
numbers = [1, 2, 3, 4, 5]
index = 0
while index < len(numbers):
    if numbers[index] % 2 == 0:
        numbers.remove(numbers[index])  # 删除元素
    index += 1  # 索引继续增加
```

结果：可能跳过某些元素或索引越界

原因：删除元素后，列表长度变化，索引错位

✅ 正确代码：
```python
# 方法1：倒序遍历
numbers = [1, 2, 3, 4, 5]
index = len(numbers) - 1
while index >= 0:
    if numbers[index] % 2 == 0:
        numbers.remove(numbers[index])
    index -= 1

# 方法2：创建新列表（推荐）
numbers = [1, 2, 3, 4, 5]
new_numbers = []
for num in numbers:
    if num % 2 != 0:
        new_numbers.append(num)
```

结果：正确删除偶数

记忆技巧：在循环中修改列表，要么倒序遍历，要么创建新列表
""")

# --------------------------------------------
# 4.5 错误5：break和continue位置错误
# --------------------------------------------

print("\n4.5 错误5：break和continue位置错误")
print("-" * 60)

print("""
❌ 错误代码：
```python
count = 0
while count < 5:
    count += 1  # 先更新
    if count == 3:
        continue  # 跳过
    print(count)  # 这行代码在continue后，会被跳过
```

结果：打印1, 2, 4, 5（跳过了3）

原因：continue跳过了print，符合预期，但要注意顺序

❌ 错误代码：
```python
count = 0
while count < 5:
    if count == 3:
        continue  # 跳过
    print(count)
    count += 1  # 这行代码在continue后，会被跳过
```

结果：无限循环（count永远是3）

原因：continue跳过了count += 1，count永远不会增加

✅ 正确代码：
```python
count = 0
while count < 5:
    count += 1  # 先更新（重要！）
    if count == 3:
        continue
    print(count)
```

结果：打印1, 2, 4, 5

记忆技巧：使用continue时，确保循环变量的更新在continue之前
""")

# --------------------------------------------
# 4.6 错误6：缩进错误
# --------------------------------------------

print("\n4.6 错误6：缩进错误")
print("-" * 60)

print("""
❌ 错误代码：
```python
count = 0
while count < 5:
print(count)  # 缩进错误
count += 1
```

结果：IndentationError

原因：循环体必须缩进

✅ 正确代码：
```python
count = 0
while count < 5:
    print(count)  # 正确缩进
    count += 1
```

结果：打印0, 1, 2, 3, 4

记忆技巧：while后面的代码块必须缩进4个空格
""")

# --------------------------------------------
# 4.7 错误7：混淆break和continue
# --------------------------------------------

print("\n4.7 错误7：混淆break和continue")
print("-" * 60)

print("""
❌ 错误理解：
- break：跳过本次循环
- continue：跳出整个循环

✅ 正确理解：
- break：跳出整个循环（循环结束）
- continue：跳过本次循环（继续下一次）

示例对比：
```python
# 使用break
for i in range(5):
    if i == 2:
        break
    print(i)
# 输出：0, 1（循环在i=2时结束）

# 使用continue
for i in range(5):
    if i == 2:
        continue
    print(i)
# 输出：0, 1, 3, 4（跳过i=2，继续循环）
```

记忆技巧：
- break = 打破循环，彻底结束
- continue = 继续循环，跳过本次
""")

# ============================================
# 第五部分：关键字速查
# ============================================

print("\n" + "=" * 60)
print("第五部分：关键字速查")
print("=" * 60)

print("""
【while】
语法：while 条件:
作用：根据条件重复执行代码
特点：条件为True时循环，False时停止

────────────────────────────────────

【break】
作用：立即跳出当前循环
使用场景：找到目标、遇到错误、用户退出
示例：
```python
while True:
    if 条件:
        break
```

────────────────────────────────────

【continue】
作用：跳过本次循环，继续下一次
使用场景：跳过某些元素、过滤数据
示例：
```python
while 条件:
    if 跳过条件:
        continue
    处理代码
```

────────────────────────────────────

【while-else】
语法：
```python
while 条件:
    代码
else:
    循环正常结束时执行
```
特点：如果用break退出，不执行else
""")

# ============================================
# 第六部分：练习题
# ============================================

print("\n" + "=" * 60)
print("练习题")
print("=" * 60)

print("""
1. 计算阶乘
   要求：使用while循环计算5的阶乘（5! = 5×4×3×2×1）
   提示：用一个变量保存结果，用另一个变量控制循环

2. 猜数字游戏
   要求：
   - 设定一个1-100的随机数
   - 用户输入猜测
   - 提示"太大了"或"太小了"
   - 猜对后显示猜了几次
   提示：使用while True和break

3. 找出列表中的最大值
   要求：使用while循环找出列表[3, 7, 2, 9, 1, 5]中的最大值
   提示：用一个变量保存当前最大值，遍历列表比较

4. 倒计时
   要求：从10倒数到1，每个数字打印一行，最后打印"发射！"
   提示：使用while循环，条件是count > 0

5. 过滤列表
   要求：从列表[1, -2, 3, -4, 5, -6]中删除所有负数
   提示：使用while循环倒序遍历，或创建新列表

6. 求和直到超过100
   要求：从1开始累加（1+2+3+...），直到和超过100，输出累加了多少个数
   提示：使用while循环，条件是total <= 100

7. 验证密码强度
   要求：
   - 密码长度至少8位
   - 包含数字和字母
   - 用户输入密码，不符合要求就重新输入
   提示：使用while True和break，用字符串方法检查
""")

print("\n" + "=" * 60)
print("学习完成！")
print("=" * 60)

print("""
🎉 恭喜！你已经学完while循环的所有内容！

📚 今日收获：
- ✅ 理解while循环的概念和执行流程
- ✅ 掌握break和continue的用法
- ✅ 学会while vs for的选择
- ✅ 了解常见错误和陷阱

🔜 下一步：
- 完成练习题，巩固知识
- 学习九九乘法表项目，实践循环嵌套
- 准备进入函数学习

💪 继续加油！
""")
