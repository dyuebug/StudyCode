# ============================================
# 元组基础 - 超详细注释版
# 日期：2026-03-05（第4天）
# 难度：⭐ 初级
# ============================================

"""
【核心概念】元组（Tuple）

是什么？
- 元组是Python中的不可变序列
- 类似列表，但创建后不能修改

为什么需要？
- 保护数据不被修改（如：坐标、日期）
- 作为字典的键（列表不行）
- 函数返回多个值
- 比列表更节省内存

核心特点：
- 有序：元素有固定顺序
- 不可变：创建后不能修改、添加、删除元素
- 可重复：元素可以重复
- 支持索引和切片：与列表相同
"""

# ============================================
# 第一部分：核心概念详解 ⭐⭐⭐⭐⭐
# ============================================

print("=" * 60)
print("第一部分：核心概念详解")
print("=" * 60)

# --------------------------------------------
# 1.1 什么是元组？
# --------------------------------------------

print("\n1.1 什么是元组？")
print("-" * 60)

print("""
【元组的概念】

元组就像"只读列表"：
- 创建后不能修改
- 适合存储固定数据

现实例子：
- 坐标：(x, y) = (10, 20)
- 日期：(年, 月, 日) = (2026, 3, 5)
- RGB颜色：(红, 绿, 蓝) = (255, 0, 0)

特点：
1. 有序：元素有固定顺序
2. 不可变：不能修改、添加、删除
3. 支持索引和切片：与列表相同
4. 可以包含不同类型的元素
""")

# --------------------------------------------
# 1.2 元组 vs 列表的区别
# --------------------------------------------

print("\n1.2 元组 vs 列表的区别")
print("-" * 60)

print("""
【对比】

列表（List）：
- 可变：可以修改、添加、删除元素
- 使用方括号：[1, 2, 3]
- 适合：需要修改的数据

元组（Tuple）：
- 不可变：创建后不能修改
- 使用圆括号：(1, 2, 3)
- 适合：固定不变的数据

示例：
列表：scores = [85, 90, 78]  # 可以修改：scores[0] = 100
元组：point = (10, 20)       # 不能修改：point[0] = 30 会报错

优势：
- 元组更安全：数据不会被意外修改
- 元组更快：访问速度比列表快
- 元组更省内存：占用空间更小
- 元组可以作为字典的键：列表不行
""")

# ============================================
# 第二部分：基础操作实践
# ============================================

print("\n" + "=" * 60)
print("第二部分：基础操作实践")
print("=" * 60)

# --------------------------------------------
# 2.1 创建元组的方式
# --------------------------------------------

print("\n2.1 创建元组的方式")
print("-" * 60)

# 方式1：使用圆括号 () 创建（最常用）
# 格式：(元素1, 元素2, ...)
point = (10, 20)
print("方式1 - 圆括号：", point)

# 方式2：不使用括号（逗号分隔）
# Python会自动识别为元组
coordinates = 100, 200, 300
print("方式2 - 逗号分隔：", coordinates)

# 方式3：使用 tuple() 函数创建
# 可以将列表、字符串等转换为元组
list_data = [1, 2, 3]
tuple_data = tuple(list_data)
print("方式3 - tuple()：", tuple_data)

# 方式4：创建空元组
empty_tuple = ()
print("方式4 - 空元组：", empty_tuple)

# 方式5：单元素元组（重要！）⚠️
# 必须在元素后加逗号，否则不是元组
single1 = (1,)    # 这是元组
single2 = (1)     # 这不是元组，是整数1
print(f"单元素元组：{single1}，类型：{type(single1)}")
print(f"不是元组：{single2}，类型：{type(single2)}")

print("""
⚠️ 重要：单元素元组必须加逗号！
正确：(1,)
错误：(1)  # 这只是整数1，不是元组
""")

# --------------------------------------------
# 2.2 访问元组元素
# --------------------------------------------

print("\n2.2 访问元组元素")
print("-" * 60)

# 创建元组
fruits = ("apple", "banana", "cherry", "date")

# 方法1：通过索引访问（与列表相同）
# 格式：元组名[索引]
# 索引从0开始，负数索引从-1开始
first = fruits[0]      # 第一个元素
last = fruits[-1]      # 最后一个元素
print(f"第一个：{first}，最后一个：{last}")

# 方法2：切片访问（与列表相同）
# 格式：元组名[start:end:step]
# 左闭右开：包含start，不包含end
slice1 = fruits[1:3]   # 索引1到2
slice2 = fruits[:2]    # 前两个
slice3 = fruits[::2]   # 每隔一个
print(f"切片[1:3]：{slice1}")
print(f"切片[:2]：{slice2}")
print(f"切片[::2]：{slice3}")

# --------------------------------------------
# 2.3 元组的不可变性
# --------------------------------------------

print("\n2.3 元组的不可变性")
print("-" * 60)

# 创建元组
numbers = (1, 2, 3, 4, 5)
print("原始元组：", numbers)

# 尝试修改元组（会报错）
print("""
❌ 不能修改元组：
numbers[0] = 100  # TypeError: 'tuple' object does not support item assignment

❌ 不能添加元素：
numbers.append(6)  # AttributeError: 'tuple' object has no attribute 'append'

❌ 不能删除元素：
del numbers[0]  # TypeError: 'tuple' object doesn't support item deletion
""")

# 但可以创建新元组
new_numbers = numbers + (6, 7)  # 拼接创建新元组
print("拼接后的新元组：", new_numbers)
print("原元组不变：", numbers)

# --------------------------------------------
# 2.4 元组解包（Unpacking）⭐⭐⭐⭐⭐
# --------------------------------------------

print("\n2.4 元组解包（Unpacking）")
print("-" * 60)

# 元组解包：将元组的元素赋值给多个变量
# 格式：变量1, 变量2, ... = 元组

# 示例1：基本解包
point = (10, 20)
x, y = point  # 将元组的两个元素分别赋值给x和y
print(f"x = {x}, y = {y}")

# 示例2：交换变量（非常实用！）
a = 1
b = 2
print(f"交换前：a = {a}, b = {b}")
a, b = b, a  # 利用元组解包交换变量
print(f"交换后：a = {a}, b = {b}")

# 示例3：函数返回多个值
def get_user_info():
    """函数返回多个值（实际返回元组）"""
    name = "张三"
    age = 18
    city = "北京"
    return name, age, city  # 返回元组

# 接收多个返回值
name, age, city = get_user_info()
print(f"姓名：{name}，年龄：{age}，城市：{city}")

# 示例4：使用 * 收集剩余元素
numbers = (1, 2, 3, 4, 5)
first, *middle, last = numbers  # *middle收集中间的元素
print(f"第一个：{first}")
print(f"中间的：{middle}")  # 列表类型
print(f"最后一个：{last}")

# --------------------------------------------
# 2.5 元组的常用操作
# --------------------------------------------

print("\n2.5 元组的常用操作")
print("-" * 60)

# 创建元组
data = (1, 2, 3, 2, 4, 2, 5)

# 1. len() - 获取元组长度
length = len(data)
print(f"元组长度：{length}")

# 2. count() - 统计元素出现次数
# 格式：元组名.count(元素)
count_2 = data.count(2)
print(f"元素2出现次数：{count_2}")

# 3. index() - 查找元素第一次出现的索引
# 格式：元组名.index(元素)
# 注意：如果元素不存在，会报 ValueError
index_3 = data.index(3)
print(f"元素3的索引：{index_3}")

# 4. in / not in - 判断元素是否存在
has_3 = 3 in data
has_10 = 10 in data
print(f"是否包含3：{has_3}")
print(f"是否包含10：{has_10}")

# 5. max() / min() / sum() - 最大值、最小值、求和
# 注意：元素必须是数字类型
max_value = max(data)
min_value = min(data)
sum_value = sum(data)
print(f"最大值：{max_value}，最小值：{min_value}，总和：{sum_value}")

# 6. 元组拼接（+）
tuple1 = (1, 2, 3)
tuple2 = (4, 5, 6)
tuple3 = tuple1 + tuple2  # 创建新元组
print(f"拼接后：{tuple3}")

# 7. 元组重复（*）
tuple4 = (1, 2) * 3  # 重复3次
print(f"重复后：{tuple4}")

# --------------------------------------------
# 2.6 遍历元组
# --------------------------------------------

print("\n2.6 遍历元组")
print("-" * 60)

# 创建元组
colors = ("red", "green", "blue")

# 方法1：直接遍历元素
print("方法1 - 遍历元素：")
for color in colors:
    print(f"  颜色：{color}")

# 方法2：遍历索引和元素（使用enumerate）
print("\n方法2 - 遍历索引和元素：")
for index, color in enumerate(colors):
    print(f"  索引{index}：{color}")

# --------------------------------------------
# 2.7 元组与列表的转换
# --------------------------------------------

print("\n2.7 元组与列表的转换")
print("-" * 60)

# 元组转列表：使用 list()
tuple_data = (1, 2, 3, 4, 5)
list_data = list(tuple_data)
print(f"元组：{tuple_data}，类型：{type(tuple_data)}")
print(f"列表：{list_data}，类型：{type(list_data)}")

# 列表转元组：使用 tuple()
list_data2 = [10, 20, 30]
tuple_data2 = tuple(list_data2)
print(f"列表：{list_data2}，类型：{type(list_data2)}")
print(f"元组：{tuple_data2}，类型：{type(tuple_data2)}")

# 应用场景：需要修改元组时
# 1. 转换为列表
# 2. 修改列表
# 3. 转换回元组
original_tuple = (1, 2, 3)
temp_list = list(original_tuple)  # 转为列表
temp_list.append(4)                # 修改列表
new_tuple = tuple(temp_list)       # 转回元组
print(f"原元组：{original_tuple}")
print(f"新元组：{new_tuple}")

# --------------------------------------------
# 2.8 嵌套元组
# --------------------------------------------

print("\n2.8 嵌套元组")
print("-" * 60)

# 元组可以包含元组（嵌套）
# 应用：表示矩阵、坐标系等

# 示例1：二维坐标
points = ((0, 0), (10, 20), (30, 40))
print("坐标点：", points)

# 访问嵌套元组
first_point = points[0]      # (0, 0)
x = points[0][0]             # 0
y = points[0][1]             # 0
print(f"第一个点：{first_point}，x={x}, y={y}")

# 示例2：学生信息（姓名、年龄、成绩）
students = (
    ("张三", 18, 85),
    ("李四", 19, 90),
    ("王五", 20, 88)
)

# 遍历嵌套元组
print("\n学生信息：")
for name, age, score in students:
    print(f"  姓名：{name}，年龄：{age}，成绩：{score}")

# ============================================
# 第三部分：深入理解（可选）
# ============================================

print("\n" + "=" * 60)
print("🔍 深入理解：元组的使用场景（选学）")
print("=" * 60)

print("""
如果你想知道"什么时候用元组，什么时候用列表"，可以阅读这部分。
如果觉得难，可以跳过，不影响后续学习。

【元组的使用场景】

1. 数据不需要修改时
   ✅ 坐标：point = (10, 20)
   ✅ 日期：date = (2026, 3, 5)
   ✅ RGB颜色：color = (255, 0, 0)

2. 作为字典的键
   ✅ 元组可以：location = {(0, 0): "原点", (10, 20): "点A"}
   ❌ 列表不行：location = {[0, 0]: "原点"}  # 报错

3. 函数返回多个值
   ✅ def get_info(): return "张三", 18, "北京"
   ✅ name, age, city = get_info()

4. 保护数据不被修改
   ✅ 配置信息：CONFIG = ("localhost", 8080, "admin")
   ✅ 常量：WEEKDAYS = ("Mon", "Tue", "Wed", "Thu", "Fri")

5. 性能优化
   ✅ 元组比列表快：访问速度更快
   ✅ 元组更省内存：占用空间更小

【列表 vs 元组的选择】

使用列表：
- 数据需要修改（添加、删除、修改元素）
- 数据是同类型的集合（如：成绩列表）
- 需要使用列表的方法（append、remove等）

使用元组：
- 数据不需要修改（固定数据）
- 数据是不同类型的组合（如：学生信息）
- 需要作为字典的键
- 需要保护数据不被修改
""")

# ============================================
# 第四部分：常见错误和陷阱 ⭐⭐⭐⭐⭐
# ============================================

print("\n" + "=" * 60)
print("常见错误和陷阱 ⭐⭐⭐⭐⭐")
print("=" * 60)

# --------------------------------------------
# 4.1 错误1：单元素元组忘记加逗号
# --------------------------------------------

print("\n4.1 错误1：单元素元组忘记加逗号")
print("-" * 60)

print("""
❌ 错误代码：
```python
single = (1)  # 这不是元组，是整数1
print(type(single))  # <class 'int'>
```

结果：single是整数，不是元组

原因：括号被当作数学运算的括号

✅ 正确代码：
```python
single = (1,)  # 必须加逗号
print(type(single))  # <class 'tuple'>
```

结果：single是元组

记忆技巧：单元素元组必须加逗号！
""")

# --------------------------------------------
# 4.2 错误2：尝试修改元组
# --------------------------------------------

print("\n4.2 错误2：尝试修改元组")
print("-" * 60)

print("""
❌ 错误代码：
```python
numbers = (1, 2, 3)
numbers[0] = 100  # TypeError: 'tuple' object does not support item assignment
```

结果：报错 TypeError

原因：元组是不可变的，不能修改

✅ 正确代码：
```python
numbers = (1, 2, 3)
# 方法1：转换为列表，修改后转回元组
temp = list(numbers)
temp[0] = 100
numbers = tuple(temp)

# 方法2：创建新元组
numbers = (100,) + numbers[1:]
```

结果：创建新元组

记忆技巧：元组不可变，需要修改时转换为列表
""")

# --------------------------------------------
# 4.3 错误3：元组解包数量不匹配
# --------------------------------------------

print("\n4.3 错误3：元组解包数量不匹配")
print("-" * 60)

print("""
❌ 错误代码：
```python
point = (10, 20, 30)
x, y = point  # ValueError: too many values to unpack (expected 2)
```

结果：报错 ValueError

原因：元组有3个元素，但只有2个变量接收

✅ 正确代码：
```python
point = (10, 20, 30)
# 方法1：变量数量匹配
x, y, z = point

# 方法2：使用 * 收集剩余元素
x, y, *rest = point  # rest = [30]

# 方法3：只取需要的元素
x, y = point[0], point[1]
```

结果：正常运行

记忆技巧：解包时变量数量要匹配，或使用 * 收集
""")

# --------------------------------------------
# 4.4 错误4：元组中包含可变对象
# --------------------------------------------

print("\n4.4 错误4：元组中包含可变对象")
print("-" * 60)

print("""
❌ 错误理解：
```python
data = ([1, 2], [3, 4])  # 元组包含列表
data[0].append(3)  # 可以修改列表！
print(data)  # ([1, 2, 3], [3, 4])
```

结果：元组中的列表被修改了

原因：元组不可变，但元组中的可变对象（列表）可以修改

✅ 正确理解：
```python
# 元组本身不可变：
data = ([1, 2], [3, 4])
data[0] = [5, 6]  # 报错：不能替换元组的元素

# 但元组中的列表可以修改：
data[0].append(3)  # 可以：修改列表的内容
```

结果：理解元组的不可变性

记忆技巧：元组不可变指的是元组的结构，不是内容
""")

# --------------------------------------------
# 4.5 错误5：元组不能作为字典的值排序
# --------------------------------------------

print("\n4.5 错误5：混淆元组和列表的方法")
print("-" * 60)

print("""
❌ 错误代码：
```python
numbers = (3, 1, 4, 1, 5)
numbers.sort()  # AttributeError: 'tuple' object has no attribute 'sort'
```

结果：报错 AttributeError

原因：元组没有 sort() 方法（因为不可变）

✅ 正确代码：
```python
numbers = (3, 1, 4, 1, 5)
# 使用 sorted() 函数（返回列表）
sorted_list = sorted(numbers)
print(sorted_list)  # [1, 1, 3, 4, 5]

# 如果需要元组，转换回去
sorted_tuple = tuple(sorted(numbers))
print(sorted_tuple)  # (1, 1, 3, 4, 5)
```

结果：正常排序

记忆技巧：元组没有修改方法，使用 sorted() 函数
""")

# ============================================
# 第五部分：函数卡片速查
# ============================================

print("\n" + "=" * 60)
print("函数卡片速查")
print("=" * 60)

print("""
【方法1：count()】

语法：元组名.count(元素)
参数：
  - 元素: 要统计的元素
返回：元素出现的次数

示例：
```python
data = (1, 2, 3, 2, 4, 2)
count = data.count(2)  # 3
```

────────────────────────────────────

【方法2：index()】

语法：元组名.index(元素)
参数：
  - 元素: 要查找的元素
返回：元素第一次出现的索引

示例：
```python
data = (1, 2, 3, 2, 4)
index = data.index(2)  # 1
```

────────────────────────────────────

【函数1：len()】

语法：len(元组名)
参数：
  - 元组名: 要计算长度的元组
返回：元组的长度

示例：
```python
data = (1, 2, 3, 4, 5)
length = len(data)  # 5
```

────────────────────────────────────

【函数2：max() / min()】

语法：max(元组名) / min(元组名)
参数：
  - 元组名: 要计算的元组
返回：最大值 / 最小值

示例：
```python
data = (1, 2, 3, 4, 5)
max_value = max(data)  # 5
min_value = min(data)  # 1
```

────────────────────────────────────

【函数3：sum()】

语法：sum(元组名)
参数：
  - 元组名: 要求和的元组
返回：所有元素的和

示例：
```python
data = (1, 2, 3, 4, 5)
total = sum(data)  # 15
```

────────────────────────────────────

【函数4：tuple()】

语法：tuple(可迭代对象)
参数：
  - 可迭代对象: 列表、字符串等
返回：元组

示例：
```python
list_data = [1, 2, 3]
tuple_data = tuple(list_data)  # (1, 2, 3)
```

────────────────────────────────────

【函数5：sorted()】

语法：sorted(元组名)
参数：
  - 元组名: 要排序的元组
返回：排序后的列表

示例：
```python
data = (3, 1, 4, 1, 5)
sorted_list = sorted(data)  # [1, 1, 3, 4, 5]
sorted_tuple = tuple(sorted(data))  # (1, 1, 3, 4, 5)
```
""")

# ============================================
# 第六部分：练习题
# ============================================

print("\n" + "=" * 60)
print("练习题")
print("=" * 60)

print("""
1. 创建一个包含5个数字的元组，并打印最大值、最小值、总和
   提示：使用 max()、min()、sum()

2. 创建一个元组，统计某个元素出现的次数
   提示：使用 count()

3. 使用元组解包交换两个变量的值
   提示：a, b = b, a

4. 创建一个函数，返回三个值（姓名、年龄、城市），并使用元组解包接收
   提示：return name, age, city

5. 创建一个嵌套元组，表示三个学生的信息（姓名、年龄、成绩），并遍历打印
   提示：for name, age, score in students

6. 将一个列表转换为元组，排序后再转换回列表
   提示：tuple() 和 sorted()

7. 创建一个元组作为字典的键，存储坐标和对应的名称
   提示：{(0, 0): "原点", (10, 20): "点A"}

8. 编写程序，判断一个元组是否包含某个元素
   提示：使用 in

9. 创建一个元组，使用 * 解包收集中间的元素
   提示：first, *middle, last = tuple

10. 比较元组和列表的内存占用（使用 sys.getsizeof()）
    提示：import sys; sys.getsizeof(tuple) vs sys.getsizeof(list)
""")

print("\n" + "=" * 60)
print("学习完成！")
print("=" * 60)
print("""
恭喜你完成元组基础的学习！

你已经掌握：
✅ 元组的概念和特点
✅ 元组的创建和访问
✅ 元组的不可变性
✅ 元组解包的使用
✅ 元组的常用操作
✅ 元组 vs 列表的区别

下一步：
→ 完成学生信息管理系统项目（12_学生信息管理系统.py）
→ 综合运用字典、列表、元组
""")
