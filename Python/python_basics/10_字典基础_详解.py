# ============================================
# 字典基础 - 超详细注释版
# 日期：2026-03-05（第4天）
# 难度：⭐⭐ 中级
# ============================================

"""
【核心概念】字典（Dictionary）

是什么？
- 字典是Python中的键值对（key-value）数据结构
- 通过键（key）来访问对应的值（value）

为什么需要？
- 存储关联数据（如：姓名→年龄、学号→成绩）
- 快速查找数据（通过键直接访问，不需要遍历）
- 表达现实世界的映射关系

核心特点：
- 键值对结构：每个元素是 key: value
- 键唯一：同一个键只能出现一次
- 键必须是不可变类型：字符串、数字、元组可以，列表不行
- 值可以是任意类型：字符串、数字、列表、字典等
"""

# ============================================
# 第一部分：核心概念详解 ⭐⭐⭐⭐⭐
# ============================================

print("=" * 60)
print("第一部分：核心概念详解")
print("=" * 60)

# --------------------------------------------
# 1.1 什么是字典？
# --------------------------------------------

print("\n1.1 什么是字典？")
print("-" * 60)

print("""
【字典的概念】

字典就像现实中的通讯录：
- 姓名（键） → 电话号码（值）
- 学号（键） → 学生信息（值）
- 单词（键） → 释义（值）

特点：
1. 键值对结构：每个元素包含键和值
2. 通过键访问值：快速查找
3. 键唯一：不能重复
4. 键不可变：必须是字符串、数字、元组等不可变类型
""")

# --------------------------------------------
# 1.2 字典 vs 列表的区别
# --------------------------------------------

print("\n1.2 字典 vs 列表的区别")
print("-" * 60)

print("""
【对比】

列表（List）：
- 通过索引（0, 1, 2...）访问
- 有序：元素有固定顺序
- 适合：存储同类数据（如：成绩列表）

字典（Dictionary）：
- 通过键（自定义名称）访问
- 无序：元素没有固定顺序（Python 3.7+保持插入顺序）
- 适合：存储关联数据（如：学生信息）

示例：
列表：scores = [85, 90, 78]  # 通过索引访问：scores[0]
字典：student = {"name": "张三", "age": 18}  # 通过键访问：student["name"]
""")

# ============================================
# 第二部分：基础操作实践
# ============================================

print("\n" + "=" * 60)
print("第二部分：基础操作实践")
print("=" * 60)

# --------------------------------------------
# 2.1 创建字典的三种方式
# --------------------------------------------

print("\n2.1 创建字典的三种方式")
print("-" * 60)

# 方式1：使用花括号 {} 创建（最常用）
# 格式：{键1: 值1, 键2: 值2, ...}
student1 = {"name": "张三", "age": 18, "score": 85}
print("方式1 - 花括号：", student1)

# 方式2：使用 dict() 函数创建
# 格式：dict(键1=值1, 键2=值2, ...)
# 注意：键名不需要引号，但只能是合法的变量名
student2 = dict(name="李四", age=19, score=90)
print("方式2 - dict()：", student2)

# 方式3：创建空字典
# 用途：先创建空字典，后续动态添加数据
empty_dict = {}
print("方式3 - 空字典：", empty_dict)

# --------------------------------------------
# 2.2 访问字典的值
# --------------------------------------------

print("\n2.2 访问字典的值")
print("-" * 60)

# 创建一个学生信息字典
student = {
    "name": "王五",      # 键："name"，值："王五"
    "age": 20,           # 键："age"，值：20
    "score": 88,         # 键："score"，值：88
    "city": "北京"       # 键："city"，值："北京"
}

# 方法1：使用 [] 访问（直接访问）
# 格式：字典名[键]
# 注意：如果键不存在，会报 KeyError 错误
name = student["name"]
age = student["age"]
print(f"姓名：{name}，年龄：{age}")

# 方法2：使用 get() 方法访问（推荐）⭐⭐⭐⭐⭐
# 格式：字典名.get(键, 默认值)
# 优点：如果键不存在，返回默认值（不报错）
score = student.get("score")
grade = student.get("grade", "未设置")  # 键不存在，返回"未设置"
print(f"成绩：{score}，年级：{grade}")

print("""
⚠️ 重要区别：
- student["key"]：键不存在会报错
- student.get("key", 默认值)：键不存在返回默认值（推荐）
""")

# --------------------------------------------
# 2.3 修改字典的值
# --------------------------------------------

print("\n2.3 修改字典的值")
print("-" * 60)

# 创建字典
person = {"name": "赵六", "age": 25, "city": "上海"}
print("原始字典：", person)

# 修改已存在的键的值
# 格式：字典名[键] = 新值
person["age"] = 26          # 修改年龄
person["city"] = "深圳"     # 修改城市
print("修改后：", person)

# --------------------------------------------
# 2.4 添加新的键值对
# --------------------------------------------

print("\n2.4 添加新的键值对")
print("-" * 60)

# 方法1：直接赋值（键不存在时自动添加）
# 格式：字典名[新键] = 值
person["phone"] = "13800138000"  # 添加电话
person["email"] = "zhao@example.com"  # 添加邮箱
print("添加后：", person)

# 方法2：使用 setdefault() 方法
# 格式：字典名.setdefault(键, 默认值)
# 作用：如果键存在，返回值；如果不存在，添加键值对并返回默认值
person.setdefault("gender", "男")  # 添加性别
person.setdefault("age", 30)       # age已存在，不修改
print("setdefault后：", person)

# --------------------------------------------
# 2.5 删除键值对
# --------------------------------------------

print("\n2.5 删除键值对")
print("-" * 60)

# 创建字典
data = {"a": 1, "b": 2, "c": 3, "d": 4}
print("原始字典：", data)

# 方法1：使用 pop() 删除指定键
# 格式：字典名.pop(键, 默认值)
# 返回：被删除的值
# 注意：如果键不存在且没有默认值，会报错
value = data.pop("b")  # 删除键"b"，返回值2
print(f"删除键'b'，返回值：{value}")
print("删除后：", data)

# 方法2：使用 del 删除指定键
# 格式：del 字典名[键]
# 注意：如果键不存在，会报 KeyError
del data["c"]  # 删除键"c"
print("del删除后：", data)

# 方法3：使用 clear() 清空字典
# 格式：字典名.clear()
# 作用：删除所有键值对，字典变为空
data.clear()
print("清空后：", data)

# --------------------------------------------
# 2.6 遍历字典
# --------------------------------------------

print("\n2.6 遍历字典")
print("-" * 60)

# 创建字典
scores = {"语文": 85, "数学": 90, "英语": 88}

# 方法1：遍历键（keys()）
# 格式：for 键 in 字典名.keys()
print("方法1 - 遍历键：")
for subject in scores.keys():
    print(f"  科目：{subject}")

# 方法2：遍历值（values()）
# 格式：for 值 in 字典名.values()
print("\n方法2 - 遍历值：")
for score in scores.values():
    print(f"  成绩：{score}")

# 方法3：遍历键值对（items()）⭐⭐⭐⭐⭐
# 格式：for 键, 值 in 字典名.items()
# 最常用：同时获取键和值
print("\n方法3 - 遍历键值对（推荐）：")
for subject, score in scores.items():
    print(f"  {subject}：{score}分")

# 方法4：直接遍历字典（默认遍历键）
print("\n方法4 - 直接遍历（等同于keys()）：")
for subject in scores:
    print(f"  {subject}：{scores[subject]}分")

# --------------------------------------------
# 2.7 字典的常用方法
# --------------------------------------------

print("\n2.7 字典的常用方法")
print("-" * 60)

# 创建字典
info = {"name": "小明", "age": 18, "city": "北京"}

# 1. keys() - 获取所有键
# 返回：dict_keys对象（可转为列表）
keys = info.keys()
print(f"所有键：{list(keys)}")

# 2. values() - 获取所有值
# 返回：dict_values对象（可转为列表）
values = info.values()
print(f"所有值：{list(values)}")

# 3. items() - 获取所有键值对
# 返回：dict_items对象（可转为列表）
items = info.items()
print(f"所有键值对：{list(items)}")

# 4. update() - 更新字典（合并）
# 格式：字典名.update(另一个字典)
# 作用：将另一个字典的键值对添加到当前字典
info.update({"phone": "13800138000", "age": 19})  # age会被更新
print(f"update后：{info}")

# 5. copy() - 复制字典（浅拷贝）
# 返回：新字典（修改新字典不影响原字典）
info_copy = info.copy()
info_copy["name"] = "小红"
print(f"原字典：{info}")
print(f"复制的字典：{info_copy}")

# 6. len() - 获取字典长度（键值对数量）
length = len(info)
print(f"字典长度：{length}")

# 7. in / not in - 判断键是否存在
# 格式：键 in 字典名
# 返回：True 或 False
has_name = "name" in info
has_email = "email" in info
print(f"是否有'name'键：{has_name}")
print(f"是否有'email'键：{has_email}")

# --------------------------------------------
# 2.8 字典推导式（简单入门）
# --------------------------------------------

print("\n2.8 字典推导式")
print("-" * 60)

# 字典推导式：快速创建字典
# 格式：{键表达式: 值表达式 for 变量 in 可迭代对象}

# 示例1：创建平方数字典
# 键：1, 2, 3, 4, 5
# 值：1, 4, 9, 16, 25
squares = {x: x**2 for x in range(1, 6)}
print(f"平方数字典：{squares}")

# 示例2：反转字典（键值互换）
original = {"a": 1, "b": 2, "c": 3}
reversed_dict = {value: key for key, value in original.items()}
print(f"原字典：{original}")
print(f"反转后：{reversed_dict}")

# ============================================
# 第三部分：深入理解（可选）
# ============================================

print("\n" + "=" * 60)
print("🔍 深入理解：字典的键必须是不可变类型（选学）")
print("=" * 60)

print("""
如果你想知道"为什么字典的键必须是不可变类型"，可以阅读这部分。
如果觉得难，可以跳过，不影响后续学习。

【为什么键必须是不可变类型？】

原因：字典使用哈希表（Hash Table）存储数据
- 哈希表需要计算键的哈希值（hash value）
- 哈希值必须固定不变
- 如果键可变（如列表），哈希值会变化，导致无法找到数据

可以作为键的类型（不可变）：
✅ 字符串（str）："name", "age"
✅ 数字（int, float）：1, 2.5
✅ 元组（tuple）：(1, 2, 3)
✅ 布尔值（bool）：True, False

不能作为键的类型（可变）：
❌ 列表（list）：[1, 2, 3]
❌ 字典（dict）：{"a": 1}
❌ 集合（set）：{1, 2, 3}

示例：
正确：person = {("name", "age"): "张三"}  # 元组可以作为键
错误：person = {["name", "age"]: "张三"}  # 列表不能作为键（会报错）
""")

# ============================================
# 第四部分：常见错误和陷阱 ⭐⭐⭐⭐⭐
# ============================================

print("\n" + "=" * 60)
print("常见错误和陷阱 ⭐⭐⭐⭐⭐")
print("=" * 60)

# --------------------------------------------
# 4.1 错误1：访问不存在的键
# --------------------------------------------

print("\n4.1 错误1：访问不存在的键")
print("-" * 60)

print("""
❌ 错误代码：
```python
student = {"name": "张三", "age": 18}
print(student["score"])  # KeyError: 'score'
```

结果：报错 KeyError: 'score'

原因：键"score"不存在

✅ 正确代码：
```python
student = {"name": "张三", "age": 18}
score = student.get("score", 0)  # 使用get()，不存在返回0
print(score)  # 输出：0
```

结果：返回默认值0，不报错

记忆技巧：使用 get() 方法更安全，推荐使用
""")

# --------------------------------------------
# 4.2 错误2：使用列表作为键
# --------------------------------------------

print("\n4.2 错误2：使用列表作为键")
print("-" * 60)

print("""
❌ 错误代码：
```python
data = {[1, 2]: "value"}  # TypeError: unhashable type: 'list'
```

结果：报错 TypeError: unhashable type: 'list'

原因：列表是可变类型，不能作为字典的键

✅ 正确代码：
```python
data = {(1, 2): "value"}  # 使用元组（不可变）
print(data[(1, 2)])  # 输出：value
```

结果：正常运行

记忆技巧：键必须是不可变类型（字符串、数字、元组）
""")

# --------------------------------------------
# 4.3 错误3：字典赋值是引用
# --------------------------------------------

print("\n4.3 错误3：字典赋值是引用")
print("-" * 60)

print("""
❌ 错误代码：
```python
dict1 = {"a": 1, "b": 2}
dict2 = dict1  # 赋值是引用，不是拷贝
dict2["a"] = 100
print(dict1)  # {"a": 100, "b": 2}  # dict1也被修改了！
```

结果：修改dict2，dict1也被修改

原因：dict2 = dict1 是引用，两个变量指向同一个字典

✅ 正确代码：
```python
dict1 = {"a": 1, "b": 2}
dict2 = dict1.copy()  # 使用copy()复制
dict2["a"] = 100
print(dict1)  # {"a": 1, "b": 2}  # dict1不变
print(dict2)  # {"a": 100, "b": 2}
```

结果：dict1和dict2是独立的字典

记忆技巧：使用 copy() 方法复制字典
""")

# --------------------------------------------
# 4.4 错误4：遍历时修改字典
# --------------------------------------------

print("\n4.4 错误4：遍历时修改字典")
print("-" * 60)

print("""
❌ 错误代码：
```python
data = {"a": 1, "b": 2, "c": 3}
for key in data:
    if data[key] == 2:
        del data[key]  # RuntimeError: dictionary changed size during iteration
```

结果：报错 RuntimeError

原因：遍历时不能修改字典大小

✅ 正确代码：
```python
data = {"a": 1, "b": 2, "c": 3}
# 方法1：先收集要删除的键，再删除
keys_to_delete = [key for key in data if data[key] == 2]
for key in keys_to_delete:
    del data[key]

# 方法2：使用字典推导式创建新字典
data = {key: value for key, value in data.items() if value != 2}
```

结果：正常运行

记忆技巧：遍历时不要修改字典，先收集再操作
""")

# --------------------------------------------
# 4.5 错误5：忘记键是区分大小写的
# --------------------------------------------

print("\n4.5 错误5：忘记键是区分大小写的")
print("-" * 60)

print("""
❌ 错误代码：
```python
student = {"Name": "张三", "Age": 18}
print(student["name"])  # KeyError: 'name'
```

结果：报错 KeyError: 'name'

原因：键"Name"和"name"是不同的（区分大小写）

✅ 正确代码：
```python
student = {"Name": "张三", "Age": 18}
print(student["Name"])  # 输出：张三
# 或者统一使用小写
student = {"name": "张三", "age": 18}
print(student["name"])  # 输出：张三
```

结果：正常运行

记忆技巧：键名区分大小写，建议统一使用小写
""")

# --------------------------------------------
# 4.6 错误6：update()会覆盖已存在的键
# --------------------------------------------

print("\n4.6 错误6：update()会覆盖已存在的键")
print("-" * 60)

print("""
❌ 错误理解：
```python
dict1 = {"a": 1, "b": 2}
dict2 = {"b": 100, "c": 3}
dict1.update(dict2)
print(dict1)  # {"a": 1, "b": 100, "c": 3}  # b被覆盖了！
```

结果：键"b"的值从2变成100

原因：update()会覆盖已存在的键

✅ 正确理解：
```python
# 如果不想覆盖，需要手动检查
dict1 = {"a": 1, "b": 2}
dict2 = {"b": 100, "c": 3}
for key, value in dict2.items():
    if key not in dict1:  # 只添加不存在的键
        dict1[key] = value
print(dict1)  # {"a": 1, "b": 2, "c": 3}  # b保持原值
```

结果：只添加新键，不覆盖已存在的键

记忆技巧：update()会覆盖，需要保留原值时手动检查
""")

# --------------------------------------------
# 4.7 错误7：空字典的判断
# --------------------------------------------

print("\n4.7 错误7：空字典的判断")
print("-" * 60)

print("""
❌ 错误代码：
```python
data = {}
if data == {}:  # 不推荐
    print("字典为空")
```

结果：虽然正确，但不是最佳实践

原因：比较操作效率较低

✅ 正确代码：
```python
data = {}
if not data:  # 推荐：空字典为False
    print("字典为空")

# 或者
if len(data) == 0:
    print("字典为空")
```

结果：更高效的判断方式

记忆技巧：空字典为False，直接用 if not dict
""")

# ============================================
# 第五部分：函数卡片速查
# ============================================

print("\n" + "=" * 60)
print("函数卡片速查")
print("=" * 60)

print("""
【方法1：get()】⭐⭐⭐⭐⭐

语法：字典名.get(键, 默认值)
参数：
  - 键: 要查找的键
  - 默认值: 键不存在时返回的值（可选，默认None）
返回：键对应的值，或默认值

示例：
```python
student = {"name": "张三", "age": 18}
name = student.get("name")        # "张三"
score = student.get("score", 0)   # 0（键不存在）
```

────────────────────────────────────

【方法2：keys()】

语法：字典名.keys()
参数：无
返回：dict_keys对象（所有键）

示例：
```python
data = {"a": 1, "b": 2}
keys = data.keys()  # dict_keys(['a', 'b'])
```

────────────────────────────────────

【方法3：values()】

语法：字典名.values()
参数：无
返回：dict_values对象（所有值）

示例：
```python
data = {"a": 1, "b": 2}
values = data.values()  # dict_values([1, 2])
```

────────────────────────────────────

【方法4：items()】⭐⭐⭐⭐⭐

语法：字典名.items()
参数：无
返回：dict_items对象（所有键值对）

示例：
```python
data = {"a": 1, "b": 2}
items = data.items()  # dict_items([('a', 1), ('b', 2)])
for key, value in data.items():
    print(key, value)
```

────────────────────────────────────

【方法5：pop()】

语法：字典名.pop(键, 默认值)
参数：
  - 键: 要删除的键
  - 默认值: 键不存在时返回的值（可选）
返回：被删除的值

示例：
```python
data = {"a": 1, "b": 2}
value = data.pop("a")  # 返回1，删除键"a"
```

────────────────────────────────────

【方法6：update()】

语法：字典名.update(另一个字典)
参数：
  - 另一个字典: 要合并的字典
返回：None（原地修改）

示例：
```python
dict1 = {"a": 1}
dict2 = {"b": 2}
dict1.update(dict2)  # dict1变为{"a": 1, "b": 2}
```

────────────────────────────────────

【方法7：clear()】

语法：字典名.clear()
参数：无
返回：None（清空字典）

示例：
```python
data = {"a": 1, "b": 2}
data.clear()  # data变为{}
```

────────────────────────────────────

【方法8：copy()】

语法：字典名.copy()
参数：无
返回：新字典（浅拷贝）

示例：
```python
dict1 = {"a": 1}
dict2 = dict1.copy()  # 复制字典
```

────────────────────────────────────

【方法9：setdefault()】

语法：字典名.setdefault(键, 默认值)
参数：
  - 键: 要查找或添加的键
  - 默认值: 键不存在时设置的值（可选，默认None）
返回：键对应的值

示例：
```python
data = {"a": 1}
data.setdefault("b", 2)  # 添加键"b"，值为2
data.setdefault("a", 100)  # 键"a"已存在，不修改
```
""")

# ============================================
# 第六部分：练习题
# ============================================

print("\n" + "=" * 60)
print("练习题")
print("=" * 60)

print("""
1. 创建一个学生信息字典，包含姓名、年龄、成绩，并打印所有信息
   提示：使用 items() 遍历

2. 编写程序，统计一个字符串中每个字符出现的次数
   提示：遍历字符串，用字典记录每个字符的出现次数

3. 创建两个字典，合并它们（如果有相同的键，保留第一个字典的值）
   提示：遍历第二个字典，检查键是否存在

4. 编写程序，反转字典（键值互换）
   提示：使用字典推导式

5. 创建一个通讯录程序，支持添加、查询、删除联系人
   提示：使用字典存储，姓名作为键，电话作为值

6. 编写程序，找出字典中值最大的键
   提示：使用 max() 函数和 items()

7. 创建一个字典，删除所有值为偶数的键值对
   提示：先收集要删除的键，再删除

8. 编写程序，判断两个字典是否相等
   提示：直接使用 == 比较

9. 创建一个嵌套字典（字典的值是字典），表示班级学生信息
   提示：{"学号1": {"name": "张三", "score": 85}, ...}

10. 编写程序，将列表转换为字典（索引作为键，元素作为值）
    提示：使用字典推导式和 enumerate()
""")

print("\n" + "=" * 60)
print("学习完成！")
print("=" * 60)
print("""
恭喜你完成字典基础的学习！

你已经掌握：
✅ 字典的概念和特点
✅ 字典的创建和访问
✅ 字典的增删改查
✅ 字典的遍历方法
✅ 字典的常用方法
✅ 字典的常见错误和陷阱

下一步：
→ 学习元组基础（11_元组基础_详解.py）
→ 完成学生信息管理系统项目（12_学生信息管理系统.py）
""")
