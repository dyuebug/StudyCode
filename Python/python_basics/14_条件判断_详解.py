# ============================================
# 条件判断 - 超详细注释版
# 日期：2026-03-08（第5天）
# 难度：⭐⭐ 中级
# ============================================

"""
【核心概念】条件判断（if-elif-else）

是什么？
- 条件判断是让程序根据不同情况执行不同代码的语法结构
- 它让程序不再只会“从上到下机械执行”，而是能够“做选择”

为什么需要？
- 现实中的很多问题都需要判断：成绩是否及格、年龄是否成年、密码是否正确
- 如果没有条件判断，程序就无法根据输入做出不同反应

核心特点？
- if：如果条件成立，就执行对应代码
- elif：前面的条件不成立时，再继续判断新的条件
- else：前面的条件都不成立时，执行这里
"""

# ============================================
# 第一部分：核心概念详解 ⭐⭐⭐⭐⭐
# ============================================

print("=" * 60)
print("第一部分：核心概念详解")
print("=" * 60)

# --------------------------------------------
# 1.1 什么是条件判断？
# --------------------------------------------

print("\n1.1 什么是条件判断？")
print("-" * 60)

print("""
【条件判断的概念】

条件判断就像现实中的“如果……就……”逻辑。

生活中的例子：
- 如果下雨，就带伞
- 如果成绩 >= 60，就及格
- 如果余额不足，就不能付款

程序中的作用：
- 根据不同输入，执行不同操作
- 让程序有“分支”能力
- 是后续学习循环、函数、项目逻辑的重要基础
""")

# --------------------------------------------
# 1.2 if语句的基本结构
# --------------------------------------------

print("\n1.2 if语句的基本结构")
print("-" * 60)

print("""
【语法结构】

if 条件:
    代码块

解释：
1. if 后面写“条件表达式”
2. 条件后面必须有英文冒号 :
3. 下一行必须缩进，表示“如果条件成立，要执行的代码”
4. 如果条件不成立，这个代码块会被跳过
""")

# --------------------------------------------
# 1.3 重要函数：input() / int() / float() / print()
# --------------------------------------------

print("\n1.3 相关函数卡片")
print("-" * 60)

print("""
【函数卡片1：input()】

函数名：input()
作用：获取用户输入的内容
类型：Python内置函数

完整语法：
input(提示文字)

参数详解：
1. 提示文字（可选）
   - 类型：字符串
   - 作用：显示给用户看的提示信息
   - 示例：input('请输入成绩：')

返回值：
- 返回字符串类型的数据

【函数卡片2：int()】

函数名：int()
作用：把字符串或数字转换成整数
类型：Python内置函数

完整语法：
int(数据)

参数详解：
1. 数据（必需）
   - 类型：字符串、整数、浮点数等
   - 作用：提供要转换的内容
   - 示例：int('80')

返回值：
- 返回整数

【函数卡片3：float()】

函数名：float()
作用：把数据转换成浮点数
类型：Python内置函数

完整语法：
float(数据)

返回值：
- 返回浮点数

【函数卡片4：print()】

函数名：print()
作用：把内容输出到屏幕
类型：Python内置函数

完整语法：
print(内容1, 内容2, ...)

返回值：
- 无返回值
""")

# --------------------------------------------
# 1.4 比较运算符和逻辑运算符
# --------------------------------------------

print("\n1.4 比较运算符和逻辑运算符")
print("-" * 60)

print("""
【比较运算符】

==   等于
!=   不等于
>    大于
<    小于
>=   大于等于
<=   小于等于

这些运算的结果只有两种：
- True（真）
- False（假）

【逻辑运算符】

and  并且：左右都为True，结果才是True
or   或者：左右只要有一个True，结果就是True
not  取反：把True变False，把False变True

优先级：
not > and > or
""")

# ============================================
# 第二部分：基础操作实践
# ============================================

print("\n" + "=" * 60)
print("第二部分：基础操作实践")
print("=" * 60)

# --------------------------------------------
# 2.1 最简单的if判断
# --------------------------------------------

print("\n2.1 最简单的if判断")
print("-" * 60)

# 创建一个变量保存成绩
score = 85

# 如果成绩大于等于60，就打印“及格”
# 这里的条件是：score >= 60
if score >= 60:
    print(f"成绩是 {score} 分，结果：及格")

# 如果条件不成立，对应代码块就不会执行
score2 = 45
if score2 >= 60:
    print("这行不会打印，因为45不满足 >= 60")

# --------------------------------------------
# 2.2 if-else 双分支结构
# --------------------------------------------

print("\n2.2 if-else 双分支结构")
print("-" * 60)

age = 17

# if-else 适合“二选一”的情况
# 如果条件成立，执行if代码块
# 否则，执行else代码块
if age >= 18:
    print(f"年龄是 {age} 岁：你已经成年")
else:
    print(f"年龄是 {age} 岁：你还未成年")

# --------------------------------------------
# 2.3 if-elif-else 多分支结构
# --------------------------------------------

print("\n2.3 if-elif-else 多分支结构")
print("-" * 60)

score3 = 88

# 多分支结构适合“多个条件依次判断”
# 执行顺序非常重要：只要某个条件成立，后面的分支就不再判断
if score3 >= 90:
    print(f"成绩 {score3} 分，等级：A")
elif score3 >= 80:
    print(f"成绩 {score3} 分，等级：B")
elif score3 >= 70:
    print(f"成绩 {score3} 分，等级：C")
elif score3 >= 60:
    print(f"成绩 {score3} 分，等级：D")
else:
    print(f"成绩 {score3} 分，等级：F")

print("""
为什么顺序重要？
- 因为程序是从上往下判断
- 一旦发现某个条件成立，就立刻执行并停止后续判断
- 所以范围大的条件不能乱放在前面
""")

# --------------------------------------------
# 2.4 逻辑运算符组合使用
# --------------------------------------------

print("\n2.4 逻辑运算符组合使用")
print("-" * 60)

math_score = 85
english_score = 78

# and：两个条件都满足才成立
if math_score >= 80 and english_score >= 80:
    print("数学和英语都优秀")
else:
    print("至少有一门没有达到优秀")

# or：只要一个条件满足就成立
if math_score >= 85 or english_score >= 85:
    print("至少有一门达到85分")

# not：把结果取反
is_raining = False
if not is_raining:
    print("现在没下雨，可以出门")

# --------------------------------------------
# 2.5 嵌套if语句
# --------------------------------------------

print("\n2.5 嵌套if语句")
print("-" * 60)

username = "admin"
password = "123456"

# 先判断用户名，再判断密码
if username == "admin":
    print("用户名正确")
    if password == "123456":
        print("密码正确，登录成功")
    else:
        print("密码错误，登录失败")
else:
    print("用户名错误，登录失败")

# --------------------------------------------
# 2.6 条件表达式（三元运算符）
# --------------------------------------------

print("\n2.6 条件表达式（三元运算符）")
print("-" * 60)

num = 10

# 条件表达式适合简单的二选一
# 格式：结果1 if 条件 else 结果2
result = "正数" if num > 0 else "非正数"
print(f"数字 {num} 的判断结果：{result}")

# --------------------------------------------
# 2.7 用户输入与条件判断结合
# --------------------------------------------

print("\n2.7 用户输入与条件判断结合")
print("-" * 60)

# 这里用固定字符串模拟用户输入，避免每次运行都必须手动输入
# 真正写交互程序时，可以把 simulated_score 换成 input()
simulated_score = "76"

# 先把字符串转换成整数
score4 = int(simulated_score)

# 再根据成绩做判断
if score4 >= 60:
    print(f"输入成绩 {score4} 分：及格")
else:
    print(f"输入成绩 {score4} 分：不及格")

# ============================================
# 第三部分：深入理解（选学）
# ============================================

print("\n" + "=" * 60)
print("第三部分：深入理解（选学）")
print("=" * 60)

print("""
🔍 深入理解：为什么 if 条件后面要加冒号？

如果你想知道“为什么”，可以阅读这部分。
如果觉得难，可以跳过，不影响后续学习。

原因：
- Python用冒号告诉解释器：“条件写完了，下面是对应代码块”
- 这是一种语法标记
- 如果没有冒号，Python就不知道后面的缩进代码属于谁

所以：
if score >= 60:
    print('及格')

这里的冒号不是装饰，而是语法的一部分
""")

print("""
🔍 深入理解：为什么缩进这么重要？

- 在很多语言里，用大括号表示代码块
- 在Python里，用“缩进”表示代码块
- 哪些代码属于 if，哪些不属于 if，靠的就是缩进层级

例如：
if True:
    print('A')
    print('B')
print('C')

这里 A 和 B 属于 if 代码块，C 不属于
""")

print("""
🔍 深入理解：短路特性是什么？

可以跳过，不影响后续学习。

短路是指：
- and：如果左边已经是 False，右边就不用再判断了
- or：如果左边已经是 True，右边就不用再判断了

这样做的好处：
- 更快
- 更安全
- 避免一些不必要的错误
""")

# ============================================
# 第四部分：常见错误和陷阱 ⭐⭐⭐⭐⭐
# ============================================

print("\n" + "=" * 60)
print("第四部分：常见错误和陷阱 ⭐⭐⭐⭐⭐")
print("=" * 60)

print("""
4.1 错误：把 = 写成 ==

❌ 错误代码：
if score = 60:
    print('及格')

结果：语法错误
原因：= 是赋值，== 才是比较是否相等

✅ 正确代码：
if score == 60:
    print('刚好及格')

记忆技巧：一个等号是“给值”，两个等号是“比一比”
""")

print("""
4.2 错误：忘记写冒号

❌ 错误代码：
if age >= 18
    print('成年')

结果：语法错误
原因：if 条件后必须加冒号

✅ 正确代码：
if age >= 18:
    print('成年')

记忆技巧：if、elif、else 后面都要带冒号
""")

print("""
4.3 错误：缩进不正确

❌ 错误代码：
if True:
print('Hello')

结果：缩进错误
原因：if 代码块必须缩进

✅ 正确代码：
if True:
    print('Hello')

记忆技巧：冒号后，下一行要缩进
""")

print("""
4.4 错误：多分支顺序写反

❌ 错误代码：
score = 95
if score >= 60:
    print('及格')
elif score >= 90:
    print('优秀')

结果：95分只会输出“及格”
原因：第一个条件已经成立，后面不会再判断

✅ 正确代码：
score = 95
if score >= 90:
    print('优秀')
elif score >= 60:
    print('及格')

记忆技巧：更严格、更高的条件放前面
""")

print("""
4.5 错误：直接把 input() 返回值当数字比较

❌ 错误代码：
score = input('请输入成绩：')
if score >= 60:
    print('及格')

结果：会出错，因为 input() 返回的是字符串
原因：字符串和整数不能这样直接比较

✅ 正确代码：
score = int(input('请输入成绩：'))
if score >= 60:
    print('及格')

记忆技巧：要做数学比较，先转成 int 或 float
""")

print("""
4.6 错误：以为多个 if 和 if-elif-else 一样

❌ 错误理解：
“多个 if 连着写，效果和 if-elif-else 一样。”

结果：不一定一样，多个 if 会各自独立判断
原因：if-elif-else 是“找到一个就停”，多个 if 是“每个都判断”

✅ 正确理解：
- if-elif-else：多选一
- 多个 if：每个条件都可能执行

记忆技巧：elif = 前一个不成立，才看下一个
""")

# ============================================
# 第五部分：函数卡片速查
# ============================================

print("\n" + "=" * 60)
print("第五部分：函数卡片速查")
print("=" * 60)

print("""
【函数1：input()】

语法：input(提示文字)
参数：
  - 提示文字：显示给用户看的提示
返回：字符串

示例：
```python
name = input('请输入姓名：')
```

────────────────────────────────────

【函数2：int()】

语法：int(数据)
参数：
  - 数据：要转换的内容
返回：整数

示例：
```python
score = int('80')
```

────────────────────────────────────

【函数3：float()】

语法：float(数据)
参数：
  - 数据：要转换的内容
返回：浮点数

示例：
```python
height = float('1.75')
```

────────────────────────────────────

【函数4：print()】

语法：print(内容1, 内容2, ...)
参数：
  - 内容：要输出的值
返回：无返回值
""")

# ============================================
# 第六部分：练习题
# ============================================

print("\n" + "=" * 60)
print("第六部分：练习题")
print("=" * 60)

print("""
1. 基础if练习
   要求：
   - 创建变量 temperature = 32
   - 如果温度大于30，打印“天气很热”
   提示：使用 if

2. if-else练习
   要求：
   - 创建变量 age = 20
   - 如果年龄大于等于18，打印“你是成年人”
   - 否则打印“你还未成年”
   提示：使用 if-else

3. if-elif-else练习
   要求：
   - 创建变量 score = 92
   - 根据分数输出等级 A/B/C/D/F
   提示：注意判断顺序

4. 逻辑运算练习
   要求：
   - 创建两个变量：math_score = 85，english_score = 90
   - 如果两门都大于等于80，打印“成绩优秀”
   - 如果至少一门大于等于90，打印“有一门特别突出”
   提示：分别使用 and 和 or

5. 综合练习：登录验证
   要求：
   - 创建用户名和密码变量
   - 判断用户名是否正确
   - 如果用户名正确，再判断密码是否正确
   - 输出登录成功或失败信息
   提示：可以使用嵌套 if
""")

print("\n" + "=" * 60)
print("🎉 第5天学习材料2完成！")
print("建议：重点理解 if-elif-else 的执行顺序和缩进规则")
print("下一步：学习 15_成绩等级评定系统.py")
print("=" * 60)
